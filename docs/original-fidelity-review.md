# TBH Original Fidelity Review

Last reviewed: 2026-06-19

Current audit delta: Battle scene combatants now keep log-triggered action frames visible longer and layer local pixel speed-line cues plus hit squash/flash feedback over the existing hero, support and monster sprites. Fixed-time battle-scene fixtures also derive this action pose from the latest fixture log entry so screenshot review can catch blank or regressed strike feedback. This is a local readability and motion-feedback improvement only: it does not add verified original attack, hit, death or cast keyframes, and `source_battle_animation_exact_action_frames` remains `0`.

Current audit delta: Battle scene terminal states now add local victory/defeat finish cues directly inside the combat lane: a gold pixel burst on the monster side for victory and a red shard cue on the player side for defeat. `BattleSceneSnapshot` exposes deterministic `victoryFinishScene` and `defeatFinishScene` fixtures, `SelfTest` renders both, and `scripts/audit-local-battle-scene.sh` measures `victory_finish_gold_pixels` and `defeat_finish_red_pixels` so those cues cannot silently disappear. This is local end-of-battle readability only; it does not add verified original death, victory, defeat, hit-stop, loot, or cast keyframes, and `source_battle_animation_exact_action_frames` remains `0`.

Current audit delta: Settings now keeps a compact `剩余硬缺口` queue at the top of the original-fidelity boundary panel. The queue deliberately lists the blockers that still cannot be promoted into runtime or original-parity claims: `60` pending source skills with `0` minimum-evidence candidates, `194` Rune nodes still waiting for cost/refund evidence, the local pacing scaffold (`Tick 1.5s`, combat delta `67%`, combat substep `0.25s`, `35%` applied XP, stage cap `+2`) that is not yet proven to match the original timing/XP curve, `0/5760` exact item records, `3` source monsters still lacking stage/art/runtime proof (`剧毒领主 / 扁虱 / 雪山法师`), `0` verified original action-frame groups, and `0` isolated original per-event SFX. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` now guard this as `original_fidelity_hard_gap_rows=7`. This is a review navigation surface and regression guard only; it does not hide the underlying `gap` rows and does not generate skills, Rune prices, original pacing values, item variants, monsters, action frames, or original audio.

Current audit delta: `scripts/audit-local-gameplay-fidelity.sh` now guards the source monster attack-speed quantization explicitly: source cooldown range `0.6s-2.5s`, local base minimum attack interval `1.0s`, hasted minimum attack interval `0.25s`, internal combat simulation step `0.25s`, and local loop-quantized cooldown range `1s-2.5s`. The outer runtime tick remains `1.5s` for slower idle-game progression, and `GamePacing.simulatedCombatDelta(for:)` now advances only `67%` of wall-clock tick time into the battle simulator, so each tick records real play time while combat itself advances about `1.0s` per `1.5s` wall-clock tick. Combat resolution still advances in smaller substeps so source-backed fast monsters and cooldown/DoT effects are not forced into one coarse bucket. The base minimum keeps normal combat from speeding up globally, while the hasted minimum lets checked attack-speed buffs such as `迅捷觉醒` produce a measurable attack-frequency change. Settings now also shows the current `等级上限`, a formula breakdown from `HeroLevelPacing.levelCapBreakdown(for:)`, a compact three-state `等级状态` from `HeroLevelPacing.levelCapStatus(for:progress:)`, and an `XP余量` row showing whether the hero can still level or whether upgrade progression has stopped at the local cap. The battle victory reward banner now reuses the same local cap status to show `已达等级上限 · 升级停止` when a positive source XP reward is reduced to `+0XP` by the local cap; when positive XP is merely reduced by the local applied-XP pacing path, it shows a compact `XP实得 source->displayed` row so the player can see why the reward number is lower. The same reward detail row now also shows `金币实得 source->displayed` when local reward-rune scaffolds change displayed gold, and `清理 xN` when one battle result aggregates multiple cleared encounters. `BattleSceneSnapshot` now renders deterministic `victoryRewardBanner` and `victoryLevelCapBanner` fixtures through the real SwiftUI `BattleResultBanner`, and `scripts/audit-local-battle-scene.sh` measures visible success, reward text, source-backed loot-icon/color, and level-cap warning pixels; the latest local run measured `victory_reward_banner_non_dark_pixels=9165`, `victory_reward_banner_rarity_pixels=781`, `victory_reward_banner_icon_pixels=1562`, and `victory_level_cap_banner_orange_pixels=1327`, so those local reward explanations cannot disappear without failing the screenshot gate. Startup loading now also persists stale over-cap save normalization after the offline timestamp check, so legacy/test saves with runaway level, negative XP or over-max HP do not remain on disk once the local cap has corrected them. This keeps the original-speed and reward-readability review boundaries visible without claiming exact original attack cadence, leveling-curve parity, exact original reward economy parity, or exact original victory-banner UI parity.

Audit gate maintenance: the local fidelity audit now parses runtime `Skill(...)` definitions with the same balanced Swift call-block scanner used by battle-log checks and keeps pending activation/damage queue grouping in a scoped variable. This restores the guard for `36/36` modeled named hero active skills, `36/36` modeled active-skill value tables, and the two source `CONTINUOUS` Priest blessing status badges (`力量祝福`, `守护祝福`) without changing gameplay behavior or promoting any pending source skill into runtime.

Monster audit status maintenance: the local fidelity audit now classifies source-monster database row counts, unique IDs, unique names, stage-composition coverage, runtime ATK/speed coverage, source-only sprite resources, source-only sprite preview rows, source-only proof matrix rows, and monster-art evidence gate/queue counts as `guarded` once they meet the current checked baseline. The real unresolved monster runtime/stage gap remains explicit as `source_monster_art_evidence_queue_source_roster_gap=3` for `剧毒领主 / 扁虱 / 雪山法师`, while the same three source-only rows now have guarded `source_monster_20042`, `source_monster_20121`, and `source_monster_30044` PNG resources with `source_monster_source_only_sprites=3/3`, `source_monster_source_only_sprite_preview_rows=3/3`, and `source_monster_source_only_proof_rows=3` in Settings. The proof matrix records that all three rows still miss stage-composition proof, runtime encounter proof, skill ownership proof, original animation frames, and isolated original SFX. This status change does not add encounters, infer stage slots, create action frames, generate monster skills, or claim original per-monster animation parity.

Source-only monster page cross-check maintenance: the `taskbarhero.org` monster pages and same-site stage pages now give stronger source-family evidence for two of the three source-only monster rows. `20042` / `剧毒领主` has 5 monster-page stage-appearance rows, with same-site stage pages confirming `4207=74` and `3204=39`; `30044` / `雪山法师` has 9 monster-page stage-appearance rows, with same-site stage pages confirming `4303=70` and `2303=30`; `20121` / `扁虱` still has an empty stage-appearances table, although the same monster page exposes Physical damage and range `130`. Settings now surfaces this as `sourceOnlyStageAppearanceCoverageText=2/3`, `sourceOnlyStageAppearanceTotalStageRows=14`, and `sourceOnlyStageAppearanceCrossCheckPageCount=4`, and the local fidelity audit guards the same values as `source_monster_source_stage_*` rows. This is still one fan-Wiki source family at `v1.00.13`, not independent official/in-game proof. The local `StageTable` still does not add these monsters to runtime composition, and no wave order, spawn slot, skill ownership, delivery shape, action-frame, art-anchor, drop-table, or isolated SFX claim is promoted from this evidence.

Source-only monster page-field maintenance: the same `taskbarhero.org` monster pages now have explicit page-field evidence for all three source-only rows. `20042` / `剧毒领主` uses `/assets/tbhdb/game/monsters/RedExplosionInsect/RedExplosionInsect_Idle_character_2.png` with `Move 220 / Damage — / Range —`; `20121` / `扁虱` uses `/assets/tbhdb/game/monsters/GiantTick/GiantTick_Idle_character_4.png` with `Move 400 / Damage Physical / Range 130`; `30044` / `雪山法师` uses `/assets/tbhdb/game/monsters/FrozenWizard/FrozenWizard_Idle_character_2.png` with `Move 220 / Damage — / Range —`. Settings exposes this as `sourceOnlyPageFieldSummaryText=页面字段 3/3 / sprite URL 3 / Move 3 / Damage 1 / Range 1`, and the local fidelity audit guards the values as `source_monster_source_page_field_*` rows. This is still same-source-family evidence only: the local runtime does not treat the sprite URL as an art-anchor proof, does not map `Move` into movement or attack cadence, does not treat `Damage Physical / Range 130` as skill ownership or hit-shape proof, and does not claim action-frame, VFX, SFX, drop-table, or stage-slot parity from these fields.

Pacing audit status maintenance: the local fidelity audit still classifies the already guarded local pacing constants as `guarded` rather than generic `gap`: runtime tick `1.5s`, combat simulation delta multiplier `67%`, combat simulation substep `0.25s`, applied XP multiplier `35%`, stage-level cap buffer `+2`, minimum attack interval `1.0s`, and hasted minimum attack interval `0.25s`. Settings now also lists original timing/XP proof as a top-level hard gap so the local slowdown cannot be mistaken for original parity. The script fails if those constants drift, if the `GamePacing`/Settings/self-test wiring disappears, or if the top-level pacing hard-gap row is removed. This status change only separates the current local slow-speed regression guard from true source-data gaps; it does not claim exact original tick cadence, XP curve, attack animation timing, support cooldown formulas, or level-cap parity.

Source skill audit status maintenance: the local fidelity audit now classifies checked source-skill review coverage as `guarded` once it matches the current extracted baseline: damage bucket counts, activation/damage and activation/delivery cross-tabs, damage/delivery cross-tabs, delivery and range distributions, checked pending `COOLDOWN/Chaos` value fields, and the non-pending local runtime coverage summary rows. The true runtime gap remains explicit as `local_skill_runtime_coverage_pending=60` plus pending rows such as `source_skill_baseattack_physical_pending=37`, `source_skill_cooldown_chaos_pending=3`, empty-delivery pending rows, and the pending-source evidence queues. This status change only distinguishes source-catalog review coverage from executable combat semantics; it does not add skill ownership, formulas, delivery shapes, action frames, VFX, SFX, or trigger cadence for pending source skills.

Pending source skill proof-matrix maintenance: Settings now adds a compact `runtime 证明矩阵` inside `待接入源技能复核`. It records positive evidence separately from missing runtime proof: all `60/60` pending rows have source-catalog entries, `15/60` have checked value/range detail pages, and `0/60` meet the current minimum runtime evidence gate. The same matrix keeps the real blockers explicit as `60` missing localized name/description proofs, `60` missing delivery/hit-shape proofs, `60` missing ownership/target/formula proofs, `60` missing original action-frame/VFX proofs, and `60` missing isolated original SFX proofs. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard the matrix rows and counts; this does not connect pending source skills to runtime, generate formulas, infer delivery, create VFX, or claim original SFX parity.

Pending source skill audit classification maintenance: the local fidelity audit now treats established pending-skill review surfaces as `guarded` once they match the checked extracted baseline: activation/damage/prefix/responsibility buckets, exact pending ID manifests, runtime gates, mutually exclusive evidence queues, activation-damage/range/prefix/value review queues, visual-priority queues, unqueued-diff review queues, per-skill value/base-attack evidence rows, same-prefix unmapped-monster candidate rows, checked monster attack review rows, and source range buckets. The true source-skill runtime gaps remain `gap`: `60` pending rows, `60` empty delivery rows, `60` six-digit unnamed rows, `0` minimum-evidence candidates, `44` rows outside the visual-priority queue, all missing localized identity, delivery/hit-shape, ownership/target/formula, original action-frame/VFX, and isolated original SFX proof. This classification change only separates review coverage from runtime readiness; it does not promote pending skills into combat.

Pending skill visual-priority maintenance: Settings now adds a `视觉复核优先队列` inside the pending-source skill review so art/effect work can start from the overlap of visible-risk evidence instead of comparing value, range, damage and unmapped-monster rows manually. The queue is intentionally overlapping rather than mutually exclusive: `14` elemental pending rows, `3` checked COOLDOWN Chaos value rows, `3` unmapped-monster same-prefix rows, and `2` highest-value detail pages currently produce `22` review entries across `4` high-priority queues and `16/60` unique pending skills. The same panel now treats the two explicit low-priority backlog queues for the remaining Physical value/catalog rows as part of the overall visual-review route, so priority plus backlog coverage is guarded as `6` queues and `60/60` pending skills. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard those counts and the boundary text that forbids generating素材,特效,公式,弹道,动作帧或音效 from element labels, source values, ID prefixes, unmapped-monster relationships, backlog membership or coverage status alone.

Runtime scaffold audit status maintenance: the local fidelity audit now classifies implemented local scaffolds as `guarded` when they match their checked baseline: runtime Rune Tree node coverage and current local stat/reward/inventory/storage/offline scaffolds, modeled local Rune prerequisite edges, direct repeatable backpack expansion, weaker-equipment handling modes, active equipment-slot coverage, player-side status/deployable battle feedback, local Rune-cost review surfaces, and exact-item-record review queues. Rune cost review navigation rows are also guarded when they match the checked current review surface: `local_rune_cost_review_pending_groups=37` source-icon groups and `local_rune_cost_review_pending_branches=7` gameplay branches. True source gaps remain explicit as `rune_unverified_cost_nodes=195`, `rune_approximate_cost_nodes=1`, `local_rune_cost_review_pending=194`, `exact_item_record_gap_review_missing=5760`, and `exact_item_records=0/5760`. This status change only protects current local behavior and review surfaces from regression; it does not invent Rune costs, exact item variants, original reset economy, original backpack caps, original buff-bar layout, or exact item affix rolls.

Audio evidence gate maintenance: Settings now includes a dedicated `原版音频/SFX 证据` review panel that puts the Steam Trailer broad audio baseline beside the local generated SFX manifest. It exposes six evidence rows covering Trailer duration/format/loudness, loudness range/true peak, the 11 local WAV cues, runtime routing, package audit coverage, and the current `0` isolated original per-event SFX gap. The same panel now adds an `原版单事件 SFX 接入门槛` queue with `7` missing evidence gates: basic combat hit, skill cast/release, projectile/impact/elemental events, buff/status loops, loot/inventory/UI events, mix/throttle/randomization, and package provenance. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` now guard `source_audio_sfx_evidence_rows=6`, `source_audio_sfx_event_gate_rows=7`, `source_audio_sfx_local_events=11`, `source_audio_sfx_local_resources=11`, `source_audio_sfx_original_isolated=0`, `source_audio_sfx_steam_duration_seconds=47`, and `source_audio_sfx_steam_sample_rate_hz=48000`; the guard also checks that `sfx_manifest.tsv` keeps every cue at `generated_substitute` with `officialAudio=false`. This improves in-app evidence visibility without claiming original attack, hit, loot, UI or skill SFX parity.

Battle animation evidence gate maintenance: Settings now also includes `原版战斗动画证据`, mirroring the official Steam battle-scene media baseline (`776x180`, `30fps`, `184` frames, frame `0` -> `8` motion sample over `0.267s`) beside the current local deterministic render footprint (`1872x4608` battle scene, `1920x5108` full Battle tab, local ratio `0.41:1`) and the local macOS Battle tab layout footprint (`960x2554pt / content 2448pt / scene 2304pt / tab 46pt`). The same Settings panel now breaks the official frame-pair sample into four review-only rows: frame pair, full-frame delta `26,623 px / 0.1906`, lower-platform delta `11,920 px / 44.8%`, and non-platform delta `14,703 px / 55.2%`; the local audit now reports `source_battle_animation_motion_sample_rows=4` as a guarded evidence surface rather than a generic gap once those rows and their boundaries are present. It also exposes an `原版动作帧接入门槛` queue with `8` missing evidence gates: hero idle/move, hero attack/cast, hero hit/death, support party actions, monster idle/move, monster attack/hit/death, projectile/impact/status effects, and timing/audio sync. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard these values as review evidence, including `source_battle_animation_action_frame_gate_rows=8`, while keeping the exact original action-frame count at `0`; this makes the enlarged bottom-tab layout, visible motion evidence, and future action-frame review order harder to regress without claiming original idle/attack/hit/death keyframe parity or exact original Windows taskbar window proportions.

Enemy status visual maintenance: the battle lane now renders local body-effect cues for modeled enemy `眩晕`, `寒冷`, `冻结`, and `出血` states in addition to the compact badge row, and the deterministic screenshot audit includes an `enemyStatusEffects` fixture with separate chilled/frozen/stunned/bleeding pixel gates. This improves local status readability and catches regressions where enemy statuses fall back to tiny icons only; it does not prove exact original status icon art, status formulas, durations, stacking rules, resistance ordering, or animation keyframes.

Battle contact feedback maintenance: the battle lane now renders compact contact-pulse cues for hero-side damage landing on enemies and monster-side damage landing on the party, and the deterministic screenshot audit includes `heroContactPulse` and `monsterContactPulse` fixtures with separate pixel gates. This improves local hit readability inside the enlarged battle area without changing combat mechanics; it does not prove exact original hit sparks, contact frames, screen shake, impact timing, or action keyframes.

## Source Quality

| Source | Scope | Confidence | Notes |
| --- | --- | --- | --- |
| Steam store page for `TBH: Task Bar Hero` | Official gameplay positioning, feature list, visual media links | High | Official product page, refreshed 2026-06-19 from AppID `3678970` (`https://store.steampowered.com/app/3678970/TBH_Task_Bar_Hero/`). Confirms tiny taskbar idle RPG, pixel heroes, classes/items/skills/builds, Cube, 3 Acts, 4 difficulty tiers, 500+ items, 50+ monster types, 10 rarity grades. Direct stream checks found the official Trailer HLS exposes AAC 48 kHz stereo audio plus H.264 video variants, while the embedded Steam extras checked in this pass (`wishlist`, `interface`, `battlescene`, `cube`, and the `battlescene` WebM) expose video streams only. `docs/steam-audio-audit.md` now records the reproducible ffprobe/ffmpeg audit: the current 47s Trailer stream is AAC LC 48 kHz stereo, measured at about -15.3 LUFS integrated loudness, 4.6 LU loudness range and 0.0 dBFS true peak. `docs/steam-battle-scene-audit.md` now records the reproducible battle-scene audit: the current official `extras/battlescene` media is video-only, 776x180, 30 fps, about 6.133s, 184 frames, with a 4.31:1 strip composition, and the local app now has a deterministic `TBH --render-battle-scene` PNG path plus repeatable ground-strip geometry and primary/support party-layout checks for rendered `BattleSceneView` output. These media prove broad official visual/audio presentation, but not isolated per-event SFX or exact battle logic. |
| `taskbarhero.org/zh` Wiki | Detailed mechanics, database pages, Chinese labels | Medium | Refreshed 2026-06-15. Page footer states it is an unofficial fan Wiki. Useful for detailed database alignment, but should not override Steam or in-game evidence. The items page cross-checks Steam's 10-grade rarity ladder with Chinese labels, colors, Alchemy value, Cube XP, and D/E/I slot counts; it also lists four Soul Stone material rows (`190001`-`190004`) and 59 stage chest rows across Normal Monster Box, Stage Boss Box and Act Boss Box families. The Chinese and English items pages both state that Synthesis combines 9 same-rarity items into 1 item of the next tier, can occasionally skip a tier, and that low Synthesis level can downgrade output item level; the same table gives approximate examples such as Common -> Rare `~60%`, Uncommon -> Legendary `~40%`, Rare -> Legendary `~20%`, and Legendary -> Immortal `~5%`, but it does not expose the complete probability table, failure/skip resolution, Synthesis-level progression costs or item-level downgrade formula. The drops tool states its current local snapshot has no complete item drop table and that box-stage relations are inferred by box family, Boss status and stage level; currently checked source examples include `1101` -> `Normal Monster Box 1` / `Stage Boss Box 1` / `HP 470` / `哥布林盗贼:1, 史莱姆:5, 哥布林:5`, `1109` -> `Normal Monster Box 3` / `Stage Boss Box 6` / `HP 6340`, `1110` -> `Normal Monster Box 3` / `Act Boss Box 1` / `HP 1050` / `骷髅王:1`, and `4310` -> `Normal Monster Box Lv90` / `Act Boss Box Lv90` / `HP 3550` / `执政官莫尔卡:1`. The stages page lists all 120 difficulty stages with level, waves, kill count, gold, XP and monster/Boss names, plus Boss HP and item-level thresholds. The guide page documents Alt+click item locking and prioritizing Rune Tree party slots for deploying heroes 2 and 3. The skills page lists 106 active/attack skills, 108 passive skills, and `CONTINUOUS`, `COOLDOWN`, `BASEATTACK`, `BASEATTACK_COUNT` activation types; direct page-source parsing of `/zh/skills/` and `/en/skills/` produced the same 108 passive rows with matching `data-id`, `data-stat`, `data-type` and `data-value`, distributed as 18 rows per class prefix. Current checked examples include `10101`/`10301`/`20101`/`20501`/`20601`/`50101`/`50601`/`60201`/`60401` as attack-count skills and `40201`/`40501` as continuous blessing skills, plus passive rows such as `101001 AttackDamage FLAT 1`, `201011 CriticalChance ADDITIVE 200`, `501021 FireDamagePercent FLAT 150`, and `601072 SkillDurationIncrease ADDITIVE 80`. Individual active-skill pages expose 10-level value tables for the 36 currently named hero skills, including `10101` Lv1/Lv10 `2500/4300`, `20101` `快速射击` with `BASEATTACK_COUNT`/攻击次数 trigger, physical projectile hit type, range `1150`, Lv1/Lv10 `1320/2400`, and description text stating that it fires multiple arrows with each dealing `1320%` physical damage, `20501` `穿透之箭` with `BASEATTACK_COUNT`/攻击次数 trigger, physical projectile hit type, range `1200`, Lv1/Lv10 `2440/3880`, and description text stating that it fires a piercing arrow dealing `2440%` physical damage to hit enemies, `20601` `穿刺射击` with `BASEATTACK_COUNT`/攻击次数 trigger, physical projectile hit type, range `1200`, Lv1/Lv10 `1000/3700`, and description text stating that each lodged arrow increases damage by `1000%` and 3+ lodged arrows cause bleeding, `50101` `爆炸弩箭` with `BASEATTACK_COUNT`/攻击次数 trigger, fire damage, projectile hit type, range `1100`, Lv1/Lv10 `4840/9430`, and description text stating that it fires a bolt that explodes on hit to deal `4840%` range fire damage, `50201` `寒霜弩箭` with `COOLDOWN`/冷却 trigger, cold damage, projectile hit type, range `1100`, Lv1/Lv10 `2100/3450`, and description text stating that it fires an exploding bolt dealing `2100%` range cold damage and freezing hit enemies, `10401` `神盾领域` with `COOLDOWN` trigger, range `150`, Lv1/Lv10 `500/1850`, and description text stating that it blocks `500` damage for allies in the field, `10601` `不屈意志` with `COOLDOWN` trigger, range `150`, Lv1/Lv10 `300/1200`, and description text stating that once per stage it stands back up with `300%` HP when near death, `20401` Lv1/Lv10 attack speed `500/1400`, `40101` Lv1 `100`, `40201` Lv1 `500`, `40301` `天堂之怒` with `COOLDOWN` trigger, lightning range `1050`, Lv1/Lv10 `4300/7900`, and description text stating that attacks deal `4300%` lightning range damage while active, `40401` `圣域` with `COOLDOWN` trigger, range `1100`, Lv1/Lv10 `300/1920`, and description text stating that allies in the field recover `300` HP per second, `60201` Lv1/Lv10 `6200/9800`, `60301` `将军怒吼` with `COOLDOWN` trigger, physical AOE, range `150`, Lv1/Lv10 `500/950`, and description text stating that it stuns surrounding enemies and raises party critical chance coefficient by `500%`, and `60601` `嗜血` with `COOLDOWN` trigger, range `900`, Lv1/Lv10 `4000/6700`, and description text stating that it consumes half of current HP and increases attack damage by `4000%` for a time; the `10501` page states `神圣之刃` is a cooldown buff with `攻击力+50%` and Lv1/Lv10 on-hit healing `20/200`, while the `60201` page states that killing an enemy with the hit creates a shockwave dealing `350%` damage to surrounding enemies. Inspected pages do not expose the hidden attack-count cadence, exact `20101` arrow count/timing, exact `20501` pierce line geometry/hit count, exact `20601` lodged-arrow duration/reset rules or bleed damage/duration, exact `50101` explosion radius/impact point/projectile timing/fire status/resistance rules, exact `50201` freeze duration/stacking/explosion radius/projectile timing/cold status/resistance rules, passive unlock paths/runtime application semantics, buff durations for `10401`/`10501`/`20401`/`40301`/`40401`/`60301`/`60601`, or exact stun duration/critical-coefficient formula for `60301`, so the current 3-hit cadence, the `20101` minimum two-arrow scaffold, the `20501` current-wave pierce scaffold, the `20601` lodged-arrow/bleed marker scaffold, the `50101` current-wave explosion scaffold, the `50201` current-wave freeze scaffold, passive effects, those buff durations and the `60301` stun timing remain placeholders. |
| Current macOS app source | Implemented behavior | High | Local repository is authoritative for current implementation. |

Additional official media refresh in this pass: `scripts/audit-steam-audio.sh` re-fetched the live Steam store page and current Trailer HLS on 2026-06-16. The HLS URL, 47s duration, AAC LC 48 kHz stereo audio stream, H.264 video variants, `-15.3 LUFS` integrated loudness, `4.6 LU` loudness range, `0.0 dBFS` true peak, `-16.4 dB` mean volume, and no-sustained-silence result still match the documented baseline. `scripts/audit-steam-battle-scene.sh` also re-fetched the live `extras/battlescene` MP4/WebM pair on 2026-06-16; the current media remains video-only, 776x180, 30 fps, about 6.133s, 184 frames, with lower warm platform geometry `width_ratio=0.700`, `x_start=0.137`, `x_end=0.836`, and `y_start=0.678-0.733`. The same audit now compares official frame `0` to frame `8` over about `0.267s`, measuring `official_motion_pixels=26623`, `official_platform_motion_pixels=11920`, `official_non_platform_motion_pixels=14703`, and `official_motion_percent=0.1906`; Settings now exposes those measurements as four frame-pair detail rows so the platform and non-platform portions remain visible during review. This refresh strengthens the external media evidence date and proves visible frame-to-frame motion in the sampled official promotional loop, but it still proves only broad official presentation, not exact frame timing, sprite keyframes, battle logic, or isolated per-event SFX.

Additional local package verification in this pass: after rebuilding `dist/TBH.app`, the local art, Rune icon, passive icon, app icon and SFX audits verified the packaged assets listed below. `scripts/audit-local-gameplay-fidelity.sh` now also reports and guards the deterministic battle scene render size `1872x4608`, configured local battle-scene ratio `0.41:1`, menu-bar popover default size `960x2554`, content min-height `2448`, bottom-tab source order, local platform width `90%`, visible battle log limit `50`, minimum hero-side retained log rows `8`, player-side highlight log limit `3`, compact scrollable battle log panel height `124`, the six class-specific `battle_hero_*` combat sprite mappings, the six `official_hero_*` source sprites backing those identity checks, the full `battleTabLayout` snapshot guard, the `inventoryPanel` and `characterPanel` snapshot guards, the `victoryRewardBanner` and `victoryLevelCapBanner` snapshot guard, the `completionSettlement` snapshot guard, `official_steam_battle_motion_guard`, the guarded `source_audio_sfx_*` Settings metrics listed above, the guarded `source_battle_animation_*` Settings metrics listed above, `GamePacing` runtime tick `1.5s`, combat delta multiplier `67%`, internal combat simulation step `0.25s`, applied XP multiplier `35%`, stage-level cap buffer `+2`, minimum attack interval `1.0s`, source monster loop-quantized cooldown range `1s-2.5s`, exact first-runtime-victory XP application through `GamePacing.pacedXP`, and the source-backed runtime Rune Tree facts currently implemented locally. This makes the broader fidelity summary catch regressions in the enlarged battle tab layout, bottom menu placement, inventory comparison/readability, character hero/party/loadout readability, reward-banner readability, completion-settlement readability/action visibility, official Steam battle-motion audit coverage, local/generated SFX provenance visibility, local/original animation evidence boundaries, slower local runtime pacing, checked Rune runtime boundary, hero-side battle-log retention, source-backed monster attack-speed substep resolution, battle-hero class identity/provenance coverage, and XP-overlevel prevention instead of leaving those checks only inside Swift tests, resource self-tests and scattered UI behavior. `scripts/audit-local-battle-scene.sh` can rerun the deterministic scene checks against the enlarged layout and against staged copies of the packaged executable and packaged SPM resource bundle with `PACKAGED_BATTLE_SCENE_RENDER=1 RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh`, so local release checks cover both SwiftPM rendering and bundled-artifact rendering while avoiding direct `.app` command-line registration. The same visual audit now renders a full Battle tab layout PNG with the real hero summary bar, enlarged battle scene, log panel and bottom tab bar. These are local regression guards over generated/substitute assets, deterministic renders and checked source-page artwork, not proof of exact original isolated SFX, full original Rune Tree behavior, exact original hero animation frames, exact original inventory layout, exact original character panel layout, exact original reward-banner layout, exact original completion-settlement UI, exact original full-window layout, or exact original Windows taskbar icon parity.

The broad local gameplay audit now distinguishes locally guarded layout/readability baselines from true original-data gaps: enlarged popover size, bottom-tab placement, deterministic battle render size, complete Battle tab layout snapshot coverage, local platform width, visible log count, player-side highlight rows, battle-log panel height, and battle-hero sprite identity coverage report as `guarded` once they meet the checked current baseline, while exact item records, unverified Rune costs, and unmapped source skills remain explicit `gap` rows. The Settings original-fidelity panel now also exposes `6/6` battle hero sprite mappings and `6/6` official hero source sprites while keeping original battle-pose/action-frame parity as an unresolved art gap, exposes the checked monster data/art split as `61/61` source monster stat rows, `52/50+` source roster identity coverage of the Steam lower bound, `49/49` stage-composition name resolution, `49/52` current stage-composition battle-art mapping coverage, and `3/3` source-only sprite resources for the source rows that still do not enter current stages. The source-roster runtime gap remains explicit too: 3 source monster rows, `20042:剧毒领主`, `20121:扁虱`, and `30044:雪山法师`, are present in the source monster database and now have guarded `source_monster_*` PNG resources, but they are not in the current stage-composition art mapping or runtime encounter mapping. Settings exposes those rows as data-only: no current stage-composition slot, battle encounter, action-frame evidence, skill ownership or drop table is inferred from their sprite resources. The per-monster queue now also cross-checks each parseable `bestFarm` stage code against the current 120-row stage-composition table: `4207` does not list `剧毒领主`, `4303` does not list `雪山法师`, and `扁虱` keeps the source-table `—` best-farm value as an explicit source-absent field rather than a local missing-data TODO. The audit now reports that single source-absent best-farm row as `source_absent`, while still treating the unmapped monster rows as real runtime/action-frame evidence queues. It also surfaces the same-prefix source-skill candidates `200421` Chaos BASEATTACK, `201211` Physical BASEATTACK, and `300441` Cold BASEATTACK as review-only rows; the same-site detail pages now only add `value 1000` plus range/page-field proof for those three rows, while the ID prefix and value fields still do not prove monster skill ownership, formulas, delivery, action frames, SFX, or runtime eligibility. The same panel now adds 5 unmapped-monster evidence gates for stage slot, battle art, runtime encounter, attack/skill source, and animation/SFX, plus 3 per-monster evidence queues covering those unmapped source rows; those gates and queues only define missing proof and explicitly do not generate encounters, skills, drops, action frames, SFX or original stage placement. It now exposes the checked monster art boundary as `49/52` plus `source_monster_source_only_sprites=3/3`, so the 49 stage-composition mappings are not mistaken for complete source-roster art/runtime parity and the 3 source-only sprites are not mistaken for spawn evidence. The legacy Steam lower-bound gap is `0` because the source table has 52 unique names, while the remaining true runtime/stage gap is the 3 source-only monster rows. It also adds a dedicated skill-effect boundary where `60` source skills remain outside runtime, and now exposes why those skills stay outside runtime: `15/60` pending rows have checked value/range detail pages, but those 15 still appear only as `Skill ID`, have no localized name/description, have empty `delivery`, and therefore do not prove ownership, target rules, formulas, hit shape or duration. The same panel now mirrors the passive-skill source boundary: `108/108` passive rows and `104/108` source-backed passive icons are visible in the high-level boundary while `IncreaseProjectileDamage` and `SkillHealIncrease` remain missing-source-icon families instead of local substitutes. It also surfaces the backpack and storage capacity boundary: all `26/26` checked `MaxInventorySlot` Rune of Expansion rows are wired to the local `+10` slot scaffold, the inventory-panel direct expansion path is shown as a repeatable local `+10` slot purchase with `50,000G` first cost and `100,000G` second cost, and all `3/3` checked `UnlockStashPageCount` Rune of Storage rows are wired to a local `+20` capacity scaffold folded into the same backpack capacity. Original exact costs, caps, stacking, independent stash-page layout, page limits, path costs and reset economy remain explicit gaps. Current skill VFX/SFX are still local auditable substitutes rather than original per-skill action-frame, hit-presentation, trigger-timing or original-audio parity. The local crafting/Cube review now also exposes the checked local reward-rune scaffold: 4 Forging/Cube XP rows and 4 Alchemy Gold rows use a local `+10%` per-node multiplier, for a current single-side scaffold cap of `+40%`, while exact Rune costs, Cube level rewards and the original alchemy economy curve remain explicit gaps. The local Rune cost review also guards the one official branch-level approximate source row, `2nd Active Skill Slot (~50,000g)`, as source evidence only: it remains excluded from verified refunds and does not reduce the broader per-node cost gap. That approximate row is now also exposed as a dedicated `1/1` evidence row for Rune of Awakening `#27`, listing the current source names, approximate cost line, missing in-game charge/path/refund proof, and the boundary that it cannot enter runtime spending, path cost, reset refund or point economy. The same review now groups all 194 pending-cost nodes into 7 mutually exclusive evidence queues covering 37 source icon groups and expands those 37 branch/icon groups into per-group evidence rows, so branch-level review order and group-level missing proof are visible without generating Rune prices, path costs, reset refunds or point economy. The Rune evidence review additionally exposes the complete `tbh.city` single-source `total_cost_to_max` candidate table as `197/197` rows totaling `10,040,515,050G`, plus the existing 13-node auto-open subset totaling `383,790,000G` split into 4 review-only buckets (`10k`, `200k`, `1M`, and `382.58M` Lubrication aggregate); all of those candidate values remain outside verified costs, local unlock spending, path costs, point economy, and reset refunds.

The pending-source skill review now also cross-links the same unmapped-monster prefix candidates `200421`, `201211`, and `300441` as `3/3` review-only rows. Their direct detail pages now expose `value 1000` for Chaos/Physical/Cold BASEATTACK rows, so the value/range evidence queue is `15` rows instead of `12`; these rows deliberately remain inside the existing pending review evidence surfaces rather than forming an implementation queue, and their boundary text states that the prefix link plus value/range fields do not prove monster appearance, skill ownership, delivery, formulas, action frames, SFX, or runtime eligibility.

The pending-source skill review now also adds a source-range evidence queue over the same 60 pending rows: 13 range buckets cover all rows, with 15 value-backed rows and 60 empty-delivery rows still blocked behind target-distance, hit-shape, action-frame and SFX proof. This queue is only a review order over source range fields; it does not generate runtime range geometry, projectile speed, AOE radius, hit timing or effects.

The pending-source skill review now also adds an ID-prefix evidence queue over the same 60 pending rows: 3 source-namespace buckets cover all rows (`1:16`, `2:21`, `3:23`), with the same 15 value-backed rows and 60 empty-delivery rows still blocked behind localized name, ownership, delivery, formula, action-frame and SFX proof. This queue is only a review order over source ID namespaces; it does not infer class, monster, stage, skill ownership, formula, projectile shape, action frames or audio from the prefix.

Additional workflow gate check in this pass: CI, manual release, and nightly workflows now run the broad local fidelity gates before packaging and then rerun package-aware resource and deterministic battle-render audits after `bash scripts/package-app.sh dist`. `scripts/audit-local-workflow-fidelity-gates.sh` requires all three workflow files to keep 10/10 pre-package gates and 7/7 post-package gates in that order, so release artifacts cannot skip the packaged app icon, equipment icon, Rune icon, passive icon, hero sprite, SFX WAV/SFX manifest payload parity checks, or the packaged-app battle-scene render audit. This strengthens artifact regression coverage, but it is still a local packaging guard rather than proof of exact original gameplay, art or isolated SFX parity.

Additional refreshed Rune Tree check in this pass: `/zh/runes/` and `/en/runes/` both describe the Rune Tree as an account-wide passive system with 197 nodes, 195 connections, hero Level 3 unlock, and full reset support. The formation branch lists `Rune of Command (2nd hero — 50,000g)` and `Rune of Command (3rd hero — 150,000g)`, while the full rune table contains two max-level-1 command rows using `UnlockArrangeSlotCount.png`: Chinese `指挥符文` IDs `21` and `24`, English `Rune of Command` IDs `21` and `24`. The same formation branch lists `2nd Active Skill Slot (~50,000g)`, and the full rune table contains the matching max-level-1 `Rune of Awakening` / `觉醒符文` row with ID `27` and icon `UnlockSkillSlotCount.png`. The offline branch says `Offline — unlock and boost offline rewards (8h cap)` / `离线 — 解锁并提升离线奖励（上限 8 小时）` and lists `Rune of Repose (unlock offline reward)`, `Offline Gold +10%`, and `Offline XP +10%`; the full rune table contains the matching max-level-1 `Rune of Repose` / `安息符文` row with ID `11001`, next nodes `110011, 110012`, and icon `UnlockOfflineReward.png`, plus `Rune of Hoarding` / `储藏符文` ID `110011` using `OfflineRewardGoldPercent.png` and `Rune of Training` / `训练符文` ID `110012` using `OfflineRewardExpPercent.png`. The same full table includes multiple `Rune of Expansion` / `扩张符文` rows with `MaxInventorySlot` icon family, including source ID `22` in the formation branch; max-level-1 `Rune of the Mainspring` / `发条符文` rows using `UnlockAutoOpenNormalChest.png`, `UnlockAutoOpenStageBossChest.png`, and `UnlockAutoOpenActBossChest.png`; and chest-capacity rows using `MaxAmountNormalChest`, `MaxAmountStageBossChest`, and `MaxAmountActBossChest`. Current local `SourceRuneCatalog` preserves all 197 checked table rows, 195 checked `Next` edges, 11 sparse `Previous` references plus their exact sparse source-node map, the checked max-level distribution (`1:62`, `2:1`, `3:43`, `5:89`, `10:2`), the 39 referenced rune icon families and their checked per-family distribution as a source catalog. The resource bundle now also packages those 39 public page icon families as `source_rune_*`, the Settings panel exposes a compact source-data review table for all 197 rows, and all 197 current `RuneTreeNode` cases derive their setting-page artwork from the matching checked source icon family instead of the older screenshot-crop subset. A follow-up Grok Search and direct page fetch found partial independent guide/community evidence from GamesRadar, Games.gg, GameRant, Mobalytics, one Steam guide, and one Steam discussion: the third formation slot `150,000G` is now cross-source supported; the second active skill slot has Wiki `~50,000g` plus Games.gg/Steam-guide `50,000G` support; GamesRadar independently names Repose/Hoarding/Training and offline reward behavior; Games.gg and the Steam guide report auto-open timing examples (`300s` common, `600s` stage/blue boss, reduction examples `9s` and `15s`), while direct `tbh.city` rune pages support the local cooldown table for Normal Monster `300s`, Stage Boss `600s`, Act Boss `60s`, and checked Lubrication reductions (`9/10/10/10`, `15/20/20/20`, `3/3`). The same `tbh.city` mirror now exposes a single-source complete `total_cost_to_max` candidate table covering `197/197` rows, totaling `10,040,515,050G`, with checked samples such as `#1 100G`, `#21 1,000G`, `#24 150,000G`, `#27 50,000G`, and `#13002 10,000G`. Its 13-node auto-open subset totals `383,790,000G` (`10k`, `200k`, `1M`, plus Lubrication totals `187.03M`, `187.45M`, and `8.1M`), but these values are not cross-source verified and therefore stay out of runtime costs and refund math; Settings now groups only the auto-open subset into four candidate-cost review buckets, without binding candidate amounts to exact source IDs or generating per-node prices. Steam guide/discussion pages show late Rune cost examples such as `1-3M`, `5M` Gale, and `50M` stash. These are still guide/community sources rather than an official full cost table. Confidence is now High for the two formation costs, Medium for the second active skill slot cost and the auto-open cooldown examples, Low for the single-source `tbh.city` candidate cost table and auto-open subset, and still Low/insufficient for complete verified per-node costs, reset price, refund behavior, point economy, exact repeated-node stacking, chest-capacity increments/caps, full active skill loadout/equip semantics, and most of the 197 nodes' exact original effects. The table alone still does not prove the exact mapping of ID `21` versus ID `24` to the second or third hero slot, and neither the Wiki nor the independent guides expose exact gold/point costs for Rune of Repose, Rune of Hoarding, Rune of Training, Rune of Expansion, Rune of the Mainspring, or the chest-capacity runes.

Additional refreshed item page checks in this pass: `/zh/items/` and `/en/items/` both list 20 equipment types, 5,760 gear items, 396 gear level progressions, 174 material/chest rows, and the same 10-grade rarity distribution totals. Direct checks of all 20 English type pages under `/en/items/gear/{type}/` expose each type's aggregate gear count, per-rarity count distribution, level progression names, base item IDs and icons. Direct parsing of the live `v1.00.09` item page tables exposes 115 material rows across Decoration 36, Engraving 33, Inscription 10, Crafting 22, Offering 10 and Soul Stone 4, plus 59 stage chest rows across Common 19, Rare 29 and Legendary 11. The same HTML exposes 115 unique material image paths under `/assets/tbhdb/game/items/materials/Item_<ID>.png` and 3 stage-box family image paths under `/assets/tbhdb/game/items/boxes/Item_910011.png`, `Item_920011.png` and `Item_930011.png`. The local `SourceItemCatalog` now preserves those 20 type-page rows, the checked aggregate total of 5,760 gear entries, the checked 396 level progression IDs, the checked 115 material rows, the checked 59 stage chest rows, and aggregate rarity counts such as Common 320, Uncommon 760 and Cosmic 320. The exact item-record review now also exposes 40 source-backed category-by-rarity matrix queue rows covering the same 5,760 aggregate entries, including the checked zero-count `饰品/普通` cell, plus a 396-row base-progression queue that lists every checked source gear progression ID/name/level as source evidence only. It keeps `exact_item_records=0/5760` and explicitly does not generate item variants, affixes, drop weights or new icons from those aggregate counts or base progression IDs. The resource bundle now also carries the 396 checked base gear progression icons as `source_gear_*`, the 115 checked material icons as `source_material_*`, and the 3 checked chest-family icons as `source_stage_chest_*`; release resource self-test verifies their 16x16 and 64x64 source dimensions. This is still one fan-Wiki source family, so confidence remains Medium. These pages do not expose a complete per-rarity/per-affix 5,760-row item database, original stat rolls, drop tables, per-item variant IDs beyond the base progression IDs, exact material use/effect systems beyond their taxonomy, exact chest contents/rates, or the full original 5,760-item icon database, so the current catalog is an aggregate/source-progression/material/chest baseline rather than exact item-record parity.

Additional local equipment-icon check in this pass: the previous `item_0_0.png` through `item_3_4.png` runtime resources included local self-drawn type-level art. That violated the current fidelity rule that equipment icons must not be redrawn locally. The app now replaces all 20 equipment-type `item_*` fallback resources with the 16x16 public gear icons exposed by the corresponding `taskbarhero.org/en/items/gear/{type}/` pages, including the accessory page display assets `AMULET_600001`, `EARING_610001`, `RING_620001`, and `BRACER_630001`. It also packages 396 `source_gear_<ID>` resources downloaded from the checked base gear progression rows and records their official URL, SHA-256 and byte count in `source_gear_icons.tsv`; runtime concrete equipment now uses `equipmentType + itemLevel` to select the closest checked `source_gear_<ID>` icon, falling back to `item_*` only when a source progression is unavailable. `GameArt.itemIconName(for:)`, `SelfTest`, `InventoryTests`, `ResourceSelfTest`, and `scripts/audit-local-item-icons.sh` now require this source-backed path; the shell audit pins each runtime icon to its source URL and SHA-256 file payload so self-drawn semantic placeholders cannot silently return. Old saves that predate concrete `equipmentType` now infer a more specific type from legacy item names/descriptions before falling back to the slot default, so old bows/crossbows/scepters/shields/orbs/amulets/earrings/bracers can render their concrete source gear progression icon instead of collapsing to Sword/Shield/Ring. This is a source-page base-progression icon baseline for 396 checked progression IDs; it does not prove exact original full per-item icon art, full per-rarity/per-affix icon variants, or the original 5,760 item records.

Additional refreshed skill-page checks in this pass: `50401` `充能陷阱` / `Charge Trap` has matching Chinese and English Wiki pages listing cooldown activation, physical damage, trap delivery, range `1150`, Lv1/Lv10 `1000/5500`, and description text that traps explode when struck by elemental damage. `50501` `弩炮塔` / `Crossbow Turret` has matching Chinese and English Wiki pages listing cooldown activation, physical damage, projectile/summon delivery, range `1100`, Lv1/Lv10 `1750/3190`, and description text that an automated crossbow fires bolts dealing `1750%` physical damage. `50601` `电击弩箭` / `Shock Bolt` has matching Chinese and English Wiki pages listing attack-count activation, lightning damage, projectile delivery, range `1100`, Lv1/Lv10 `2700/4500`, and description text that the bolt lodges in an enemy and briefly releases lightning currents dealing `2700%` lightning damage to nearby enemies. This is still a single fan-Wiki source family, not an independent official confirmation. The pages do not expose trap duration, exact trap-count semantics, explosion damage formula, trigger source, placement, radius, resistance interaction, maximum active traps, turret duration, turret firing cadence, target selection, projectile travel, support-member turret state or summon replacement/stacking rules, Shock Bolt current duration, current tick cadence, nearby-target geometry, lodged-target retention, lightning resistance/status behavior or exact attack-count cadence.

Additional refreshed Slayer check in this pass: `60101` `猛击跳跃` / `Slam Jump` is present at `/zh/skills/active/60101-slam-jump/`, `/zh-tw/skills/active/60101-slam-jump/`, `/ja/skills/active/60101-slam-jump/`, and `/en/skills/active/60101-slam-jump/`. The structured page fields list cooldown activation, physical damage, melee/AOE delivery, range `850`, Lv1/Lv10 `3100/5350`, and order `400`. The Chinese, Traditional Chinese and Japanese descriptions say the Slayer leaps to an enemy and creates a landing shockwave dealing `3100%` physical damage to surrounding enemies. The English description conflicts by describing a focused `3100%` hit plus a `350%` kill shockwave, which matches the already-checked `60201` `Crushing Blow` rider more closely. Confidence remains Medium because all pages are one fan-Wiki source family; the implementation follows the repeated structured fields and non-English descriptions while documenting the English text conflict. Exact leap travel timing, landing delay, AOE radius/geometry, target selection and physical resistance/falloff remain unverified.

Additional refreshed Knight check in this pass: `10201` `盾牌冲锋` / `Shield Charge` has matching Chinese, English and Japanese fan-Wiki pages listing cooldown activation, physical damage, melee delivery, range `900`, Lv1/Lv10 `3000/5700`, and description text that the Knight raises a shield, charges forward, and deals `3000%` physical damage to enemies collided with. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact charge path, travel timing, collision width/count, target ordering, knockback/stun behavior or physical resistance/falloff.

Additional refreshed Knight check in this pass: `10301` `报应打击` / `Retribution Strike` has matching Chinese, English and Japanese fan-Wiki pages listing attack-count activation, physical damage, melee delivery, range `150`, Lv1/Lv10 `1500/3300`, order `600`, and description text that the Knight strikes enemies multiple times, each strike dealing `1500%` physical damage, with more strikes as the hero's HP gets lower. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact attack-count cadence, HP threshold table, minimum/maximum strike count, retargeting behavior after overkill, physical resistance/falloff or support-member behavior.

Additional refreshed Knight check in this pass: `10101` `穿透突刺` / `Piercing Thrust` has matching Chinese, English and Japanese fan-Wiki pages listing attack-count activation, physical damage, melee/AOE delivery, range `200`, Lv1/Lv10 `2500/4300`, order `400`, and description text that the Knight thrusts the sword deeply forward to deal `2500%` physical damage to enemies in range. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact attack-count cadence, thrust path, collision width, maximum target count, target ordering, travel timing, physical resistance/falloff or support-member behavior.

Additional refreshed Knight check in this pass: Grok Search returned no sources for `Taskbar Hero 10401 Aegis Field blocks damage allies field`, so direct page checks used `/zh/skills/active/10401-aegis-field/`, `/en/skills/active/10401-aegis-field/`, and `/ja/skills/active/10401-aegis-field/`. The Chinese, English and Japanese pages match on cooldown activation, physical damage category, range/AOE delivery, range `150`, Lv1/Lv10 `500/1850`, order `600`, and description text that the field/aura blocks `500` damage for allies within the area. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact duration, field geometry, whether the `500` value is a shared pool or per-ally shield, whether downed allies are included, support-member field-cast state, damage-type interaction, stacking/replacement rules, or visual timing.

Additional refreshed Priest check in this pass: Grok Search returned no sources for `Taskbar Hero 40601 Revive skill details`, so the direct page checks used the Wiki sitemap and the `/zh/skills/active/40601-resurrection/`, `/en/skills/active/40601-resurrection/`, and `/ja/skills/active/40601-resurrection/` pages. The Chinese, English and Japanese pages match on cooldown activation, physical damage category, no delivery type, range `1100`, Lv1/Lv10 `300/750`, order `900`, and description text that it revives one fallen party member with `300%` of that member's max HP. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose target priority, whether the Priest can revive self, whether cooldown is held until a valid fallen target exists, independent party member HP/level/equipment formulas, over-max-HP cap behavior after revival, animation timing, or whether revived support cooldowns continue ticking while down.

Additional refreshed Priest healing check in this pass: Grok Search returned no sources for `Taskbar Hero 40101 Heal skill details` or `Taskbar Hero 40401 Sanctuary skill details`, so direct page checks used `/zh/skills/active/40101-heal/`, `/en/skills/active/40101-heal/`, `/zh/skills/active/40401-sanctuary/`, and `/en/skills/active/40401-sanctuary/`. The Chinese and English `40101` pages match on cooldown activation, physical damage category, no delivery type, range `950`, Lv1/Lv10 `100/280`, order `400`, and description text that it restores one ally by `100%` of that ally's maximum HP. The Chinese and English `40401` pages match on cooldown activation, physical damage category, range/AOE delivery, range `1100`, Lv1/Lv10 `300/1920`, order `700`, and description text that all allies inside the sanctuary regenerate `300` HP per second. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose `治愈` target priority, whether full-health allies are skipped before cooldown consumption, `圣域` duration, exact field geometry, support-member independent HP/equipment/skill-level formulas, or support-member field-cast state.

Additional refreshed guide interaction check in this pass: Grok Search returned no indexed snippets for the guide query, so direct page-source checks used `/zh/guide/` and `/en/guide/`. The Chinese guide says to use `Alt+点击` to lock any item the player wants to keep before Alchemy, and the English guide says `Alt+Click to lock anything you want to keep first`. This still comes from a single fan-Wiki source family, so confidence remains Medium. The macOS app maps that original `Alt+Click` affordance to an `Option`-click item-grid action because Option is the macOS Alt key; exact original lock icon styling, modifier-key handling on Windows, and broader item safety UX remain unverified.

Additional refreshed skill-catalog check in this pass: direct page-source parsing of `/en/skills/` and `/zh/skills/` found 106 active/base/monster skill rows in both languages, 106 unique IDs in each page, matching ID order, and matching technical fields (`data-id`, `data-activation`, `data-damage`, `data-delivery`, `data-range`). The extracted source distribution is `BASEATTACK:58`, `BASEATTACK_COUNT:11`, `CONTINUOUS:2`, `COOLDOWN:35`; damage types are `Physical:77`, `Fire:12`, `Chaos:8`, `Cold:5`, and `Lightning:4`; delivery values include empty source delivery, `AOE`, `Melee`, `Melee, AOE`, `Projectile`, `Projectile, AOE`, `Projectile, Summon`, and `Trap`. The damage distribution is now separately guarded as source metadata: `Physical 77`, `Fire 12`, `Chaos 8`, `Cold 5`, and `Lightning 4`, with `15` non-physical rows and `1` Chaos row currently runtime-mapped. The activation/damage source cross-tab now also exposes the unmodeled `COOLDOWN/Chaos` pending IDs `309021`, `309041`, and `309051` as source-review targets without inventing combat behavior for them. Individual skill pages for those three rows expose `Value` fields `800`, `1700`, and `2300` with the same empty delivery and no localized descriptions; `SourceSkillCatalog` now preserves those checked values as `sourceValue` and Settings/audit display `309021:800/r700;309041:1700/r700;309051:2300/r600` as source-review data only. The pending-source review now also separates the 15 pending rows that already have checked `sourceValue` data from runtime-ready work: all 15 are still unnamed `Skill xxxxxx` rows, all 15 still have empty source `delivery`, including same-prefix candidates `200421`, `201211`, and `300441` whose direct pages only add `value 1000`; the highest checked values remain `209041=2300/r270` and `309051=2300/r600`, so they remain prioritized review targets rather than executable skills. The pending-source review now also exposes and guards the complete pending elemental damage manifest: `Fire 100231,200911,300311,300611,300821,300911`; `Cold 300441`; and `Chaos 200411,200421,300711,309011,309021,309041,309051`. This is data-only targeting evidence, not a runtime or visual-effects implementation. The activation/delivery source cross-tab is now guarded as trigger/visual-form review metadata: `16` source combinations, `15` currently runtime-mapped combinations, empty delivery runtime `11/71`, largest pending pair `BASEATTACK/空 delivery 48`, and `BASEATTACK_COUNT/空 delivery 0/2`, while explicitly not treating this as proof of exact original trigger cadence, cast frames, visual form, projectile/range geometry or animation timing. The damage/delivery source cross-tab is now also guarded as art/effect review metadata: `20` source combinations, empty delivery `11/71`, largest pending pair `Physical/空 delivery 46`, and `Chaos/空 delivery 1/8`, while explicitly not treating empty delivery as proof of no projectile, no range, or no animation. The extracted full source `range` distribution spans `120` through `1650` across 24 buckets, with the largest bucket `150 x16`, followed by `800 x12` and `1100 x11`. This is now preserved in `SourceSkillCatalog` as a data-only source catalog. The six source-backed hero base attack rows now feed runtime battle-log element/delivery metadata for the main hero and support members, so Sorcerer base attacks carry fire/projectile visual cues and Ranger/Hunter base attacks carry projectile cues. The runtime metadata enum now also preserves checked `Chaos` damage rows such as source skill `309021` without collapsing them to `none`, and generic chaos damage logs have a distinct local impact cue for future source-backed monster/skill work. The deterministic battle-scene snapshot/audit now also renders a `chaosBurst` fixture and gates the cue with `damage_chaos_pixels=5444`, so the local purple/green chaos feedback cannot disappear silently. This does not imply that all 106 skills execute in combat or that exact original chaos visuals have been captured: only the 36 named hero active skills currently have runtime behavior and 10-level value tables in `HeroSkills`, while exact base-attack formulas/cadence/projectile travel, monster skills, status effects, elemental resistance/status rules, animation timing, range geometry, chaos sprite/keyframes and audio behavior are verified gaps.

The pending-source review now also guards the complete 60-row pending damage manifest by damage type. The Physical subset is `100111,100211,100221,100311,100411,100421,100431,100511,100521,100531,109011,109021,109031,109041,109051,200111,200211,200221,200231,200241,200311,200511,200611,200621,200711,200811,201111,201211,209011,209021,209031,209041,209051,300111,300121,300131,300211,300411,300421,300431,300511,300811,300831,300841,301111,309031`; the Fire/Cold/Chaos subsets remain the elemental manifest above. The overlapping visual-priority queue now also reports `22` review entries as `16/60` unique pending skills, with `6` overlapping entries and `44` pending rows outside the high-priority queue. Those `44` rows are now explicitly counted as the low-priority visual backlog, and the overall visual-review route is guarded as `60/60` pending skills covered by either high-priority or backlog queues. This is still a target list for future source-backed implementation, not proof of original base-attack formulas, monster ownership, cadence, hit geometry, projectile sprites, animation timing, or audio parity.

Additional highest-value pending skill detail check in this pass: direct fetches of `/zh/skills/active/id-209041/`, `/en/skills/active/id-209041/`, `/zh/skills/active/id-309051/`, and `/en/skills/active/id-309051/` confirm that both highest checked value candidates still expose only `Skill 209041` / `Skill 309051`, trigger `Cooldown`, damage `Physical` / `Chaos`, delivery `—`, range `270` / `600`, value `2300`, order `999`, and no localized description in either checked language. The Settings pending-source review now surfaces this as `2 页 / Skill ID / 无说明 / 空 delivery` with exact detail paths. This strengthens the evidence that these rows should stay in review instead of runtime until localized names, ownership, target rules, delivery shape and descriptions appear.

Additional pending source-page snapshot check in this pass: direct fetches of `/zh/skills/active/id-309021/`, `/en/skills/active/id-309021/`, `/zh/skills/active/id-309041/`, `/en/skills/active/id-309041/`, `/zh/skills/active/id-309051/`, and `/en/skills/active/id-309051/` confirm the same `v1.00.13` snapshot state for the three pending `COOLDOWN/Chaos` value rows: still `Skill ID`, no localized description, delivery `—`, Lv `—`, and only range/value/order fields are exposed. Settings now exposes `30` zh/en value-detail page checks for the 15 value-backed pending rows, `4` zh/en checks for the two highest-value rows, and a dedicated `3` row / `6` zh-en page `COOLDOWN/Chaos` evidence review for `309021`, `309041`, and `309051`. This is deliberately recorded as one source-family snapshot, not as a second independent source or runtime permission; those rows remain blocked until name/description, delivery/hit shape, ownership/target rules, formula/duration, action-frame/VFX, and isolated SFX evidence exists.

Additional refreshed passive skill icon check in this pass: direct page-source parsing of `/en/skills/` and `/zh/skills/` found 108 passive rows in both languages. The current source pages expose `<img class="db-icon">` assets for 104 of those rows, collapsing to 27 unique `Passive_*.png` icon families with a mixed source-size distribution: 16 families are `16x16` and 11 families are `32x32`. The four source rows with no exposed icon are the two `IncreaseProjectileDamage` rows and the two `SkillHealIncrease` rows; guessed asset URLs for `Passive_IncreaseProjectileDamage.png` and `Passive_SkillHealIncrease.png` were not present in the current source snapshot and returned `404` in the live source check. The macOS app now bundles only the exposed source-backed passive icons as `source_passive_*`, maps the 104 source-backed passive rows to those assets, keeps the current source dimensions, and deliberately leaves the two missing stat families unmapped instead of using a fabricated substitute. The current source snapshot also exposes one exact pixel-reuse group across `CastSpeed`, `DamageAbsorption`, `MaxDodgeChance`, `MaxHp`, and `MovementSpeed`, so local audits allow that explicit source reuse while still rejecting unexpected duplicate payloads. Confidence remains Medium because this comes from the same unofficial fan-Wiki source family; it is a current source-page parity check, not proof of original in-game passive icon completeness.

## Local Gameplay Coverage Audit

Run:

```bash
scripts/audit-local-gameplay-fidelity.sh
```

Local item icon art audit:

```bash
scripts/audit-local-item-icons.sh
```

Local passive skill icon audit:

```bash
scripts/audit-local-passive-skill-icons.sh
```

Local deterministic battle-scene visual audit:

```bash
RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh
```

The local gameplay audit now also reports `derived_skill_damage_metadata_runtime_guard`, `derived_skill_damage_metadata_self_test_guard`, and `derived_skill_damage_metadata_swift_test_guard`. These guards require derived skill damage logs such as `粉碎强击冲击波`, `充能陷阱爆炸`, `弩炮塔`, and `电击弩箭电流` to preserve explicit source element/delivery metadata through active buff state instead of relying only on Chinese skill-name inference. The separate `charge_trap_actual_damage_log_*` guards require `充能陷阱` to detonate from actual new hero-side elemental damage logs, including elemental damage-over-time, while physical damage logs leave the armed trap unspent. These are local metadata, trigger-filtering and cue-regression guards, not proof of exact original projectile timing, shockwave radius, trap trigger source matrix, summon cadence, or current-arc geometry.

The local gameplay audit now also includes `battle_damage_log_metadata_static_guard`, a source-level scan over every `BattleLogEntry(... kind: .damage ...)` constructor in `Battle.swift`. Any direct damage log constructor without explicit `damageElement:` and `delivery:` metadata fails the audit, so new combat paths cannot silently fall back to name inference and lose battle-log labels, trajectory cues, or impact cues. This guard proves only the local metadata plumbing invariant; it is not proof of exact original hit timing, projectile travel, cast frames, or effect sprites.

The deterministic battle-scene fixtures now use explicit `damageElement` and `delivery` metadata for their damage log inputs, and the gameplay audit reports `battle_scene_snapshot_damage_metadata_static_guard` over `BattleSceneSnapshot.swift`. This keeps visual screenshot fixtures aligned with the runtime metadata invariant: the local render tests no longer depend on Chinese skill-name inference to select trajectory or impact cues. Monster incoming fixtures still use explicit `.none` delivery where the checked source skill row has an empty delivery field, so the guard does not invent projectile/range semantics from the separate source `range` number.

`BattleSceneSnapshot.Fixture` is now `CaseIterable`, the snapshot CLI rejects missing or unknown `--render-battle-scene-fixture` values instead of silently falling back to `frostBolt`, and `scripts/audit-local-gameplay-fidelity.sh` reports `battle_scene_snapshot_fixture_audit_guard` plus `battle_scene_snapshot_fixture_cli_guard`. The guard statically compares every current fixture case to the fixture names rendered by `scripts/audit-local-battle-scene.sh`, while `TBH --self-test` checks that all fixture cases are categorized as default scene, damage cue, utility cue or status-row fixtures. This prevents typoed or newly added visual fixtures from escaping the deterministic render audit, but it is still a local coverage guard rather than proof of exact original keyframes or projectile timing.

`--render-battle-scene-hero-class` now follows the same explicit-argument policy: omitted values still default to the Knight fixture path, but missing or unknown explicit values fail the snapshot CLI instead of rendering a misleading Knight image. `TBH --self-test` verifies every `HeroClass` case name and Chinese display name resolves through the CLI parser, and `scripts/audit-local-gameplay-fidelity.sh` reports `battle_scene_snapshot_hero_class_cli_guard`. This protects hero-sprite review and screenshot automation from class-selector typos; it does not prove exact original hero animation keyframes, facing timing or class-specific combat poses.

`--render-battle-scene-time` is now also strict when explicitly supplied: missing, non-numeric, non-finite or negative values fail the snapshot CLI rather than falling back to live `TimelineView` time. The deterministic render audit still uses fixed `0` and `0.267` second samples for base and motion frames, matching the rounded official frame `0` -> frame `8` motion interval, while status-row fixtures remain time-independent. `TBH --self-test` covers valid positional/inline times plus missing, non-numeric and negative failures, and `scripts/audit-local-gameplay-fidelity.sh` reports `battle_scene_snapshot_time_cli_guard` plus the fixed-time argument count. This protects flame/combatant-motion regression checks from accidental nondeterministic frame selection; it is not evidence of exact original animation timing or keyframes.

The script is a local source-coverage guard, not an external fact source. It reads the current Swift model files plus `Battle.swift`, `BattleView.swift`, `MenuBarPopover.swift`, `BattleSceneSnapshot.swift`, self-test source and the local battle-scene audit script, then compares them to the already-recorded Steam/Wiki baselines so regressions in currently modeled gameplay scaffolds and their visible battle feedback are caught while known incomplete areas remain explicit gaps. Current 2026-06-19 output includes: 6/6 hero classes, 106/106 active/base/monster source skill catalog rows, 106/106 Settings skill source-table rows, 8 Settings source delivery buckets, 71 source rows with empty delivery, 35/35 non-empty delivery rows already runtime-mapped, source most-common delivery `EMPTY x71`, 36/36 runtime-modeled named hero active skills, 42/106 runtime hero source rows after adding hero base attacks, 4/4 checked monster attack rows, 4/4 Settings monster attack review rows, 46/106 total runtime-modeled source skill rows, `COOLDOWN/Chaos` pending source IDs `309021,309041,309051`, `pending_source_skill_cooldown_chaos_page_rows=3`, `pending_source_skill_cooldown_chaos_locale_pages=6`, `pending_source_skill_cooldown_chaos_empty_delivery=3`, `pending_source_skill_cooldown_chaos_unnamed=3`, 60 Settings pending-source skill review rows, 60/60 pending rows with empty delivery, 3 pending activation buckets, 4 pending damage buckets, 3 pending source-ID prefix buckets, 4 pending responsibility buckets, 14-row pending elemental damage ID manifest `Fire:100231,200911,300311,300821,300911;Cold:300441;Chaos:200411,200421,300711,309011,309021,309041,309051`, 12-row pending triggered/cooldown ID manifest `109021,109031,109041,109051,209021,209031,209041,209051,309021,309031,309041,309051`, 15 value-checked pending candidates that are still 15/15 unnamed and 15/15 empty-delivery, 7 pending-source runtime evidence gates, 3 mutually exclusive pending-source evidence queues covering all 60 pending rows (`15` value/range detail-page rows, `9` non-Physical base-attack catalog rows, and `36` Physical base-attack catalog rows), 45 per-skill base-attack evidence rows (`9` non-Physical and `36` Physical), highest value-checked pending candidates `209041=2300/r270; 309051=2300/r600`, 13 pending source-range buckets, pending source most-common range `150 x10`, 36/36 runtime active-skill value tables, 108/108 passive skill catalog rows, 30/30 source passive stat kinds with explicit runtime hooks, 197/197 checked Rune Tree source rows, 195/195 checked Rune Tree source `Next` edges, 197 runtime-unlockable Rune Tree nodes, 197/197 runtime-modeled source Rune Tree nodes, 39/39 runtime Rune icon families, 38 modeled local Rune Tree prerequisite edges, 195 runtime Rune nodes still explicitly marked cost-unverified, 3/3 Acts, 30/30 display stages, 120/120 difficulty-stage runtime rows, 120/120 Settings stage source-table rows, 120/120 stage-composition rows, 49 unique stage-composition monster names, 5 unmapped-monster evidence gates, 4/4 difficulty tiers, 10/10 rarity grades, 20/20 equipment types, 20/20 checked gear type pages, 5,760/5,760 checked per-type aggregate gear entries, 396/396 checked base item level progression IDs, `exact_item_record_gap_progression_queues=396`, `exact_item_record_gap_progression_queue_coverage=396`, 5,760/5,760 checked per-type rarity-count totals, 115/115 checked material rows, 6/6 checked material categories, 59/59 checked source stage chest rows, 0/5,760 exact per-rarity/per-affix item records, 59/59 current chest catalog rows, 3/3 chest families, 4/4 Soul Stone kinds, the checked 9-item Synthesis input count, 10/10 Settings Synthesis/Cube/Alchemy rule review rows, 16 compact player-side battle status badge cases, 14 active buff/summon/trap status mappings, 2/2 source `CONTINUOUS` Priest blessing status mappings, 3 deployable field markers, menu-bar popover size `960x2554`, content min-height `2448`, bottom-tab guard `enabled`, deterministic battle render size `1872x4608`, configured local battle ratio `0.41:1`, local platform width `90%`, battle log visible entries `50`, player-side highlight entries `3`, battle log panel height `124`, `source_rune_candidate_cost_queues=4`, `source_rune_candidate_cost_queue_coverage=13`, `source_rune_candidate_cost_queue_gold_total=383790000`, `source_rune_tbh_city_candidate_cost_table_rows=197`, and `source_rune_tbh_city_candidate_cost_table_gold_total=10040515050`. It also prints source skill activation/damage/delivery distributions, pending source skill activation/damage/ID-prefix/range/responsibility/value-readiness distributions and empty-delivery count, the pending source-skill evidence queue manifests, runtime source ID coverage, the current `GamePacing` values (`1.5s` outer tick, `67%` combat delta multiplier, `0.25s` combat simulation step, `35%` XP multiplier, `+2` stage-level buffer, `1.0s` minimum attack interval), full source Rune Tree icon-family coverage, whether runtime loot and Synthesis preview use checked source gear progression names/IDs, whether support-member sustained summon/range skill state remains wired for `烈焰九头蛇` / `暴风雪` / `弩炮塔`, whether source-backed Normal Monster / Stage Boss / Act Boss chest-capacity runes apply the local conservative `+1` per-family storage cap scaffold, whether source base-attack metadata feeds hero/support battle logs, whether main-hero damaging skill logs explicitly carry source element/delivery metadata instead of relying only on name inference, whether checked source `Chaos` damage rows map to runtime metadata/cues, whether deterministic battle-scene snapshot and shell visual audit gate the chaos impact cue, whether Ranger `穿透之箭` and `穿刺射击` deterministic visual fixtures are gated, whether source monster attack metadata feeds incoming player-lane cues, whether Settings exposes the checked monster attack review boundary, whether local visual audit gates fire/cold/lightning/chaos monster incoming cues, source gear/material/chest rarity totals, source Soul Stone IDs, current local Rune Tree prerequisite edges, player status badge cases, active status skill names, continuous status skill names (`力量祝福`, `守护祝福`), and deployable marker skill names. This gives future gameplay work a repeatable baseline for deciding whether an edit expands fidelity or silently erodes an already-modeled part, and prevents source catalogs or queue order from being mistaken for complete original runtime systems.

The same pending-source audit now also prints and guards the complete 48-row pending `BASEATTACK` source-ID manifest by prefix: `1:100111,100211,100221,100231,100311,100411,100421,100431,100511,100521,100531,109011`; `2:200111,200211,200221,200231,200241,200311,200411,200421,200511,200611,200621,200711,200811,200911,201111,201211,209011`; `3:300111,300121,300131,300211,300311,300411,300421,300431,300441,300511,300611,300711,300811,300821,300831,300841,300911,301111,309011`. This keeps broad base-attack source rows traceable without implementing unverified monster ownership, formulas, cadence, hit shapes, projectile sprites, or cast frames.

The exact-item record gap review now also exposes a 10-row rarity evidence queue over the checked aggregate gear counts: Common `320`, Uncommon/Rare/Legendary/Immortal `760` each, Arcana `640`, Beyond `560`, Celestial `480`, Divine `400`, and Cosmic `320`, for `5,760/5,760` aggregate rarity coverage. These rows are only a review index over source type-page rarity counts; they do not create exact per-variant records, affix rolls, drop weights, icon variants, or additional item art, so the exact item-record boundary remains `0/5,760`.

The same Rune-cost audit now also prints and guards `local_rune_cost_review_branch_evidence_rows 37`, `local_rune_cost_review_branch_evidence_coverage 194`, and `local_rune_cost_review_branch_evidence_group_coverage 37`. These rows are one-per-pending branch/icon group and only expose the current source ID samples, source names, missing proof and no-price-generation boundary; they still leave `rune_pending_cost_nodes=194` unresolved. It also guards `local_rune_cost_review_approximate_evidence_rows=1` and `local_rune_cost_review_approximate_evidence_coverage=1`, so the one medium-confidence approximate cost is visible as a review row without becoming verified cost. It also adds a max-level evidence queue over those same 194 pending nodes: 5 source `maxLevel` buckets cover all pending costs with distribution `1:59,2:1,3:43,5:89,10:2` and 62 bucket-local icon groups. The max-level queue only proves the source table's level caps and review order; it does not infer per-level prices, cost gradients, path costs, reset refunds, point economy or runtime spend values.

The same gameplay audit now also requires battle victory reward previews and the visible victory banner to use XP after `GamePacing` and level-cap checks, requires offline XP statistics to accumulate only the XP actually granted by `HeroLevelPacing`, and requires the Settings stage source rows to preview applied XP beside the raw mined XP value. This prevents future balance edits from showing or recording raw mined stage XP while the runtime hero receives the slower local pacing amount.

The same gameplay audit now also requires source-backed monster attacks to preserve the stage monster name in `BattleLogEntry.attackerName` and `attackerDisplayName`, requires battle-log damage elements to expose compact Chinese labels for physical, fire, cold, lightning and chaos metadata, and requires the visible victory reward banner to render dropped loot through `GameArt.itemIconName(for:)` plus `PixelSprite`. This keeps the combat log tied to the mined stage composition names and makes unnamed source-backed base/monster attacks visibly comparable without expanding unverified monster skill mappings beyond the four checked elemental priest rows. It also prevents the reward banner from regressing to text-only loot feedback while still relying only on the already-checked source-backed item icon path.

The same gameplay audit now also requires Settings to expose a separate `精确装备记录缺口` review. It reports the current source-page aggregate equipment coverage by category: weapons `6` types / `1,752` aggregate entries / `120` base progressions, offhands `6` / `1,752` / `120`, armor `4` / `1,168` / `80`, and accessories `4` / `1,088` / `76`. Each category remains `0/N` for exact per-rarity/per-affix records, with all `5,760` exact rows still missing. The panel now also exposes all 20 source gear type rows as exact-record gaps, including their type-page rarity distributions, for example Sword `292` aggregate rows / `20` base progressions / `C:20 U:38 R:38 L:38 I:38 A:32 B:28 Ce:24 D:20 Co:16`, and Amulet `272` / `19` / `C:0 U:38 R:38 L:38 I:38 A:32 B:28 Ce:24 D:20 Co:16`. It now also exposes 4 exact-record category evidence queues, 20 exact-record type evidence queues, and a largest-type priority queue of `16` weapon/offhand/armor types covering `4,672` aggregate missing records where each type has `292` missing exact rows; those queues keep the next required proof visible per category/type, such as variant IDs, rarity rows, affix roll ranges, drop sources, weights, and icon sharing rules. It also exposes 5 explicit evidence gates that must be satisfied before exact item records can be modeled: per-variant IDs, affix/stat rolls, drop pools and weights, icon-variant mapping evidence, and cross-source provenance. Those rows are a review checklist only: they do not generate equipment records, affixes, drop weights, or new icons. This prevents the 396 source-backed base progression icons, the 20 type-page rarity distributions, the evidence queue order, largest-gap ranking, and the 5,760 aggregate count from being mistaken for a complete per-variant item database.

The item icon art audit is a local visual-resource guard, not an external fact source. It parses `EquipmentType`, `GameArt.itemIconName(for:)`, `SourceItemCatalog` gear progressions, and `source_gear_icons.tsv`, then checks that the 20 source equipment types retain one-to-one `item_*` fallback icons and that the 396 checked base gear progression IDs have packaged `source_gear_*` PNG files. It verifies pinned source URL coverage, 16x16 RGBA source dimensions, exact SHA-256 file payloads, byte counts, nonblank and non-solid visible-pixel coverage, visible-pixel connectivity, no duplicate payloads, no missing mapped resources, and no unmapped source-gear extras. It also checks the generic `official_item_*` fallback icons and the 3 `source_stage_chest_*` box-family icons for expected dimensions, transparent corners, visible-pixel coverage and zero visible outer-edge pixels. When the local save file exists, the audit reads the real backpack and equipped items from `~/Library/Application Support/TBH/save.json` and verifies that saved equipment resolves to existing source-backed `source_gear_*` or `item_*` resources. This catches the specific regression where runtime equipment icons become local redrawn placeholders, cropped inventory UI tiles, or adjacent-fragment crops, while keeping the documented limitation that these are 396 base-progression icons rather than original full 5,760 per-variant icon parity.

The deterministic local UI audit now also renders a real `InventoryView` fixture with checked source gear progression items, a selected comparable weapon, ready Synthesis inputs, a locked item, Cube progress, direct backpack expansion context and the current worse-equipment handling selector. The latest `RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh` run measured `inventory_panel_non_dark_pixels=126724`, `inventory_panel_control_light_pixels=7225`, `inventory_panel_grid_colored_pixels=3452`, `inventory_panel_detail_non_dark_pixels=71581`, `inventory_panel_delta_green_pixels=1126`, and `inventory_panel_rarity_pixels=3677`. The gate now fails if backpack controls, source-backed icon color, selected-item detail, live comparison deltas or rarity feedback disappear from the local inventory panel. This uses the existing `GameArt.itemIconName(for:)` source-backed icon resolver and does not add, redraw or infer any new equipment artwork; it is a local readability/comparison regression guard, not proof of exact original inventory layout, item variants, affix rolls, drop weights or full per-variant icon parity.

The deterministic local UI audit now also renders a real `CharacterView` fixture with a Hunter hero, checked source-backed equipment icons, direct support-slot unlock affordances for positions 2 and 3, two active-skill loadout slots, passive skill chips and equipped-gear rarity color. The latest `RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh` run measured `character_panel_non_dark_pixels=175205`, `character_panel_hero_colored_pixels=4393`, `character_panel_party_orange_pixels=5756`, `character_panel_skill_colored_pixels=17975`, `character_panel_passive_icon_pixels=13511`, and `character_panel_equipment_rarity_pixels=3155`. The gate now fails if hero identity art, direct party unlock controls, active/passive skill presentation or equipped source icon/rarity feedback disappear from the local character panel. This uses existing hero, skill, passive and item-art paths; it is a local readability/source-wiring regression guard, not proof of exact original character panel layout, Rune Tree economy, active-skill equip rules, per-skill icon parity or original character animation frames.

The deterministic local UI audit now also renders the real `ChestControlsView` from Settings with four saved chest fixtures, batch opening unlocked, all three automatic chest-opening families unlocked, and source-backed Normal Monster / Stage Boss / Act Boss box icons. The latest `RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh` run measured `chest_panel_non_dark_pixels=148188`, `chest_panel_button_blue_pixels=6820`, `chest_panel_auto_green_pixels=2543`, `chest_panel_icon_colored_pixels=10636`, and `chest_panel_rarity_pixels=7874`. The gate now fails if the all-chests button, auto-open status strip, individual open buttons, source chest icons or rarity/source-family color disappear from the Settings chest controls. This verifies local chest-control readability and source-backed icon wiring; it does not prove exact original chest UI layout, chest drop rates, auto-open Rune economy, cooldown timing, box contents or opening animation parity.

The passive skill icon audit is a local visual-resource guard, not an external fact source. It parses `PassiveSkills`, `GameArt.passiveSkillIconName(for:)`, and bundled `source_passive_*` PNGs, then checks 108 passive rows, 30 passive stat names, 104 source-icon-mapped rows, 27 current source icon families, and the expected missing source-icon stat families `IncreaseProjectileDamage` and `SkillHealIncrease`. It also verifies source dimensions (`16x16` or `32x32` by icon family), nonblank/non-solid alpha coverage, no unexpected duplicate pixel payloads beyond the explicit current source reuse group, and no missing or extra `source_passive_*` resources. When `dist/TBH.app` exists, the audit repeats the same checks against the packaged app bundle and compares all packaged `source_passive_*` payloads with source resources. Set `AUDIT_REMOTE_PASSIVE_SKILL_SOURCE=1` to extend the same script with a live `/en/skills/` and `/zh/skills/` source-page check for row counts, `db-icon` coverage, missing target rows, and the two missing candidate PNG `404` statuses; this is intentionally opt-in so ordinary local audits remain deterministic and offline.

The battle-scene visual audit is a local rendered-pixel guard, not an external fact source. It uses the deterministic `TBH --render-battle-scene` path to render the real SwiftUI battle strip and checks the enlarged local horizontal-strip geometry, animated flame phase change, primary/support party separation, top-left stage pill, overhead HP bars, deployable markers, damage/utility/status cue pixels and the current chaos cue gate. This audit now runs in the push/tag/PR `CI/CD` workflow, the manual `Release Build` workflow, and the scheduled/manual `Nightly Build` workflow so battle-scene regressions are not limited to nightly-only detection.

## Original Gameplay Baseline

| Area | Original Evidence | Current macOS State | Gap |
| --- | --- | --- | --- |
| Core loop | Deploy party, auto fight waves, collect gold/XP/equipment/chests, use Alchemy/Synthesis/Cube, invest Rune Tree/skill points, push stages and Act Bosses. | A primary hero now leads a three-slot party scaffold; the settings Rune Tree path still spends checked Rune of Command formation-slot costs (`50,000G` and `150,000G`) after the Lv.3 Rune Tree gate, while the character panel now also offers direct support-slot unlock buttons that spend the same checked formation gold without requiring navigation through the Rune Tree UI. It earns aggregated raw XP/gold and retained loot from defeated encounters, then applies local `GamePacing` XP slowdown and level-cap checks before mutating hero XP or showing the battle victory XP preview. It advances through the new Act/stage/difficulty scaffold by the number of cleared encounters, grants source-mapped Normal + Stage Boss chests from non-Boss stage clears and Normal + Act Boss chests from X-10 Boss clears, records verified per-stage monster composition metadata, and gates X-10 Boss stages behind Soul Stone counts from opened chests. The active battle now maintains per-enemy HP/defeat state for the current wave, lets every alive enemy attack on its own cooldown, keeps the first alive enemy as the focused target for hero/support attacks, and exposes the live enemy group to the UI. A DEBUG `GameEngine.runSelfTestTick()` hook lets `TBH --self-test` drive the same private runtime tick used by the timer; each runtime tick records real wall-clock play time but advances battle simulation through `GamePacing.simulatedCombatDelta(for:)`, currently about `1.0s` simulated combat per `1.5s` outer tick. The `[GameEngine runtime]` suite now proves a live tick can produce combat events, win the battle, settle paced XP/gold, advance encounter progress, accumulate play time, record statistics, play victory SFX, keep loot SFX consistent with retained loot, and start the next battle. Soul Stone metadata now uses the item database's four material IDs and rarities, and opened chests no longer promote Soul Stone type by item level. The local chest catalog now covers all 59 Wiki item-page stage-box rows; the stage reward path still filters to the 51 box IDs that the current drops stage table actually maps to stages, so duplicate early `Stage Boss Box` catalog IDs do not overwrite checked stage rewards such as `1109 -> Stage Boss Box 6 #920022`. The settings UI now lists concrete saved chest instances by source family/database ID/stage source and can open a specific `Normal Monster`, `Stage Boss`, or `Act Boss` chest instead of only opening the first chest in a difficulty bucket; batch chest opening is now gated behind checked `Rune of Opening` source rows, with `OpenOneTypeChestAllAtOnce` enabling same-kind batch opening and `OpenAllTypeChestAllAtOnce` enabling all-kind one-click opening while exact cost/path semantics remain marked unverified. Inventory now has conservative Cube, Alchemy and Synthesis entries: unlocked items can be consumed into a persisted `CubeProgress` total using the checked rarity `Cube XP` values, converted to hero gold using the checked rarity `Alchemy value` table, or combined as 9 unlocked same-rarity backpack items into 1 next-rarity item. Locked items are protected from discard/Cube/Alchemy/Synthesis, and the item grid now maps macOS `Option`-click to the source guide's `Alt+Click` lock toggle before selection handling; `TBH --self-test` guards that interaction mapping. The first modeled `Rune of Expansion` / `扩张符文` source row with `MaxInventorySlot` category now increases local backpack capacity by a conservative `+10` scaffold after the modeled formation prerequisite, without claiming exact original cost or stacking semantics. Reset clears the local Cube progress. Settings now exposes a conservative unlocked-stage selector plus a current-stage retry action; the tracker keeps a high-water unlocked stage so revisiting earlier stages does not erase forward progress. `SourceRuneCatalog` now preserves the checked 197-row Rune Tree table as source data, but most nodes are not executable runtime effects yet. | Missing exact original simultaneous combat timing/targeting, full Cube leveling/reward/effect semantics beyond checked XP accumulation, full Alchemy UI/context/rate semantics beyond checked rarity gold conversion, full Synthesis skip/failure probabilities and Synthesis-level item-level downgrade semantics beyond the checked 9-to-next-tier scaffold, runtime costs/effects/layout/point economy for most of the 197-node Rune Tree beyond the currently modeled nine nodes, exact Rune of Expansion cost/slot increment/stacking/layout semantics, full skill-point tree, exact Soul Stone box drop rates, exact chest contents/rates, exact box drop probabilities/conditional timing beyond the current source-mapped clear rewards, exact original item-lock visual styling/modifier handling beyond macOS Option-click mapping, and exact original stage-selection UI/autofarm/retry semantics beyond the current high-water selector. |
| Party and heroes | Six heroes: Knight, Ranger, Sorcerer, Priest, Hunter, Slayer. Up to three deployed; second and third slots unlock through Rune Tree. | `HeroClass` exposes the six original classes with role/grade/base stats and art mapping. `HeroParty` persists three candidate slots with a default Knight/Priest/Ranger lineup and starts with one deployed slot. Settings still exposes the checked Rune Tree route for slots 2/3 after Lv.3 using checked `50,000G`/`150,000G` Rune of Command formation costs; the character panel now also lets players directly unlock locked support positions from the party rows, spending the same checked formation gold and, when unlocking position 3 from a one-slot party, opening position 2 at the same time so the party remains contiguous. Unlocked non-primary members now emit visible per-member support attacks from per-slot independent cooldowns based on their support speed, the modeled all-hero movement-speed Rune bonus, and active support attack-speed buffs, while also rotating through their own class cooldown skills using checked Lv1 skill values. `SourceRuneCatalog` preserves the checked 197-node / 195-`Next` / 11-sparse-`Previous` Rune Tree source table, while `RuneTree` currently executes the checked `Rune of Command`, `Rune of Awakening`, `Rune of Repose`, `Rune of Hoarding`, `Rune of Training`, first `Rune of Expansion` / `MaxInventorySlot`, and two `Rune of Opening` / chest-batch-opening scaffold nodes only. `RuneTree` models `Rune of Awakening` as a Lv.3-gated second active skill slot with approximate-cost labeling only, and runtime battles receive that slot count instead of enabling every modeled active skill at once. The first modeled `Rune of Expansion` is also Lv.3-gated, requires the modeled second party-slot prerequisite, and raises local backpack capacity from 50 to 60 as a conservative runtime scaffold because the checked source row proves category but not exact cost or per-node increment. Active skill slots now read a persisted per-class loadout, so the character panel can choose which checked active skill occupies each unlocked slot; invalid/old selections fall back to the source-order defaults. The settings panel can reset currently modeled Rune Tree nodes, re-lock party, active skill slot and inventory expansion state, refresh battle state, and refund only the locally checked formation gold costs. | Need runtime behavior for the rest of the 197-node Rune Tree source catalog, exact chest-opening Rune path/cost semantics, non-formation costs/effects, exact reset price/refund/point semantics beyond the currently checked local formation-cost refund, exact point/gold economy beyond the two checked formation command runes and approximate-cost `Rune of Awakening`, exact Rune of Expansion cost/capacity-increment/stacking rules, exact original active skill equip UI/order/cost constraints beyond the current persisted per-class slot picker, independent HP/equipment/skill levels per party member, exact original support cooldown formulas/attack timing, party-targeting effects, and exact original class balance. |
| Stages | Steam officially confirms 3 Acts and 4 difficulty tiers. Wiki gives the detailed 3 Acts x 10 stages x 4 difficulties = 120-stage shape, Normal/Nightmare/Hell/Torment labels, X-10 Act Boss/Soul Stone rules, per-stage levels, waves, kill counts, gold, XP, monster/Boss names, Boss HP, monster compositions, item-level thresholds, and the 61-row monster stat table with ATK and Atk Spd. | The app keeps the 30 display stages for navigation, but now attaches a 120-row mined runtime table keyed by difficulty/act/stage. Progress bars and advancement use the mined kill count, spawned monsters use mined monster/Boss names and HP, and the active monster name is selected by scaling the stored composition weights across the mined kill target instead of repeating the final composition entry when counts differ. Spawned monsters now also resolve their source monster database row by name/stage code and use that row's ATK as the local base attack plus `round(Atk Spd * 10)` as the runtime speed scalar before the existing local difficulty/level/New Game+ scaling; live incoming hit logs now also consume each spawned monster's stored `critRate` with the current local `1.5x` monster-crit scaffold. `StageEncounterState` exposes current encounter, wave number, wave count and wave-local encounter progress from the mined data, while `StageEncounterPlan` materializes the full per-stage encounter list and wave buckets. `GameLoop` now starts a battle with all remaining encounters in the current wave, `Battle` keeps those monsters alive together with per-enemy HP/defeat state, aggregates rewards/loot as each enemy dies, and `ProgressTracker` advances by the cleared encounter count. `ProgressTracker` also exposes unlocked stage selections up to the current high-water stage, rejects future locked stages, and can restart the current stage by clearing encounter progress; `GameEngine` rebuilds the active battle after either action. The battle/settings UI surfaces the current wave, wave-local progress, enemy, highest unlocked stage, target-stage picker and retry action. Rewards use mined gold/XP, Boss HP also comes from the same mined table, offline rewards use the current difficulty row, regular monster drops and chest item rewards use the stage item-level threshold, stage clear now persists both source-mapped chest families for that stage (`Normal Monster` + `Stage Boss`, or `Normal Monster` + `Act Boss` on X-10), and Boss Soul Stone gates still consume only on successful clear. | Still missing exact original simultaneous combat timing/targeting, exact monster DEF and critical-damage formulas, exact confirmation that the local difficulty/level ATK scaling matches original combat, exact chest/box Soul Stone drop rates, exact chest contents/rates, exact box drop probability rules, exact original stage-selection/autofarm behavior, and exact visual assets for every listed monster. |
| Combat stats | Wiki lists 63 tracked stats, ordered flat/additive/multiplicative bonuses, dodge/block/resistance/armor/damage reduction/absorption/floor, status effects, elements and delivery types. | Basic ATK/DEF/SPD/crit HP model with simple damage calculator. The modeled active skills now carry structured damage-element metadata (`physical`, `fire`, `cold`, `lightning`) and delivery metadata (`melee`, `projectile`, `rangeAOE`, `trap`, `summonProjectile`, heal/buff/resurrection categories). Runtime charged-trap triggering now reads this metadata instead of a hard-coded skill-id list, so elemental-trigger behavior has a concrete data hook. The checked `穿刺射击` and `旋转斧` bleeding scaffolds now feed battle status UI: once 3 lodged arrows mark a target bleeding, or a `旋转斧` tick hits a live enemy, the active enemy and side-enemy views expose a red status badge guarded by self-test. The current cold-delay scaffolds for `冰球术`, `暴风雪` and `寒霜弩箭` now also feed compact enemy status badges (`寒冷` / `冻结`) so visible combat feedback matches the already-modeled slow/freeze stand-ins. The current `将军怒吼` stun-delay scaffold now feeds a compact `眩晕` enemy status badge. | Need expanded stat model before high-fidelity combat: full resistances, armor, dodge/block, exact stun/cold/freeze/bleed durations, additive/multiplicative stat ordering, exact status damage/expiry rules and exact damage pipeline remain unimplemented. |
| Items | 10 rarity grades: Common, Uncommon, Rare, Legendary, Immortal, Arcana, Beyond, Celestial, Divine, Cosmic. 20 gear types across weapon/offhand/armor/accessory categories, materials, boxes, D/E/I slots, Alchemy value, Cube XP, and Synthesis. | The 10-grade rarity ladder is modeled with Chinese labels, original-style colors, Alchemy value, Cube XP, and D/E/I slot metadata, plus next-tier Synthesis progression. The item model now carries the 20 source equipment types and persists `itemLevel` plus optional structured `sourceGearID` instead of relying only on description text: Sword/Bow/Staff/Scepter/Crossbow/Axe, Shield/Arrow/Orb/Tome/Bolt/Hatchet, Helmet/Armor/Gloves/Boots, and Amulet/Earring/Ring/Bracer. `SourceItemCatalog` now preserves all 20 checked gear type pages, their aggregate 5,760 gear-entry count, per-type rarity distributions, 396 checked base level progression IDs/names such as Sword `300001 Long Sword` through `300020 Radiant Sword`, 115 checked material rows across six material categories, and 59 checked stage chest rows. Runtime loot resolves each concrete equipment type and itemLevel to the closest checked base level progression at or below that level, so a Lv.12 Scepter uses checked source identity `330003 Blessed Scepter` instead of a fabricated rarity-prefix name, writes `sourceGearID = 330003`, and old saves that only stored `来源装备 330003` in the description migrate that ID on decode. These map to 10 macOS loadout slots with item locking, auto-equip, live comparison preview, persisted chest counts, manual opening for individual saved chest instances, stage-threshold-capped monster drops, a first Cube infusion scaffold that consumes unlocked backpack items into a saved Cube XP total, a first Alchemy action that consumes unlocked backpack items for checked rarity gold, and a first Synthesis action that consumes 9 unlocked same-rarity backpack items into 1 item of the next rarity tier while preserving the highest consumed item level. The Synthesis preview now uses the same input-selection rule as runtime synthesis, shows unlocked/locked same-rarity material counts, shows the guaranteed output item level and checked source base gear name/ID when the checked 9-item recipe is ready, surfaces the checked approximate source result examples (`普通 -> 稀有 ~60%`, `优秀 -> 传说 ~40%`, `稀有 -> 传说 ~20%`, `传说 -> 不朽 ~5%`), and labels the still-unverified skip-tier/item-level downgrade variants instead of treating those examples as a complete probability table. Runtime equipment art now prefers structured `Item.sourceGearID`, then falls back to `Item.equipmentType + itemLevel`, and maps concrete equipment to one of 396 source-backed 16x16 `source_gear_*` base progression icons; 20 source-backed `item_*` gear page icons remain as equipment-type fallback art. `ResourceSelfTest` guards source dimensions/alpha/coverage, and `scripts/audit-local-item-icons.sh` additionally pins source URLs, SHA-256 payloads and byte counts so self-drawn semantic placeholders or UI-tile crops cannot silently return. Legacy saves without `equipmentType` now infer a concrete type from old item names/descriptions for common cases such as old bows, crossbows, scepters, shields, orbs, amulets, earrings and bracers before falling back to the slot default, reducing mismatched backpack icons for old inventories. `SourceItemCatalog` material rows map to 115 source `source_material_*` icons, `SoulStoneKind` uses the four checked Soul Stone material icons, and Settings chest rows use the three checked source chest-family icons instead of a single generic box. Unknown non-equipment items still retain the older generic `official_item_*` fallback. | Need exact per-rarity/per-affix 5,760 item database rows beyond type-page aggregate counts and checked base progression names, exact material use/effect systems beyond the checked taxonomy, full Synthesis probability/skip/failure table, Synthesis level costs and item-level downgrade formula, full Cube level/reward/effect execution beyond checked rarity XP accumulation, full Alchemy flow/context beyond checked rarity gold conversion, exact chest contents/rates, exact per-item variant icon/name progression beyond the checked 396 base-progression gear icons and material/chest source icons, and original item stat affixes. |
| Skills | Wiki page lists 106 active/attack skills and 108 passive nodes. Skills include physical/fire/cold/lightning, melee/projectile/AOE/summon/DoT/trap delivery, buffs, heals, resurrection, continuous blessings, cooldowns, base attacks, and attack-count triggers. | `SourceSkillCatalog` now preserves all 106 checked active/base/monster rows as a data-only source catalog with ID, English name, activation, damage type, delivery and range; this includes base attacks and monster skills but does not make them executable combat logic. `HeroSkills` contains the 36 runtime-modeled named hero active skills, with six per class, activation metadata and the checked 10-level value tables from the individual skill pages. `PassiveSkills` now preserves all 108 checked passive rows with ID, English name, stat, value type and value, with 18 rows per class prefix; the character panel renders the 104 rows whose current source page exposes a passive icon through bundled `source_passive_*` assets, while `IncreaseProjectileDamage` and `SkillHealIncrease` remain intentionally blank because the current source pages expose no icon for those stat families. `Hero.unlockedPassiveSkillIDs` now persists explicitly unlocked passive IDs, ignores IDs from the wrong hero class, and applies a conservative runtime pass for 30 source stat kinds: `MaxHp`, `AttackDamage`, `AllElementalResistance`, `Armor`, `AttackSpeed`, `BlockChance`, `CastSpeed`, `CriticalChance`, `CriticalDamage`, `HpLeech`, `HpRegenPerSec`, `AddHpPerHit`, `AddHpPerKill`, `DamageReduction`, `DamageAbsorption`, `DodgeChance`, `ElementalDodgeChance`, `MaxDodgeChance`, `PhysicalDamagePercent`, `FireDamagePercent`, `ColdDamagePercent`, `LightningDamagePercent`, `IncreaseProjectileDamage`, `IncreaseAreaOfEffectDamage`, `AreaOfEffect`, `MovementSpeed`, `SkillHealIncrease`, `SkillRangeExpansion`, `SkillDurationIncrease`, and `CooldownReduction`; battle ticks now consume the regen, on-hit, on-kill and incoming-damage-reduction, fixed damage-absorption, element-marked fire/cold/lightning resistance and elemental dodge plus pre-damage dodge, dodge-cap and block hooks, main-hero attacks and skills consume the life-leech hook from actual HP damage dealt, and main-hero skills consume the damage, healing, skill-range expansion, duration, cooldown and cast-speed hooks. Checked projectile skill damage consumes `IncreaseProjectileDamage`, checked AOE skill damage consumes `IncreaseAreaOfEffectDamage`, while `AreaOfEffect` target-radius geometry remains parsed but unverified until exact original range/shape rules are available; movement-speed passives feed the current macOS battle speed scalar. `CastSpeed` currently shortens main-hero cooldown skill intervals inside the current cooldown scaffold; it does not model separate cast animations or exact original cast-time semantics. There is still no verified passive unlock path UI, and public source checks did not expose complete unit semantics for every passive stat, so this is an opt-in runtime hook scaffold rather than a complete passive tree. Runtime battles now restrict the main hero and support-member skill lists by the modeled active skill slot count and a persisted per-class `ActiveSkillLoadouts` selection. With no saved selection, the app keeps the earlier source-order fallback; with a selection, the chosen checked skills fill the unlocked slots and invalid/duplicate saved IDs are ignored or replaced by defaults. This is closer to original build/loadout play than the old first-N-only stand-in, but it is still not a verified copy of the original equip-selection UI or ordering rules; isolated self-tests still create max-slot battles when validating individual later-skill formulas. Main-hero cooldown skills execute from cooldown timers; attack-count skills such as `穿透突刺` and `粉碎强击` trigger after base attacks using the still-placeholder 3-hit cadence; continuous Priest blessings are modeled as metadata and now apply Lv1 source-value modifiers (`力量祝福` uses +500% attack as a 6.0x attack multiplier, while `守护祝福` uses +10 elemental resistance as a temporary 10% reduction only for source elemental incoming damage, currently `Fire`/`Cold`/`Lightning`, without reducing physical or chaos hits). Damage skills now use Lv1 percent values such as `穿透突刺` 2500%, `天堂之怒` 4300% and `粉碎强击` 6200% instead of earlier placeholder multipliers; Priest `治愈` now restores one wounded living ally using that ally's own max HP and checked Lv1 `100%` value, with target priority represented by a most-wounded scaffold. `神盾领域` now creates an active party damage-shield buff for main-Knight and support-Knight casts: while active it applies the checked Lv1 `500` damage-block value as a consumable shield pool against incoming monster damage to the main hero and living support members, support casts emit support-attributed buff logs, and support casts use the raw Lv1 value without independent support skill-level/equipment scaling; duration still uses the cooldown-length stand-in and field geometry stays simplified to the current party scaffold until exact rules are verified. `不屈意志` is now modeled as a lethal-damage rescue instead of a normal cooldown cast: when incoming monster damage would defeat the Knight, it triggers once for the current stage key, logs the skill, restores Lv1 `300%` max HP even above the normal max HP cap, and persists the consumed stage key so later battles in the same stage do not refresh it. `神圣之刃` now creates an active cooldown buff instead of an instant heal for main-Knight and support-Knight casts: main casts apply the checked +50% attack modifier and heal Lv1 `20` HP on hero attacks, while support casts apply the same +50% attack modifier to that support member and heal that support member by the raw Lv1 `20` HP on support attacks; duration still uses the current cooldown-length stand-in and support casts do not invent independent support scaling. `迅捷觉醒` now creates an active cooldown buff and applies its checked Lv1 `+500%` attack-speed modifier as a 6.0x attack-speed multiplier, also using the cooldown-length duration stand-in until exact duration is verified. `天堂之怒` now creates an active attack-damage buff instead of an instant direct hit: while active, later hero attacks apply the checked Lv1 `4300%` lightning range-damage value across the live wave, with duration still represented by the cooldown-length stand-in and range simplified to the current wave until exact geometry is verified. `圣域` now creates an active over-time healing buff for main-Priest and support-Priest casts: while active it applies the checked Lv1 `300` HP-per-second healing value to the main hero and living support members on battle ticks, with support casts keeping support-attributed heal logs. Duration is still represented by the current cooldown-length stand-in, support casts use the raw Lv1 value without main-hero passive healing modifiers, and field geometry is simplified to the current party scaffold until exact rules are verified. `粉碎强击` now applies the checked kill rider: if the main hit kills its focused target, a `350%` attack shockwave damages the other alive enemies in the current wave. `将军怒吼` now creates an active crit-coefficient buff for main-Slayer and support-Slayer casts: while active it maps the checked Lv1 `+500%` party critical chance coefficient into a 6.0x crit-rate multiplier for main-hero and support attacks, delays all alive current-wave enemy cooldowns by at least one attack interval as a stun stand-in, exposes that stand-in as a compact `眩晕` enemy status badge, and keeps support casts support-attributed in the battle log. `嗜血` now consumes half of the Slayer's current HP on activation and applies the checked Lv1 `+4000%` attack-damage value as a 41.0x active attack multiplier, with duration still represented by the cooldown-length stand-in. `快速装填` now creates an active attack-count buff: at Lv1 it grants +50% attack speed for the checked 3 hero attacks. Unlocked support members now also rotate through the `COOLDOWN` skills in their current active loadout, log those skill names as support actions, route support skill casts through the skill-cast audio event, keep their own sustained summon/range state for `烈焰九头蛇`, `暴风雪`, and `弩炮塔`, and keep a support-attributed `圣域` healing field for Priest support casts. | Need exact original active skill equip UI/order/cost constraints beyond the current persisted per-class slot picker, exact skill-slot ordering, attack-count intervals, passive unlock paths, exact passive stat units/stacking/caps for all 30 stat kinds including exact block reduction/animation/cap semantics and full elemental resistance/dodge/status pipeline semantics, exact base-attack and monster skill execution, exact buff durations for `神盾领域`, `神圣之刃`, `迅捷觉醒`, `天堂之怒`, `圣域`, `将军怒吼`, `嗜血`, and the remaining buffs, exact `治愈` target priority/cooldown behavior when no ally is wounded, exact `神盾领域` field geometry, shared-pool versus per-ally shield semantics, support-member skill-level/equipment formulas and stacking/replacement rules for `神盾领域`, exact `将军怒吼` stun duration/stacking and critical-coefficient math, elemental resistance/status effects, exact resurrection target priority/self-revive/cooldown behavior, summoned entities, exact support-member independent skill levels/cooldowns/equipment, exact range geometry for `天堂之怒`, exact field geometry, support-member skill-level/equipment formulas and stacking/replacement rules for `圣域`, exact AOE geometry for “surrounding enemies”, and exact semantics for whether `不屈意志` resets on stage retry versus stage identity. The current source catalogs, Lv1 value use and passive catalog/icons are data and visual baselines, not a complete skill-level, base-attack, monster-skill or passive-effect system. |
| Offline | Wiki states offline rewards grant gold + XP only, no chests, capped at 8 hours, using last logged-out stage; the Rune Tree page also states that Rune of Repose unlocks offline rewards and that the first visible offline branch boosts are `Offline Gold +10%` and `Offline XP +10%`. | Offline rewards grant gold + XP only, cap at 8 hours, use the current stage rather than only the broad Act, and stay disabled until the Lv.3 Rune Tree gate plus `安息符文` / Rune of Repose unlock. `储藏符文：离线金币 +10%` and `训练符文：离线经验 +10%` now require Rune of Repose and independently apply 1.10x gold or XP multipliers. Offline XP then passes through the same local `GamePacing` XP multiplier and level-cap grant path as online combat, and `statistics.offlineXP` records only the XP actually applied to the hero rather than the raw offline formula output. Because the checked page exposes these nodes/effects but not their exact costs, the macOS implementation labels their costs as unverified and does not invent gold prices. | Exact original offline formula, Rune of Repose / Hoarding / Training costs, repeated boost node stacking beyond the first checked +10%, original XP pacing/level-cap formula, and full offline branch reset/economy semantics are still not proven. |
| Controls | Wiki lists `Shift+F11` scale reset, `Shift+F12` recenter, `Alt+Left-Click` item lock. | macOS menu-bar UI has tabs and settings. Inventory now supports Option-click item locking, explicit lock/unlock buttons, lock badges, and discard protection for locked items. Settings now exposes a menu-bar translation of scale control: the panel can be scaled between `85%` and `125%` in deterministic `5%` steps, and `重置面板尺寸` returns it to the default `430x520` baseline. The reset button also maps the checked original `Shift+F11` shortcut, guarded by self-test and Swift Testing. Because the app uses `MenuBarExtra.window`, popover placement remains system-anchored to the status item rather than a freely positioned original taskbar window. | Need a stronger original capture before adding more keyboard-driven controls. `Shift+F12` recenter is currently not mapped because the macOS popover has no independent saved window position to recenter. |

### Current Skill Detail Addendum

- `10601` `不屈意志`: The checked fan Wiki page lists cooldown trigger, physical damage category, range `150`, Lv1/Lv10 `300/1200`, and description text stating that the Knight stands back up once per stage with `300%` HP when near death. The macOS app now maps this away from normal cooldown casting: main-Knight lethal monster damage consumes the current persisted stage key and revives the hero with checked Lv1 `300%` max HP, while a support Knight with `10601` in its active loadout now self-revives once from lethal monster damage in the current battle state using the same raw Lv1 `300%` support max-HP value. Exact original near-death threshold, retry/stage reset semantics, support-specific reset scope, animation timing, independent support level/equipment formulas and over-max-HP follow-up rules remain unimplemented.
- `10201` `盾牌冲锋`: The checked fan Wiki pages list cooldown trigger, physical damage, melee hit type, range `900`, Lv1/Lv10 `3000/5700`, and description text stating that the Knight raises a shield, charges forward, and deals `3000%` physical damage to enemies collided with. The macOS app now maps this to a focused melee collision-target scaffold using the checked Lv1 `3000%` value for both main-hero and support-member casts, instead of widening the source `Melee` delivery into an all-wave AOE. Exact original charge travel timing, path/width, collision target count beyond the current focused target, target ordering, knockback/stun rules, physical resistance interaction and damage falloff remain unimplemented.
- `10101` `穿透突刺`: The checked fan Wiki pages list attack-count trigger, physical damage, melee/AOE hit type, range `200`, Lv1/Lv10 `2500/4300`, and description text stating that the Knight thrusts forward and deals `2500%` physical damage to enemies in range. The macOS app now maps this to checked Lv1 `2500%` physical melee range damage across alive current-wave enemies when the attack-count trigger fires. Exact original attack-count cadence, forward thrust path, range geometry, collision width, maximum target count, target ordering, travel timing, physical resistance interaction and falloff remain unimplemented.
- `10301` `报应打击`: The checked fan Wiki pages list attack-count trigger, physical damage, melee hit type, range `150`, Lv1/Lv10 `1500/3300`, and description text stating that it strikes multiple times, each hit dealing `1500%` physical damage, with more strikes as the hero's HP gets lower. The macOS app now maps this to focused melee multi-hit damage using the checked Lv1 `1500%` value per hit, with a monotonic HP-ratio scaffold: at least two strikes, increasing through lower HP buckets up to five strikes. Exact original attack-count cadence, HP thresholds, strike-count cap, retargeting after overkill, melee geometry, physical resistance interaction and support-member behavior remain unimplemented.
- `10401` `神盾领域`: The checked fan Wiki pages list cooldown trigger, physical damage category, range/AOE hit type, range `150`, Lv1/Lv10 `500/1850`, and description text stating that it deploys a protective field/aura that blocks `500` damage for allies within the area. The macOS app now maps this to a checked Lv1 `500` consumable party damage-shield pool for main-Knight and support-Knight casts; that pool can absorb incoming monster damage for the main hero and living support members, and support casts produce support-attributed buff logs. Exact original duration, field geometry, shared-pool versus per-ally shield semantics, support independent formulas, stacking/replacement behavior, damage-type interaction and downed-ally inclusion remain unimplemented.
- `20101` `快速射击`: The checked fan Wiki page lists attack-count trigger, physical damage, projectile hit type, range `1150`, Lv1/Lv10 `1320/2400`, and description text stating that the Ranger continuously fires multiple arrows, with each arrow dealing `1320%` physical damage. The macOS app now maps this to checked Lv1 `1320%` physical projectile damage and emits a minimum two-hit focused-target volley when the attack-count trigger fires. Exact original arrow count, attack-count cadence, shot spacing, target retargeting rules after overkill, travel timing and physical resistance interaction remain unimplemented.
- `20201` `散弹射击`: The checked fan Wiki page lists cooldown trigger, physical damage, projectile hit type, range `1650`, Lv1/Lv10 `1620/3060`, and description text stating that it fires multiple arrows that track enemies, with each arrow dealing `1620%` physical damage. The macOS app now maps this to checked Lv1 `1620%` physical tracking-projectile damage, distributing one projectile hit to each alive current-wave enemy for both main-hero and support-member casts. Exact arrow count, projectile distribution, target priority, travel timing, physical resistance interaction and overkill/falloff behavior remain unimplemented.
- `20301` `箭雨`: The checked fan Wiki page lists cooldown trigger, physical damage, range hit type, range `1300`, Lv1/Lv10 `2150/4490`, and description text stating that the Ranger shoots many arrows into the sky to deal `2150%` physical damage to enemies in a wide range. The macOS app now maps this to checked Lv1 `2150%` physical range damage across alive current-wave enemies for both main-hero and support-member casts. Exact arrow count, fall timing, target distribution, impact geometry, physical resistance interaction and damage falloff remain unimplemented.
- `20501` `穿透之箭`: The checked fan Wiki page lists attack-count trigger, physical damage, projectile hit type, range `1200`, Lv1/Lv10 `2440/3880`, and description text stating that it fires a piercing arrow to deal `2440%` physical damage to enemies hit. The macOS app now maps this to checked Lv1 `2440%` physical projectile pierce damage across alive current-wave enemies. Exact original pierce line geometry, maximum pierce count, target ordering, travel timing, collision width, physical resistance interaction and falloff remain unimplemented.
- `20601` `穿刺射击`: The checked fan Wiki page lists attack-count trigger, physical damage, projectile hit type, range `1200`, Lv1/Lv10 `1000/3700`, and description text stating that it fires a deep-wounding arrow; each lodged arrow increases damage by `1000%`, and 3+ lodged arrows cause bleeding. The macOS app now tracks lodged arrows per enemy, scales the checked Lv1 physical projectile damage by the lodged-arrow count, marks the target as bleeding at 3 lodged arrows, and exposes that bleeding state as a compact red battle status badge on active and side enemies. Exact lodged-arrow duration/reset rules, bleed damage, bleed tick cadence, bleed duration, bleed stacking, target retention after stage/wave changes, physical resistance interaction and projectile travel remain unimplemented.
- `30101` `火球术`: The checked fan Wiki page lists cooldown trigger, fire damage, projectile/AOE hit type, range `950`, Lv1/Lv10 `2700/4950`, and description text stating that a fireball explodes on contact with an enemy and deals `2700%` range fire damage. The macOS app now maps the checked projectile/AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `2700%` value for both main-hero and support-member casts. Exact projectile travel, contact timing, explosion radius/positioning, elemental resistance interaction and fire-specific status rules remain unimplemented.
- `30201` `冰球术`: The checked fan Wiki page lists cooldown trigger, cold damage, projectile/AOE hit type, range `950`, Lv1/Lv10 `1500/2580`, and description text stating that an ice orb is hurled at enemies, slows enemies hit, and deals `1500%` multi-hit cold damage. The macOS app now maps this to two range-damage hit logs across all alive current-wave enemies, splitting the checked Lv1 `1500%` value across those hits, delays hit enemies' next attack tick as a slow stand-in, and exposes that stand-in as a compact `寒冷` enemy status badge. Exact projectile travel, multi-hit count/timing, slow amount/duration, cold resistance interaction, freeze/chill status rules and AOE geometry remain unimplemented.
- `30301` `闪电术`: The checked fan Wiki page lists cooldown trigger, lightning damage, AOE hit type, range `1050`, Lv1/Lv10 `2550/4980`, and description text stating that electricity is unleashed forward to deal `2550%` lightning damage to enemies. The macOS app now maps the checked AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `2550%` value for both main-hero and support-member casts. Exact forward electricity path, chain/line geometry if any, lightning resistance interaction, shock/status behavior and target falloff remain unimplemented.
- `30401` `烈焰九头蛇`: The checked fan Wiki page lists cooldown trigger, fire damage, projectile/summon hit type, range `1100`, Lv1/Lv10 `2300/3650`, and description text stating that it summons a hydra which fires fireballs dealing `2300%` damage. The macOS app now maps this to an active summon buff for main-hero and support-member Sorcerer casts: after activation, the hydra stand-in fires checked Lv1 `2300%` focused fire projectile damage on battle ticks, with support casts keeping support-attributed damage logs and visible deployable/status state. The page does not expose exact summon duration, hydra count, projectile cadence, target selection, travel time, fireball splash behavior, fire resistance interaction, support summon replacement or stacking, so those remain placeholders or unimplemented.
- `30501` `暴风雪`: The checked fan Wiki page lists cooldown trigger, cold damage, range hit type, range `1100`, Lv1/Lv10 `500/1940`, and description text stating that it summons many ice blocks, deals `500%` cold damage per second to enemies in range, and cools enemies. The macOS app now maps this to an active range damage-over-time buff for main-hero and support-member Sorcerer casts: while active, battle ticks apply checked Lv1 `500%` cold range damage per second across alive current-wave enemies, delay hit enemies' next attack tick as a cooling stand-in, and expose that stand-in as a compact `寒冷` enemy status badge. The page does not expose exact storm duration, ice-block cadence/count, cooling amount/duration, freeze/chill rules, field geometry, cold resistance interaction, support storm replacement or stacking, so those remain placeholders or unimplemented.
- `30601` `陨石打击`: The checked fan Wiki page lists cooldown trigger, fire damage, range hit type, range `1100`, Lv1/Lv10 `5500/9550`, and description text stating that it summons a meteor to deal `5500%` damage to enemies in a wide range. The macOS app now maps this to checked Lv1 `5500%` fire range damage across alive current-wave enemies for both main-hero and support-member casts. Exact meteor travel/fall timing, impact delay, impact geometry, fire resistance interaction, burn/status behavior and damage falloff remain unimplemented.
- `40101` `治愈`: The checked fan Wiki pages list cooldown trigger, physical damage category, no hit type, range `950`, Lv1/Lv10 `100/280`, and description text stating that it restores one ally by `100%` of that ally's maximum HP. The macOS app now maps this to a living-ally heal for main-hero and support-member casts, using each target's own max HP and choosing the most wounded living ally by missing-HP ratio as a scaffold. Exact original target priority, cooldown behavior when no ally is wounded, independent support-member HP/equipment/skill-level formulas and any overheal/cap edge cases remain unimplemented.
- `40301` `天堂之怒`: The checked fan Wiki page lists cooldown trigger, lightning range hit type, range `1050`, Lv1/Lv10 `4300/7900`, and description text stating that attacks deal `4300%` lightning range damage while the buff is active. The macOS app now maps this to an active attack-added damage buff for main-Priest and support-Priest casts: while active, later hero or support attacks apply the checked Lv1 `4300%` lightning range-damage value across alive current-wave enemies, with support casts and later support-added damage staying support-attributed in battle logs. Exact original buff duration, support-member independent skill/equipment formulas, range geometry, stacking/replacement behavior and hit-frame timing remain unimplemented.
- `40401` `圣域`: The checked fan Wiki pages list cooldown trigger, physical damage category, range/AOE hit type, range `1100`, Lv1/Lv10 `300/1920`, and description text stating that allies inside the sanctuary recover `300` HP per second. The macOS app now maps this to an active over-time healing buff for main-Priest and support-Priest casts: while active, battle ticks heal the main hero and all living support members by the checked Lv1 `300` HP-per-second value, and support casts produce support-attributed heal logs. Exact original duration, field geometry, inclusion/exclusion rules for downed allies, support-member independent HP/equipment/skill-level formulas and stacking/replacement behavior remain unimplemented.
- `40601` `复活`: The checked fan Wiki pages list cooldown trigger, physical damage category, no hit type, range `1100`, Lv1/Lv10 `300/750`, and description text stating that it revives one fallen party member with `300%` of that member's max HP. The macOS app now keeps runtime HP/defeat state for support members, prevents defeated support members from attacking or casting, and maps main-Priest and support-Priest `复活` casts to reviving the first eligible fallen support member for checked Lv1 `300%` max HP, with support casts attributed to the support Priest and excluding that caster as a self-revive target. Exact target priority, support self-revive rules, whether cooldown waits for a valid fallen target, independent support HP/equipment/skill-level formulas, over-max-HP cap behavior, animation timing and downed-member cooldown ticking remain unimplemented.
- `50101` `爆炸弩箭`: The checked fan Wiki page lists attack-count trigger, fire damage, projectile hit type, range `1100`, Lv1/Lv10 `4840/9430`, and description text stating that the Hunter fires a bolt that explodes on hit and deals `4840%` range fire damage. The macOS app now maps this to checked Lv1 `4840%` fire projectile explosion damage across alive current-wave enemies when the attack-count trigger fires. Exact explosion radius, impact point, projectile travel timing, fire resistance interaction, burn/status rules, target falloff and attack-count cadence remain unimplemented.
- `50201` `寒霜弩箭`: The checked fan Wiki page lists cooldown trigger, cold damage, projectile hit type, range `1100`, Lv1/Lv10 `2100/3450`, and description text stating that the Hunter fires a bolt that explodes on hit, deals `2100%` range cold damage, and freezes hit enemies. The macOS app now maps this to checked Lv1 `2100%` cold projectile explosion damage across alive current-wave enemies for both main-hero and support-member casts, then delays hit enemies' next attack tick as a freeze stand-in and exposes that stand-in as a compact `冻结` enemy status badge. Exact freeze duration, freeze stacking, projectile travel, impact timing, explosion radius/positioning, cold resistance interaction, freeze/chill status rules and target falloff remain unimplemented.
- `50401` `充能陷阱`: The checked fan Wiki page lists cooldown trigger, physical damage, trap hit type, range `1150`, Lv1/Lv10 `1000/5500`, and description text stating that the Hunter launches traps that explode when struck by elemental damage. The macOS app now maps this to an active trap buff: activation arms one pending trap, and the next checked elemental skill hit from the party triggers `充能陷阱爆炸`, applying a checked Lv1 physical range-damage scaffold across alive current-wave enemies before consuming the pending trap. Support-Hunter casts now arm the same player-side visible trap counter/deployable state and detonate as support-attributed `充能陷阱爆炸` damage when a later support elemental hit, such as `爆炸弩箭`, triggers it. Exact original trap count semantics, trap duration, placement, explosion formula/radius, which elemental sources can trigger it, resistance interaction, maximum active traps, support-specific trap scaling and multi-trap detonation behavior remain unimplemented.
- `50501` `弩炮塔`: The checked fan Wiki page lists cooldown trigger, physical damage, projectile/summon hit type, range `1100`, Lv1/Lv10 `1750/3190`, and description text stating that the Hunter sets an automated crossbow that fires bolts dealing `1750%` physical damage. The macOS app now maps this to an active summon buff for main-hero and support-member Hunter casts: after activation, the turret stand-in fires checked Lv1 `1750%` focused physical projectile damage on battle ticks. Exact turret duration, firing cadence, target selection, projectile travel, bolt count, summon placement, physical resistance interaction, support turret replacement/stacking behavior and independent support cooldown semantics remain unimplemented.
- `50601` `电击弩箭`: The checked fan Wiki page lists attack-count trigger, lightning damage, projectile hit type, range `1100`, Lv1/Lv10 `2700/4500`, and description text stating that the Hunter fires a crossbow bolt that lodges in an enemy and briefly releases lightning currents dealing `2700%` lightning damage to nearby enemies. The macOS app now maps this to a checked Lv1 `2700%` lightning projectile hit on the focused target, then activates an `电击弩箭电流` range damage-over-time scaffold across alive current-wave enemies. Exact current duration, tick cadence, nearby-target geometry, lodged-target persistence, projectile travel, lightning resistance/status behavior, support-member state and attack-count cadence remain unimplemented.
- `60101` `猛击跳跃`: The checked fan Wiki pages list cooldown trigger, physical damage, melee/AOE hit type, range `850`, Lv1/Lv10 `3100/5350`, and Chinese/Traditional Chinese/Japanese description text stating that the Slayer leaps to an enemy and creates a landing shockwave dealing `3100%` physical damage to surrounding enemies. The English page currently conflicts with a focused hit plus `350%` kill shockwave description, so this remains a documented source conflict rather than a second rider. The macOS app now maps the checked melee/AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `3100%` value for both main-hero and support-member casts. Exact leap travel timing, impact delay, landing radius, positioning, physical resistance interaction and damage falloff remain unimplemented.
- `60401` `大地强击`: The checked fan Wiki page lists attack-count trigger, physical damage, melee/AOE hit type, range `300`, Lv1/Lv10 `3700/5950`, and description text stating that it creates an earthquake dealing `3700%` physical damage and that earthquake rocks explode when hit by physical AOE attacks. The macOS app maps the checked melee/AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `3700%` value, then arms a conservative local `groundSlamRockCharges` scaffold. A later modeled physical AOE hit consumes those charges, emits `大地强击岩石爆炸`, applies physical melee/AOE damage across the alive current wave, and drives the dedicated rock-burst trajectory plus rock-explosion impact cue. Exact original rock spawn count beyond the current hit-count cap, rock HP/positioning, explosion timing, explosion geometry, damage formula and full physical-AOE interaction rules remain unverified.
- `60501` `旋转斧`: The checked fan Wiki page lists cooldown trigger, physical damage, melee/AOE hit type, range `150`, Lv1/Lv10 `1000/1720`, and description text stating that Axe Spin deals `1000%` physical damage per second to surrounding enemies, may make them bleed, and attacks against bleeding enemies deal extra damage. The macOS app now models the checked per-second range damage across alive current-wave enemies and marks hit live enemies with the existing bleeding status badge. Exact spin duration, bleed chance, bleed duration, bleed damage and the extra damage formula against bleeding enemies remain unimplemented.
- `60601` `嗜血`: The checked fan Wiki page lists cooldown trigger, range `900`, Lv1/Lv10 `4000/6700`, and description text stating that it consumes half of current HP and increases attack damage by `4000%` for a time. The macOS app now maps this to an active sacrifice attack buff for main-Slayer and support-Slayer casts: activation consumes half of the caster's current HP, applies the checked Lv1 value as a 41.0x attack multiplier while active, and keeps support casts support-attributed with the sacrifice recorded in the battle log. Exact original duration, whether HP cost can be reduced/prevented, independent support-member skill/equipment formulas, stacking/replacement behavior and low-HP edge cases remain unimplemented.

## Original Art And Animation Baseline

| Area | Original Evidence | Current macOS State | Gap |
| --- | --- | --- | --- |
| Presentation | Steam confirms tiny taskbar idle RPG with adorable pixel heroes. Wiki and Steam media expose pixel heroes, monsters, gear icons, taskbar-themed presentation. The reproducible battle-scene audit records the official strip as 776x180, 30 fps, 184 frames, about 4.31:1 aspect ratio, with a low active lane, large dark negative space above it, a simple upper-left stage pill reading `1-6`, no decorative card frame, and a short lower warm platform whose sampled width ratio is `0.700` with x range `0.137-0.836`. | App uses extracted pixel sprites for hero, monsters, item icons, app icon and brand/menu-bar resources. Battle view now follows the Steam battle-scene strip more closely: horizontal unframed arena, upper-left platform pill showing only the stage code, party on the left platform side, enemy side on the right, compact overhead HP bars, a large focused target, smaller live wave enemies with mini HP bars, no decorative border, and no in-scene numeric HP labels. The popover arena now uses a 2304 pt viewport inside the expected 936 pt content width; the lower warm ground keeps a local `0.90` width ratio and `0.14` height ratio so the enlarged macOS battle tab adds vertical viewing area without letting the warm platform dominate the taller viewport. The deterministic `1872x4608` render keeps the local platform width at `90%`, the configured local scene ratio at about `0.41:1`, and the same relative ground/platform/animation guards. The guard now rejects regressions where the subtle-margin platform, upper-left platform pill, overhead HP bars, deployable marker, deterministic combatant idle motion, projectile trajectory or skill-impact cue disappear or drift out of the local battle lane. | Broad style direction is aligned. Need more exact class/monster/stage mappings, direct live screenshot capture is still permission/state-dependent, and frame-by-frame rendered comparison is still needed before claiming exact original crop, timing, sprite scale, lane anchors, HP bar dimensions or stage pill offsets. |
| Heroes | Wiki links official-style portraits for six heroes, and Steam's battle-scene media shows the Knight as a compact white-cloak, red-shield front-line combat figure rather than a gray armor stand-in. | Resource folder includes six `official_hero_*` UI sprites plus six edge-transparent `battle_hero_*` combat sprites. All six `battle_hero_*` files now use the corresponding complete `official_hero_*` transparent sprite as an identity-safe battle figure, preserving each class's recognizable silhouette after earlier screenshot/portrait trimming made some battle-tab heroes read as the wrong class. The legacy `battle_*` / `hero_*` broad screenshot crops remain reference-only because they include HP bars, background, effects or UI fragments. Self-test and release resource self-test both assert that battle hero mappings use dedicated transparent compact `battle_hero_*` resources; the release resource self-test also requires exact class-to-resource identity, unique hero/battle sprites, transparent corners, a fully transparent outer edge, registered source dimensions, reasonable visible-pixel coverage, raw-sample provenance against `official_hero_*` connected-frame removal, no embedded HP-bar green runs, visible Knight color markers and a connected Knight subject so swapped classes, empty transparent files, white-backed portraits, oversized screenshot blocks, HP-bar screenshot crops, fragmented equipment crops and undocumented visible-pixel edits are rejected. `scripts/audit-local-hero-sprites.sh` now also repeats this check against `dist/TBH.app` when present and compares packaged `battle_hero_*` / `official_hero_*` payloads with source resources. | Need exact original party spacing/scale audit, class-specific combat animation timing, and unobscured original battle-pose sprites for all six classes before claiming original combat-pose parity. |
| Monsters | Steam states 50+ monster types; Wiki stages reference many specific monster sprites. | The source monster database preserves 61 Wiki/datamined rows and 52 unique monster names, so the source roster identity now covers the Steam `50+` lower bound as `52/50+` without treating stage-composition art rows as the roster count. The stage runtime table preserves 49 monster/Boss composition names across the 120 listed stages, and Settings now exposes the current runtime/art boundary as `49/52`: `剧毒领主`, `扁虱`, and `雪山法师` exist in the source monster table and now have guarded source-only PNG resources (`source_monster_20042`, `source_monster_20121`, `source_monster_30044`), but they are not present in the current stage-composition art mapping or runtime encounter mapping. Extracted stage sprites cover the currently bundled exact IDs for core stage and Boss encounters, while the remaining composition names use explicit type-near mappings to existing checked art instead of silently falling back to slime. Slime battle rendering now uses the clean transparent `official_monster_slime` instead of the old `monster_slime_red` full battle-screenshot crop, which contained background and adjacent-character fragments. Self-test, Swift Testing and release resource self-test all scan all 49 composition names; the release resource self-test also verifies each sampled battle monster sprite can be loaded from the package, rejects non-slime monsters that resolve to slime art, rejects stage battle mappings that use legacy full-screenshot UI crops such as `monster_slime_red`, validates the 3 source-only monster sprite resources by resource name and dimensions, and pins the three Act Bosses to `stage_monster_skeleton_king`, `stage_monster_sibuna`, and `stage_monster_voidcaller`. | Need exact per-name sprites for the approximate mappings, runtime/stage evidence for the 3 source-only monster names, exact animation frame sets, hit/attack/death frames, and stage-specific scale/spacing tuning. |
| Animation | `scripts/audit-steam-battle-scene.sh` extracts and probes the official `extras/battlescene` MP4/WebM from Steam. Current evidence: video-only, 776x180, 30 fps, about 6.133s, 184 frames. Sampled frames show a left-to-right strip with top-left `1-6` stage pill, grounded combatants, overhead HP bars, a centered warm lava/fire platform with dark side margins, dark upper negative space, visible flame/background movement, and no central `VS` label. The official frame `0` -> frame `8` motion sample covers about `0.267s` and currently measures `official_motion_pixels=26623`, including `official_platform_motion_pixels=11920` and `official_non_platform_motion_pixels=14703`, so the official promotional loop has measured frame-to-frame motion rather than only a static layout. | Battle view now uses a similar single-lane composition, hit-triggered nudge/flash effects, floating damage text, compact HP bars above combatants, a low-rate animated pixel flame band in the warm platform strip, and a self-tested enlarged macOS geometry (`BattleSceneMetrics`) that keeps the left-to-right combat lane readable while intentionally using more vertical display area than the official compact strip. Active `烈焰九头蛇`, `充能陷阱`, and `弩炮塔` states render as compact in-lane deployables. Damage logs drive compact trajectory and impact cues for projectile, range-field, summon, trap, charge, leap, meteor, ground-rupture, shockwave, elemental and chaos categories without changing combat mechanics. Non-damage logs drive separate heal, resurrection, shield and sacred-blade utility cues. `BattleSceneSnapshot` renders the real SwiftUI `BattleSceneView` to an audit-ready PNG; self-test checks the rendered PNG signature, dimensions, subtle-margin platform ratio, distinct damage-cue fixtures and distinct utility-cue fixtures, plus a separate `playerStatusRow` fixture for the real bottom status row. The shell visual audit also checks upper-left platform pill presence/position, overhead HP-bar presence, deployable trap marker, deterministic projectile/impact cues, movement cues, utility cues, incoming monster hit cues, and status-row non-dark/text/gold/teal/green pixels for modeled blessing/shield/trap feedback. | Need exact official keyframe/timing comparison beyond the broad frame `0` -> frame `8` motion sample, exact attack/idle/hit/death keyframes, exact summon/trap/projectile/original impact sprites, exact charge/leap/meteor/shockwave/earthquake/chaos keyframes and travel timing, exact heal/revive/shield/blade utility keyframes, exact original buff/status bar layout, icon art, duration/stacking presentation, exact explosive-bolt and meteor impact delay/placement, exact lightning arc geometry, exact ground-rock spawn/explosion visuals, exact projectile/trap trigger animation, exact sprite scale, x/y lane anchors, HP bar dimensions and stage pill offsets measured against stronger original captures before claiming parity. |
| Taskbar integration | Original is a Windows taskbar docked mini window. | macOS app is a menu-bar extra with popover. | Product translation is intentional, but should preserve tiny glanceable status and avoid oversized menu-bar icon. |

## Original Audio Baseline

| Area | Original Evidence | Current macOS State | Gap |
| --- | --- | --- | --- |
| Audio presence | Direct `ffprobe` check of Steam's official Trailer HLS found AAC 48 kHz stereo audio streams. Direct checks of the page's embedded extras videos found video-only streams for the 776x180 battle-scene MP4/WebM, 776x200 interface MP4, 780x468 cube MP4 and 780x62 wishlist MP4, so those clips do not prove battle-specific SFX by themselves. `docs/steam-audio-audit.md` classifies the current Trailer audio technically as a continuous mastered promo mix: 47s, -15.3 LUFS integrated loudness, 4.6 LU loudness range, near-full-scale peak, and no sustained silence at -45 dB for 0.2s. | Current implementation packages original generated pixel-style SFX for game events and keeps system sounds only as a fallback. `scripts/audit-local-sfx.sh` now measures the local SFX set against broad technical boundaries, statically checks that all local audio events are routed by gameplay/UI code, and when `dist/TBH.app` exists verifies packaged WAV payloads match source SFX resources, with current RMS range `-22.7..-11.2 dBFS`, peak range `-11.1..-6.5 dBFS`, coarse spectral-centroid range `553..3173 Hz`, and runtime minimum-interval range `0.18-0.50s`. | Trailer analysis can guide broad loudness/energy for substitutes, and the local SFX now avoid obvious technical failures such as clipping, silence, overlong events, duplicate payloads, major event-profile drift, missing runtime throttles, or stale packaged WAVs that would make release builds diverge from the checked source set. It still cannot prove original per-event attack, hit, loot, equip, item-consumption, victory or defeat sounds. Exact original mix and per-event sounds still need in-game capture or authorized assets. |
| Event coverage | Expected game moments: attacks, crits, taking damage, victory, loot, defeat, level-up, equipment actions, and item-consuming progression actions such as Cube infusion and Alchemy. | Added event-driven SFX for hero attack, crit, damage, victory, loot, defeat, level-up, equip, item consumption, and preview. Cube infusion and Alchemy now emit the item-consumed event when they successfully consume an item. The app prefers packaged, original generated, short pixel-style WAV files and falls back to macOS system sounds if a resource is missing. `ResourceSelfTest` derives the required resource list from `GameAudioEvent`, rejects extra unreferenced bundled WAVs, rejects exact duplicate WAV payload reuse across events, verifies representative `BattleEvent` routes through `GameEngine.audioEvent(for:)`, verifies each event WAV exists, is playable by `NSSound`, parses through `AVAudioFile`, is 16-bit PCM, is mono, uses 22050 Hz sample rate, stays within the same 0.05-0.75s duration bounds used by the local SFX audit, falls within the same broad RMS/peak dBFS bounds, validates each event's runtime playback volume against local category profiles, validates each event's minimum playback interval against the local category profiles, and gates basic local substitute relationships: critical hit longer and not materially weaker than basic attack, skill cast longer than basic attack, level-up longer and at least as peaky as preview, and item-consumed at least as long and at least as peaky as item-equipped. `scripts/audit-local-sfx.sh` derives the expected file list from `GameAudioEvent`, checks for extra unreferenced WAVs, reports duration/channel/rate/bit-depth/RMS/peak/onset/zero-crossing/coarse spectral-centroid/high-low ratios for every event, verifies every `GameAudioEvent` case is played by runtime code, verifies all 7 `BattleEvent` cases route through the shared `GameEngine.audioEvent(for:)` mapping to attack/crit, skill/crit, damaged, victory, or defeat SFX, guards broad event profiles such as brighter/noisier critical hits, slower-onset skill casts, lower-pitched defeat, longer level-up cues, and a more magical item-consumed cue than item-equipped, and parses `GameAudioEvent.minimumInterval` to ensure every event has runtime throttling in the expected category range. `TBH --self-test` now verifies the same battle-event mapping, local playback volume profiles and local minimum playback interval profiles in Swift, drives a real tick-based victory path with an injected recording audio client, and proves preview, equip, Cube infusion, Alchemy, chest opening, Synthesis, offline level-up, mute suppression, and re-enable behavior. The current files are 16-bit PCM mono WAVs at 22050 Hz, about `0.110-0.360s` long, with repeatable combat-event throttles at `0.18s`, inventory/preview throttles at `0.25s`, and victory/defeat/level-up throttles at `0.50s`. | The packaged WAV files are style-aligned substitutes, not original game audio assets. Exact original mix, loudness and per-event sounds still need in-game capture or authorized assets. |
| User controls | Original Steam page does not prove exact volume/mute controls from fetched evidence. | Added sound effects toggle and test button in Settings. | Volume slider can be added if direct evidence or product need justifies it. |

## Implemented In This Pass

- Added `GameAudio` service with event-driven macOS system SFX.
- Added `BattleEvent` emission for hero attack, crit, damage, victory and defeat.
- Added GameEngine audio routing for battle, loot, level-up and equip events, with battle-event routing centralized in `GameEngine.audioEvent(for:)`.
- Added a DEBUG-only `GameEngine.runSelfTestTick()` hook and `[GameEngine runtime]` self-test coverage for the real timer tick path: live battle update, combat SFX, victory SFX, mined XP/gold settlement, encounter progress, play-time/statistics update, retained-loot SFX consistency, and next-battle restart.
- Added `scripts/audit-steam-audio.sh` and `docs/steam-audio-audit.md` so official Steam Trailer audio checks are reproducible without storing official audio or derived clips in the repository.
- Added persisted `soundEffectsEnabled` setting with old-save default compatibility.
- Added Settings UI controls for `播放战斗音效` and `测试音效`.
- Updated self-test and Swift Testing coverage for sound setting persistence, runtime audio-event routing, preview/equip/Cube infusion/Alchemy/chest/Synthesis/level-up event emission, and mute/re-enable behavior.
- Changed offline reward cap from 24 hours to the original 8-hour cap and added test coverage.
- Added six original hero classes, class role/grade/base stat metadata, matching sprite mappings, class selection UI, old `战士` save compatibility, and six named active skills per class as a data baseline.
- Added original stage scaffold: 3 Acts, 10 stages per Act, Normal/Nightmare/Hell/Torment, X-10 Boss flags, current-stage UI, stage-based monster scaling, and stage-based offline reward estimation.
- Added Soul Stone gating for X-10 Boss stages: persisted stone counts by difficulty, Boss lock UI, and stone consumption only on successful Boss clear.
- Added persisted chest inventory, source-mapped stage chest rewards, manual chest opening, Soul Stone grants from opened chests, chest item drops, and Settings UI controls for chest counts/opening.
- Expanded `Rarity` from the original MVP's 4 tiers to the original 10-tier ladder: Common/Uncommon/Rare/Legendary/Immortal/Arcana/Beyond/Celestial/Divine/Cosmic, with Chinese display labels, colors, Alchemy value, Cube XP, D/E/I slot metadata, updated item detail UI, and higher-tier placeholder loot rolls.
- Added persisted item locking: Option-click in inventory toggles lock state, selected item details expose a lock/unlock command, locked items show a lock badge, and locked items cannot be discarded. Old saves decode item locks as disabled.
- Added a macOS menu-bar equivalent for the original scale-reset control: Settings now exposes panel scaling from `85%` to `125%` in deterministic `5%` steps plus `重置面板尺寸`, which returns the `MenuBarExtra.window` popover to the default `430x520` baseline and is bound to the checked original `Shift+F11` shortcut. This intentionally does not implement `Shift+F12` recenter, because the current macOS translation is system-anchored to the menu-bar status item and does not persist an independent window position.
- Extended release resource checks for app and menu-bar presentation assets: `ResourceSelfTest` now validates the required brand PNG dimensions, opaque coverage, color/saturation signal, `app_icon.png` dark backing ratio and `TBH.icns` header/size/chunk structure, while `scripts/audit-local-app-icons.sh` also keeps the menu-bar icon at the native `14` pt size and `18` pt row height and compares `dist/TBH.app` packaged icon payloads with source resources when a local app bundle exists. This guards the prior startup/menu-bar icon regressions and stale packaged icon resources without claiming exact original Windows taskbar icon parity.
- Added a persisted three-slot `HeroParty` scaffold with default Knight/Priest/Ranger candidates, party slot selection in the character panel, battle-scene party rendering, and real support damage/log/audio events from unlocked non-primary members. Old saves derive a party from the saved hero class.
- Added persisted `RuneTree` party-slot, active-skill-slot and first inventory-expansion nodes: new games start with one deployed slot, one active skill slot and base 50-slot backpack capacity; hero Level 3 gates the Settings Rune Tree route, checked Rune of Command costs (`50,000G` for slot 2 and `150,000G` for slot 3) unlock party slots 2 and 3 in order there, and approximate-cost `Rune of Awakening` unlocks the modeled second active skill slot without treating `~50,000g` as an exact verified cost. The first modeled `Rune of Expansion` maps to checked source row `22` / `MaxInventorySlot` and raises local backpack capacity by a conservative `+10` scaffold after the modeled party-slot prerequisite; exact original cost, per-node increment and stacking remain unverified. The character panel now also exposes direct support-slot unlock buttons that spend the same checked formation costs without requiring the player to open Settings first; directly unlocking position 3 from a one-slot party spends both missing formation costs and opens positions 2 and 3 together. Runtime `Battle` instances now receive `runeTree.activeSkillSlotCount`, so the main hero and support members no longer activate every locally modeled skill before the second slot is unlocked; the temporary loadout rule takes the first N checked skills by current local order. Locked party slots do not contribute support damage, and old party saves infer unlocked slots for compatibility. The settings panel now exposes a Rune Tree reset action for currently modeled nodes; it clears node state, re-locks party, active skill slot and inventory expansion state, refreshes active battle state, and refunds only checked local formation gold costs because the checked page proves reset support but not original reset price/refund/point semantics. The remaining runtime implementation of the full Rune Tree, non-formation rune costs/effects, exact active skill slot cost/equipment semantics, exact reset semantics and broader point/gold economy remain incomplete until verified against stronger original evidence.
- Added two checked `Rune of Opening` source effects to the runtime Rune Tree scaffold: `OpenOneTypeChestAllAtOnce` unlocks same-kind batch chest opening, and `OpenAllTypeChestAllAtOnce` unlocks all-kind one-click chest opening after the modeled same-kind prerequisite. The Settings chest panel now shows batch buttons only after the matching rune is unlocked; individual chest opening stays available. These nodes use source-published `16x16` Rune icons and keep costs/path semantics explicitly unverified instead of inventing gold prices or a complete original Rune branch.
- Added checked `Rune of the Mainspring` / `发条符文` runtime nodes for the three source auto-open chest families: `13002` / `UnlockAutoOpenNormalChest`, `15001` / `UnlockAutoOpenStageBossChest`, and `1902001` / `UnlockAutoOpenActBossChest`. Each node now starts a persisted cooldown instead of opening immediately on unlock, then opens only its matching chest family when the cooldown expires. The current runtime uses the checked cooldown table `300s` Normal Monster, `600s` Stage Boss, and `60s` Act Boss, plus checked Lubrication reductions `39s`, `75s`, and `6s` respectively. Full branch path, complete cost table, point economy and reset/refund behavior remain unverified, so these nodes still use unverified local cost labels.
- Added checked chest-capacity Rune source categories to the runtime scaffold: `1031` / `MaxAmountNormalChest`, `1071` / `MaxAmountStageBossChest`, and `1056` / `MaxAmountActBossChest`. Each currently raises only its matching local chest-family storage cap by a conservative `+1` and keeps cost/path/stacking/cap semantics explicitly unverified.
- Added `SourceRuneCatalog` as a data-only baseline for the full checked Rune Tree table: it preserves 197 source rows, 195 `Next` edges, 11 sparse `Previous` references, Chinese and English rune names, the checked max-level distribution (`1:62`, `2:1`, `3:43`, `5:89`, `10:2`), checked `Next` out-degree distribution (`0:79`, `1:63`, `2:35`, `3:18`, `4:2`), sparse previous links, next links and 39 icon families from the refreshed `/zh/runes/` and `/en/runes/` pages. Settings now exposes those 197 source rows in a folded source-data review table with source icons, source IDs, max level, connection counts, and a runtime/data-only marker, without turning unverified rows into executable effects. Self-test and the local gameplay audit now guard the source row count, connection count, next out-degree distribution, previous-reference count, exact sparse previous-reference map, max-level distribution, icon-family distribution, duplicate IDs, dangling next-node references, dangling previous-node references, the 197/197 Settings source-table coverage, and the mapping from currently executable runtime nodes back to source IDs. `scripts/audit-local-rune-icons.sh` also rechecks `dist/TBH.app` when present and compares all packaged `source_rune_*` payloads with source resources, guarding stale release bundles.
- Added Settings `本地符文成本复核` as a separate visibility surface for the current runtime Rune Tree cost boundary. It lists all 197 runtime Rune nodes with their source ID/icon, local display title, source names, cost text, and cost status. Current guarded counts are 2 verified exact Rune of Command costs totaling `200,000G`, 1 approximate Rune of Awakening cost totaling `50,000G`, 1 official branch-level approximate source row (`2nd Active Skill Slot (~50,000g)`), and 194 truly pending unknown costs. The same panel now groups those 194 pending nodes into 37 source icon/family buckets, including large unresolved families such as `MaxInventorySlot` and chest drop/storage branches, so follow-up source review can target the original branches with the biggest remaining price gaps. It also groups the 37 icon/family buckets into 7 gameplay branches: chest drop/storage/auto-open `82` pending nodes across `13` icon groups, backpack/storage `29/2`, combat gold/XP rewards `43/10`, all-hero combat stats `20/6`, Cube/Alchemy rewards `8/2`, offline rewards `11/3`, and stage pacing `1/1`. The same review now exposes 7 mutually exclusive branch-level evidence queues covering all 194 pending nodes, expands the 37 branch/icon groups into 37 per-group evidence rows, and adds a 5-bucket source `maxLevel` queue covering the same 194 pending costs (`1:59,2:1,3:43,5:89,10:2`) with 62 bucket-local icon groups. Each row shows the branch, source icon group or source max level, pending node count, sample source IDs, source names or icon samples, missing proof, required proof, and a boundary against deriving prices from icon groups, repeated-node counts or source max-level caps. It also exposes 6 Rune cost evidence gates before pending costs can enter runtime: per-node cost, branch/path cost, reset/refund economy, candidate-cost cross-source proof, currency/point type, and repeated-node stacking/caps. These groupings, queues and gates are only navigation aids over source icon families, source maxLevel values, sample source IDs and missing proof; they do not infer cost gradients, branch prices, path prices, per-level prices, reset prices, refunds, point economy, or runtime spend values. The review panel states that approximate costs do not participate in verified refunds, pending-cost nodes are not assigned fabricated gold costs, reset refund logic uses only checked exact costs, and the cost-gate checklist does not generate Rune prices, path costs, reset refunds or point economy. This is an audit surface over current local cost evidence, not proof of exact original Rune Tree cost tables, reset economy, point economy, or full per-node price parity.
- Added Settings `符文证据分层` to separate stronger cross-source facts from still-missing Rune Tree semantics. It records 2 checked Wiki locale pages plus 6 non-Wiki guide/community pages, exposes 9 evidence rows, upgrades the two formation costs to cross-source evidence, keeps the second active skill slot as mixed exact/approximate evidence, records the auto-open cooldown evidence used by runtime (`300s`/`600s` from guide/community evidence, `60s` Act Boss and detailed reduction rows from direct `tbh.city` rune pages), and now exposes both the complete `tbh.city` single-source `total_cost_to_max` candidate table (`197/197`, `10,040,515,050G`) and the 13-node auto-open subset totaling `383,790,000G` without promoting either into verified costs. The auto-open candidate costs are additionally split into 4 review-only buckets (`10k`, `200k`, `1M`, and `382.58M` Lubrication aggregate) so follow-up review can see the available single-source evidence while the UI still refuses to bind exact Rune IDs, generate per-node prices, calculate path costs, or enter runtime refund math from those values. The remaining 194 pending local costs, complete verified 197-node cost/path table, reset price/refund rules, point economy and complete branch path semantics remain unresolved. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` now guard this review surface so partial independent evidence cannot be mistaken for complete Rune Tree parity.
- The Rune Tree runtime coverage is now explicit rather than implied by the source catalog: only 15 of 197 checked source nodes are executable runtime nodes, 182 source nodes remain data-only, 14 of 39 source icon families are used by runtime nodes, and 25 source icon/effect families are currently unmodeled-only. `SelfTest`, `HeroTests`, `scripts/audit-local-rune-icons.sh`, and `scripts/audit-local-gameplay-fidelity.sh` all guard this split so source-data completeness cannot be mistaken for original Rune Tree behavior parity.
- Added the first non-formation Rune Tree nodes: `安息符文` / Rune of Repose now gates offline rewards, and its checked follow-up `储藏符文：离线金币 +10%` / `训练符文：离线经验 +10%` nodes independently raise offline gold or XP by 10%. The exact costs are not exposed by the checked evidence, so these nodes are shown as `成本待核对` and unlock without fabricated gold prices until stronger evidence is available.
- Added a folded Settings review table for the mined stage runtime database: it exposes all 120 checked difficulty-stage rows from `StageDefinition.runtimeData(for:)`, with source code, difficulty, display stage, level, wave/kill or Boss marker, gold/XP/HP values, and compact monster composition text. The local gameplay audit now requires the `SourceStageDatabaseView` path and reports `source_stage_review_rows 120/120`, so the checked stage table cannot silently disappear from the app UI. This is a review surface over the current mined rows, not proof of exact original wave timing, target priority, monster ATK/DEF, drops, stage-specific animation frames, or per-monster skills beyond the separately mapped source metadata.
- Added a Settings `原版怪物数据库` review table from the refreshed `taskbarhero.org` monster pages (`/zh/monsters/` and `/en/monsters/`). It preserves the 61 Wiki/datamined monster rows with source ID, Chinese/English name, HP, ATK, attack speed, gold, XP and best-farm text, including checked rows such as `10011` Slime, `20121` Tick, `30013` Fallen Angel's Helm, `30104` Chaos Hell Priest, and `30904` Archon Morkar. Runtime monster generation now resolves that source row for every checked stage-composition name, including duplicate Boss rows by stage code, uses source ATK as the local base attack, converts source Atk Spd into the current battle speed scalar with `round(Atk Spd * 10)`, and feeds each spawned monster's stored `critRate` into live incoming damage logs. The self-test and local gameplay audit now guard `61` rows, `61` unique IDs, one source-absent best-farm row (`Tick` / `扁虱` uses source value `—` on both `/en/monsters/` and `/zh/monsters/`), `49/49` coverage of the current stage-composition monster names, `3/3` source-only monster sprite resources, `3/3` Settings sprite preview rows, `3/3` source-only proof matrix rows, runtime ATK/speed wiring for representative leader, non-leader and Boss spawns, plus a deterministic `critRate=1.0` versus `critRate=0` monster-hit check. The same panel keeps `20042:剧毒领主`, `20121:扁虱`, and `30044:雪山法师` as source-only rows, renders their existing `source_monster_*` PNGs as review-only sprite previews, and now exposes both a proof matrix and 5 evidence gates that must be satisfied before those rows can enter runtime mapping: stage slot, battle art/anchor evidence, runtime encounter, attack/skill source, and animation/SFX. The proof matrix records positive evidence only as source row plus single sprite, while all 3 rows still show missing stage-composition proof, runtime encounter proof, skill ownership proof, original animation frames, and isolated original SFX. Its per-monster evidence queue parses best-farm stage codes only as review context and cross-checks them against the mined stage-composition table, so `4207` and `4303` are explicitly shown as not listing `剧毒领主` or `雪山法师` rather than being promoted to spawn slots, while `扁虱` is shown as a source-empty best-farm row rather than a local field to invent. The same queue now links the source-ID-prefix skill candidates `200421`, `201211`, and `300441` so follow-up review can target concrete source rows, while the UI and audit keep them out of runtime because ID prefix does not prove ownership, formula, delivery, frames, SFX or battle eligibility. This is deliberately separate from the 49-row battle-art mapping and does not claim source sprite sheets, keyframes, death frames, exact DEF/critical-damage formulas, full difficulty scaling or full monster skills are complete; the panel explicitly says source-only sprites do not draw or infer new runtime monster behavior.
- Settings now also exposes an `原版怪物美术映射` review panel for the 49 checked stage-composition monster names. It lists each source name, sampled stage row, runtime monster ID, current battle sprite, and whether the mapping uses an extracted stage sprite, a generic official sprite, or an explicit type-near approximation. The panel now separates source roster identity from runtime art coverage: the source monster table has 52 unique names and covers the Steam `50+` lower bound as `52/50+`, while the current battle-art mapping is `49/52` and the remaining source-only rows `剧毒领主 / 扁虱 / 雪山法师` now have guarded single-sprite resources but no current stage-composition or runtime encounter mapping. It also exposes 5 dedicated monster-art evidence gates before approximate art can be replaced or upgraded: full roster identity, per-monster dedicated sprite, animation frame sequence, scale/ground anchor, and provenance/hash audit. The same review now groups the current monster-art boundary into 4 evidence queues covering type-near approximations, generic official sprites, extracted stage sprites, and the 3 source-only sprite/runtime gaps; the queues cover the current 49 mappings plus the 3 source-only rows only as review order. Those gates and queues only define missing proof and explicitly do not generate monster images, action frames, scale values, SFX, skills/drops, stage slots, or a complete roster. `SelfTest`, `ResourceSelfTest`, and `scripts/audit-local-gameplay-fidelity.sh` guard the 49-row review surface, the 52/50+ roster-identity boundary, the 49/52 art-coverage boundary, the `source_monster_source_only_sprites=3/3` resources, the 5-gate art checklist, the 4-queue art review order, the non-slime no-fallback rule, the source-roster runtime gap, and the legacy full-screenshot crop rejection. This makes approximate monster art reuse and source-only sprites visible in-app without redrawing missing monster sprites or claiming that all original per-monster sprite sheets, keyframes, scales, attacks or death frames are complete.
- Expanded item taxonomy from five generic equipment slots to the 20 equipment types listed on the refreshed items page, grouped as 6 weapons, 6 offhands, 4 armor pieces, and 4 accessories. The runtime loadout now exposes 10 concrete slots, generated loot keeps its concrete type, auto-equip evaluates the expanded slots, item details show the type line, legacy `饰品` saves migrate into concrete accessory slots when item names allow it, and the generic fallback remains Ring only when the old name gives no stronger evidence.
- Replaced slot-level equipment icon fallback with source-backed gear art: `GameArt.itemIconName(for item:)` now prefers `Item.equipmentType + itemLevel`, maps concrete equipment to the closest checked 16x16 `source_gear_*` base progression icon from `source_gear_icons.tsv`, keeps all 20 modeled equipment types mapped to independent 16x16 `item_*` fallback icons from the corresponding gear pages, and keeps generic `official_item_*` art only as an unknown non-equipment fallback. Release resource self-test plus `scripts/audit-local-item-icons.sh` guard one-to-one fallback mapping, 396 source progression icons, source dimensions, pinned source URL/SHA-256/byte-count payloads, non-solid visible-pixel coverage, duplicate payloads, and the current local save's backpack/equipped item icon resolution when a save file exists. This is still a 396-base-progression icon baseline, not the exact 5,760-item icon database.
- Added `SourceItemCatalog` as a data-only baseline for checked item pages: it preserves all 20 equipment type pages, aggregate 5,760 gear-entry counts, 396 base level progression IDs/names/icons, per-type rarity distributions, 115 material rows across six material categories, and 59 stage chest rows. The resource bundle now includes the checked 396 base progression gear icons (`source_gear_*`, 16x16), 115 material icons (`source_material_*`, 16x16) and 3 stage-box family icons (`source_stage_chest_*`, 64x64), Settings renders saved chests with the source family box icon, and Soul Stone counters render the checked material icon for each difficulty. This intentionally stops at source-page aggregate/progression/material/chest data because the checked pages do not expose the complete per-rarity/per-affix 5,760-row item database, stat affixes, exact material effects, or exact chest contents/rates.
- Settings now also exposes an `原版物品数据库` source review panel instead of leaving item-page evidence only in tests and docs. The panel summarizes 20 checked equipment types, 5,760 aggregate gear entries, 396 checked base gear progressions, `0/5760` exact item records, 115 material rows, six material categories, 59 stage chest rows, and the three checked stage-box icon families, then folds out source-backed equipment/material/chest rows with their current `source_gear_*`, `source_material_*`, and `source_stage_chest_*` artwork paths. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard this panel and its exact-record warning, so future UI changes cannot silently hide the source-backed item/art review boundary or imply complete per-variant item parity.
- Settings now also exposes a narrower `精确装备记录缺口` panel. It separates the checked aggregate source counts from unavailable exact records: weapon `6` types / `1,752` aggregate entries / `120` base progressions, offhand `6` / `1,752` / `120`, armor `4` / `1,168` / `80`, and accessory `4` / `1,088` / `76`, with every category still marked as exact-record `0/N`. The panel explicitly states `未取得逐件词缀/稀有度记录`, `基础进度图标不等于完整物品变体`, and `属性 rolls/掉落权重待核对`, and now adds a largest-type priority queue: `16` weapon/offhand/armor source gear types cover `4,672` aggregate missing exact records at `292` missing rows each. This only ranks the next item-data audit target; the 396 source-backed icons remain a baseline rather than a claim of full 5,760-item parity.
- Connected checked base gear progression names to runtime loot and Synthesis preview: generated equipment now selects the closest source progression at or below its item level for that equipment type, uses the checked base name as the item name, and keeps the source gear ID in the item description; the Synthesis preview shows the same checked output base name/ID before consuming materials. This removes fabricated rarity-prefix names from newly generated gear while preserving the documented gap that per-rarity/per-affix 5,760-row names, icons, stat rolls and drop rates remain unavailable.
- Verified official Steam media streams: the main Trailer HLS includes AAC audio, while the embedded battle-scene MP4 is video-only at 776x180/30fps. Reworked the macOS battle view toward that official battle strip: single horizontal arena, top-left stage pill, party cluster on the left, enemy on the right, overhead HP bars, floating damage, and larger bundled battle/boss sprites.
- Added `scripts/audit-steam-battle-scene.sh` and `docs/steam-battle-scene-audit.md` so the official Steam `extras/battlescene` media can be re-probed for stream metadata, frame count and sampled timeline frames without storing official video frames in the repository.
- Extended the official Steam battle-scene audit with a temporary frame `0` -> frame `8` motion sample. The latest run measured `official_motion_pixels=26623`, `official_platform_motion_pixels=11920`, `official_non_platform_motion_pixels=14703`, and `official_motion_percent=0.1906`; `scripts/audit-local-gameplay-fidelity.sh` now reports `official_steam_battle_motion_guard=enabled` so the broad official motion evidence is kept in the local fidelity gate. This proves visible motion in the sampled official promotional loop, not exact original keyframe or timing parity.
- Aligned the local deterministic battle-scene motion audit with the same rounded `0.267s` interval used by the official frame `0` -> frame `8` sample. The latest local render reports `local_motion_sample_time_seconds=0.267`, `flame_motion_pixels=128697`, `combatant_motion_pixels=79385`, `player_combatant_motion_pixels=32794`, and `enemy_combatant_motion_pixels=37244`; this keeps local regression checks on the same sampling cadence while still avoiding any claim of exact original animation parity.
- Enlarged the macOS battle arena while preserving a horizontal left-to-right combat composition: `BattleSceneMetrics` keeps the official `776x180` source ratio as evidence, but the popover arena now uses a 2304 pt height and 936 pt expected content width for about 0.41:1 so combatants and effects get more display area while the menu-bar popover remains usable on ordinary screens. The battle log is now a compact scrollable panel below the scene, scene-level numeric HP labels remain removed in favor of compact overhead bars, and the lower ground uses a `0.90` width ratio with `0.14` height ratio so the enlarged macOS battle tab keeps subtle dark side margins without making the warm platform dominate the taller viewport. The bottom tab bar remains outside and below the content area.
- Strengthened the local battle-log regression guard for long fights: `SelfTest` now builds a default production-window fixture with 12 hero/support records followed by 120 monster records, then verifies the 50-row visible window still fills completely, retains the latest eight hero-side rows, keeps the newest monster-side row, scrolls back to the latest hero-side row when the tail is monster-only, and also checks the actual `BattleLogPanel` input shape where the scroll target is calculated from the already-retained visible window. The same fixture verifies that the fixed hero-side highlight strip survives long monster-only tails. `BattleView` now feeds the panel through a shared `BattleLogPresentation`, and `SelfTest` verifies that this presentation preserves the retained window, hero-focus rows, total count and visible scroll target. `scripts/audit-local-gameplay-fidelity.sh` now requires those self-test phrases and the shared presentation path, so the earlier "hero records disappear in the log" regression cannot be hidden by the shorter compact-window test or by UI code bypassing the tested helper. This is a macOS readability guard for the current log panel, not proof of exact original combat-log ordering, retention, or scrolling behavior.
- Added deterministic screenshot coverage for the real `BattleLogPanel` instead of only testing log-retention helpers. `BattleSceneSnapshot` now has a `battleLogPanel` fixture that renders the actual SwiftUI panel with a long-fight hero/support-plus-monster log presentation, `TBH --self-test` verifies the rendered panel PNG dimensions and non-trivial output, and `scripts/audit-local-battle-scene.sh` gates the panel with current metrics `battle_log_panel_non_dark_pixels=23028`, `battle_log_panel_light_pixels=6814`, `battle_log_panel_title_light_pixels=713`, `battle_log_panel_hero_blue_pixels=1225`, `battle_log_panel_support_purple_pixels=2007`, `battle_log_panel_monster_red_pixels=193`, and `battle_log_panel_critical_orange_pixels=375`. `scripts/audit-local-gameplay-fidelity.sh` now reports `battle_log_panel_snapshot_guard=enabled` and statically requires the real panel fixture, render call and pixel metrics. This catches blank, unreadable, title-regressed, hero/support-row-regressed, monster-row-regressed or critical-label-regressed log panels, but it still does not prove exact original log typography, order, timing, retention or scroll physics.
- Removed the battle strip's visible rounded card stroke so the combat scene no longer contradicts the official sampled frames' lack of a decorative card frame. `SelfTest` now guards that battle-scene corner radius and border line width stay at zero.
- Simplified the in-scene stage pill to show only the stage display code, matching the sampled official `1-6` pill. The richer wave and encounter counters remain in the surrounding macOS header instead of inside the battle strip.
- Added `scripts/audit-local-battle-scene.sh` for local rendered screenshot checks. It opens the TBH menu-bar popover, captures a temporary screenshot, rejects black/blank captures, detects the warm battle ground strip, estimates the rendered strip ratio, verifies the local subtle-side-margin platform width, and also supports `SCREENSHOT_PATH=...` for analyzing saved local screenshots when live `screencapture` is unavailable. Current deterministic analysis uses a static opening battle fixture with a fixed `寒霜弩箭` damage log and checks the enlarged `1872x4608` render for the `90%` warm ground platform, about `0.41:1` local scene ratio, deterministic flame and combatant motion, upper-left stage pill, overhead HP bars, deployable markers, cold impact pixels, and projectile trajectory pixels.
- Added `TBH --render-battle-scene <path>` for deterministic SwiftUI battle-strip rendering. `scripts/audit-local-battle-scene.sh` now supports `RENDER_BATTLE_SCENE=1` and automatically uses the snapshot renderer when no running TBH process is available, while `SelfTest` guards that the renderer produces an audit-ready `1872x4608` PNG.
- Corrected battle-scene hero art mapping so all deployed hero combatants use dedicated edge-transparent compact `battle_hero_*` figures. All six battle hero files now preserve the corresponding complete `official_hero_*` figure as an identity-safe fallback, replacing the deterministic gray combat-lane template and later partial screenshot/portrait trims that could read as the wrong hero in the battle tab. The battle party layout places the primary hero on the left front lane with support heroes to the right so the first visible hero is no longer a support class. `SelfTest` and `ResourceSelfTest` guard that battle hero sprites resolve to dedicated transparent battle art, the release resource self-test rejects swapped class mappings, duplicate class sprites, oversized battle hero crops, opaque corners/edges, blank transparent sprites, suspiciously full screenshot blocks, embedded HP-bar green runs, registered-size drift, missing Knight color markers, fragmented Knight subjects and undocumented visible/alpha drift from the `official_hero_*` connected-frame-removal source, `scripts/audit-local-hero-sprites.sh` now checks packaged app hero-sprite payload parity when `dist/TBH.app` exists, and the local battle-scene audit now rejects the pre-fix party layout where the support/portrait cluster sits too close to or ahead of the primary hero lane.
- Added compact player-side battle status badges in the Battle Tab bottom status row for the active state scaffolds that already exist in `Battle`: `神盾领域`, `神圣之刃`, `天堂之怒`, `烈焰九头蛇`, `暴风雪`, `迅捷觉醒`, `圣域`, `将军怒吼`, `快速装填`, `充能陷阱`, `弩炮塔`, `电击弩箭电流`, `旋转斧` and `嗜血`. The same status row now also exposes source-checked `CONTINUOUS` Priest blessings as `力量祝福` / `守护祝福` badges when those modifiers are equipped by the main hero or an unlocked support Priest. The badges show live shield and trap counters where those values exist, hide depleted shields and spent traps, keep source blessings first, and share a self-tested first-five plus `+N` folding rule with the SwiftUI row. This is visible feedback for current modeled active buffs, continuous blessings, summons and traps, not a claim that the original game's buff bar layout, icon art, exact duration or stack rules are fully replicated.
- Added deterministic visual auditing for the player-side battle status row. `BattleSceneSnapshot` now supports a `playerStatusRow` fixture that renders the actual `BattleOngoingStatusView` with Priest `力量祝福` / `守护祝福` continuous badges plus live `神盾领域` and `充能陷阱` counters, plus a `playerStatusRowCrowded` fixture that activates all currently modeled player-side statuses through the DEBUG battle buff path. `TBH --self-test` now guards the normal fixture's exact badge count/order (`力量`, `守护`, `神盾`, `陷阱`), verifies crowded-row folding, and confirms both status-row fixtures render distinct PNGs. `scripts/audit-local-battle-scene.sh` gates the normal row with current metrics `status_row_non_dark_pixels=9212`, `status_row_gold_pixels=160`, `status_row_teal_pixels=701`, `status_row_green_pixels=474`, and `status_row_light_pixels=1020`; it also gates the crowded row with `crowded_status_row_non_dark_pixels=12476`, `crowded_status_row_light_pixels=990`, and `crowded_status_row_overflow_light_pixels=90`, so the local checks catch blank, unreadable, color-regressed, order-regressed, or folded-overflow-regressed status feedback in addition to the static badge mapping checks.
- Added compact in-lane deployable visuals for active `烈焰九头蛇`, unspent `充能陷阱`, and active `弩炮塔` states. The deterministic battle-scene snapshot now activates those deployables through a DEBUG fixture while keeping the primary Knight party layout intact, and `scripts/audit-local-battle-scene.sh` rejects renders where the trap marker disappears from the expected mid-lane region. These are intentionally pixel-style placeholders until exact original summon/trap sprites, placement, projectile timing and trigger animations are captured.
- Added compact in-scene skill trajectory and impact cues driven by existing `BattleLogEntry.damageElement`, `delivery` metadata and source-checked skill identity. Current trajectory cues cover generic projectile, range-field, summon-projectile and trap-arc deliveries, plus dedicated Ranger arrow trails for `快速射击` multi-arrow volley, `散弹射击` tracking volley, `箭雨` falling arrows, `穿透之箭` piercing shot and `穿刺射击` lodged-arrow hit. Hunter bolt skills now also get source-text-aligned cues: `爆炸弩箭` renders an explosive bolt trail and impact, `寒霜弩箭` renders a frost bolt trail and cold explosion, `电击弩箭` renders a shock bolt trail/impact, and the derived `电击弩箭电流` log renders a current-arc trajectory plus current impact. They also cover dedicated charge-dash and leap-arc movement trails for `盾牌冲锋` and `猛击跳跃`, a dedicated spin arc for `旋转斧`, a bleed-rend trail for `旋转斧流血追击`, a dedicated falling-meteor trail for `陨石打击`, a ground-rupture trail for `大地强击`, a rock-burst trail for `大地强击岩石爆炸`, and a shockwave-ring trail only for the derived `粉碎强击冲击波` log. Ordinary melee damage such as `穿透突刺` and the primary `粉碎强击` hit still do not render movement/shockwave trails. Current impact cues cover physical slash, axe-spin impact, bleed-rend impact, shockwave impact, earthquake impact, earthquake rock explosion, fire burst, explosive-bolt impact, meteor impact, cold burst, frost-bolt impact, lightning spark, shock-bolt impact, shock-current impact, chaos burst, trap burst and summon-projectile hits; ordinary fire skills such as `火球术` still use the generic fire burst while `陨石打击` uses the heavier meteor impact. Non-damage logs now use separate utility cues instead of pretending to be impact/trajectory damage: `治愈` shows a heal pulse, `圣域` shows a sanctuary pulse, `复活` and `不屈意志` show a rising revive cue, `神盾领域` shows a shield field, `天堂之怒` shows a lightning utility cue, `神圣之刃` shows a sacred-blade glow, `迅捷觉醒` shows a blue haste streak, `快速装填` shows a green reload-haste cue, `将军怒吼` shows a roar cue, `嗜血` shows a blood surge, and other modeled buff logs get a compact aura. The deterministic battle-scene snapshot appends a fixed `寒霜弩箭` damage log, and `scripts/audit-local-battle-scene.sh` now rejects renders where the cold projectile trajectory, cold impact cue, rapid-volley trail, scatter-shot trail, arrow-rain trail, piercing-arrow trail, lodged-arrow trail, axe-spin arc, bleed-rend trail, shock-bolt lodged hit, shield-charge dash trajectory, slam-jump leap trajectory, rock-explosion cue, chaos impact cue, Priest sanctuary/wrath utility cues, attack-speed utility cues or Slayer utility cues disappear from the battle lane. The latest deterministic audit measured `rapid_volley_pixels=13326`, `scatter_shot_pixels=13459`, `arrow_rain_pixels=12734`, `piercing_arrow_pixels=13129`, `skewer_shot_pixels=2766`, `damage_axe_spin_pixels=1311`, `damage_axe_spin_bleed_pixels=2017`, `damage_shock_bolt_pixels=4751`, `damage_shield_charge_pixels=12788`, `damage_slam_jump_pixels=3147`, `damage_rock_explosion_pixels=2794`, `damage_chaos_pixels=5444`, `monster_chaos_incoming_pixels=1437`, `utility_sanctuary_pixels=2296`, `utility_wrath_of_heaven_pixels=1944`, `utility_swift_surge_pixels=3416`, `utility_quick_loader_pixels=824`, `utility_generals_cry_pixels=618`, and `utility_bloodlust_pixels=1409`. These are visual feedback placeholders until exact original projectile/impact/movement/utility/chaos sprites, spin/bleed-rend keyframes, travel timing, arrow/bolt counts, explosion radii, current arcs, charge/leap keyframes, heal/sanctuary/wrath/revive/haste/shout/bloodlust keyframes and hit-frame captures are available.
- Expanded deterministic battle-scene fixture coverage for the existing compact cues: `BattleSceneSnapshot` can now render `rapidVolley`, `scatterShot`, `arrowRain`, `piercingArrow`, `skewerShot`, `trapBurst`, `summonProjectile`, `shockBolt`, `shockCurrent`, `shieldCharge`, `slamJump`, `axeSpin`, `axeSpinBleedFollowUp`, `earthquakeImpact`, `earthquakeRockExplosion`, `shockwaveImpact`, and `chaosBurst` fixtures in addition to the earlier frost/explosive/meteor/lightning and utility fixtures. The base and motion snapshots now explicitly request the `frostBolt` fixture, so the shell script's fixture list covers every `BattleSceneSnapshot.Fixture` case. `scripts/audit-local-battle-scene.sh` now measures and gates `rapid_volley_pixels=13326`, `scatter_shot_pixels=13459`, `arrow_rain_pixels=12734`, `piercing_arrow_pixels=13129`, `skewer_shot_pixels=2766`, `damage_axe_spin_pixels=1311`, `damage_axe_spin_bleed_pixels=2017`, `damage_trap_teal_pixels=2956`, `damage_summon_fire_pixels=3098`, `damage_shock_bolt_pixels=4751`, `damage_shock_current_pixels=4082`, `damage_shield_charge_pixels=12788`, `damage_slam_jump_pixels=3147`, `damage_earthquake_pixels=3316`, `damage_rock_explosion_pixels=2794`, `damage_shockwave_pixels=13195`, `damage_chaos_pixels=5444`, and `monster_chaos_incoming_pixels=1437` in the current deterministic render. This makes local regression checks cover the visible `快速射击`, `散弹射击`, `箭雨`, `穿透之箭`, `穿刺射击`, `旋转斧`, `旋转斧流血追击`, `充能陷阱爆炸`, `弩炮塔`, `电击弩箭`, `电击弩箭电流`, `盾牌冲锋`, `猛击跳跃`, `大地强击`, `大地强击岩石爆炸`, derived `粉碎强击冲击波`, and generic source-backed chaos-damage cues, while preserving the existing limitation that exact original sprite art, projectile timing, arrow/bolt count/spacing, spin/bleed-rend timing, charge/leap timing, exact ground-rock entities, shockwave/chaos radius/keyframes and hit-frame placement remain unverified.
- Added a low-rate animated pixel flame band to the battle scene backdrop so the macOS strip reflects the official battle media's visible fire/background movement without claiming exact frame-by-frame parity; the current enlarged combat viewport uses 2304 pt scene geometry while combatant scale is capped independently to avoid crop regressions.
- Extended `ResourceSelfTest` from static monster file presence checks to a runtime stage-art scan: it walks every stage, difficulty and encounter through `StageDefinition.spawnMonster`, verifies all 49 mined composition names are sampled, ensures every resulting battle monster sprite loads from packaged resources, rejects non-slime monsters that fall back to slime art, rejects legacy full-screenshot UI crops such as `monster_slime_red` in stage battle mappings, and guards the three Act Boss sprite mappings.
- Added the first skill execution layer: main-hero skills now maintain cooldown timers inside `Battle`, damage skills emit named skill damage logs/events, Priest healing-style utility skills restore HP, non-damage utility skills log buffs, battle floating text/log rows show skill names, and `GameAudio` has a separate skill-cast event. Runtime skill execution is now limited by the modeled active skill slot count, with full-slot `Battle` construction reserved for isolated local skill formula tests. This is explicitly a scaffold, not the exact original 106-skill ruleset or final skill equip UI.
- Added `SourceSkillCatalog` as a data-only catalog for all 106 active/base/monster skill rows exposed by the checked `/en/skills/` and `/zh/skills/` pages. Settings now exposes those 106 rows in a folded source-data review table with source ID, English name, activation type, damage type, delivery, range, optional checked `sourceValue`, and runtime/data-only marker. A separate Settings `原版技能 damage 分布` panel now makes the full source damage distribution visible: `Physical 77`, `Fire 12`, `Chaos 8`, `Cold 5`, and `Lightning 4`, with `15` non-physical rows currently runtime-mapped and the `Chaos 1/8` runtime gap explicit. A separate Settings `原版技能 activation × damage` panel now cross-tabs source activation and damage fields: `14` source combinations, `13` currently runtime-mapped combinations, `BASEATTACK 10/58`, largest pending pair `BASEATTACK/Physical 37`, and `COOLDOWN/Chaos 0/3` runtime-mapped rows with pending IDs `309021`, `309041`, and `309051`. A separate Settings `原版技能 activation × delivery` panel now cross-tabs trigger type and delivery/shape fields: `16` source combinations, `15` currently runtime-mapped combinations, empty delivery runtime `11/71`, largest pending pair `BASEATTACK/空 delivery 48`, and `BASEATTACK_COUNT/空 delivery 0/2`; this remains source-field review only, not proof of exact trigger cadence, cast frames, projectile/range geometry or animation timing. A separate Settings `原版技能 damage × delivery` panel now cross-tabs damage and delivery source fields for art/effect review: `20` source combinations, `20` combinations with at least one current runtime mapping, empty delivery `11/71`, `Physical/空 delivery 7/53`, largest pending pair `Physical/空 delivery 46`, and `Chaos/空 delivery 1/8`. A separate Settings `原版技能 delivery 分布` panel now makes the full source delivery distribution visible: empty delivery `71`, `AOE 10`, `Projectile 10`, `Melee 6`, `Melee, AOE 4`, `Projectile, AOE 2`, `Projectile, Summon 2`, and `Trap 1`, with all `35/35` non-empty delivery rows currently runtime-mapped and the `71` empty delivery rows still explicitly bounded. Settings now also exposes `原版技能 range 分布` for all 106 source rows: 24 buckets spanning `120-1650`, largest bucket `150 x16`, runtime-mapped rows `46/106`, and a guarded source-order table for each range bucket. The panels and audit keep the boundary that damage, activation x damage, activation x delivery, damage x delivery, delivery, range and sourceValue are source fields only and do not imply exact trigger cadence, skill ownership, elemental resistance/status rules, projectile sprites, hit geometry, screen-pixel scale, VFX/SFX, cast frames, animation timing or complete runtime semantics. The local gameplay audit now reports `source_skill_catalog_entries 106/106`, `source_skill_review_rows 106/106`, source damage buckets, source activation-damage cross-tab buckets, source activation-delivery cross-tab buckets, source damage-delivery cross-tab buckets, source delivery buckets, source range buckets, and `source_skill_cooldown_chaos_value_rows 3` separately from `runtime_active_skills 36/106`, so source-data coverage and UI review coverage can improve without falsely claiming that base attacks and monster skills execute with original semantics.
- Added Settings `本地技能运行时覆盖` as a separate runtime coverage review beside the 106-row source skill table. It shows `106` checked source skill rows, `46` currently runtime-modeled source rows, and `60` pending source rows; it also breaks the modeled set into `36` named hero active skills, `6` source-backed hero base attacks, and `4` checked monster attacks. The activation split is guarded as `BASEATTACK 10/58`, `BASEATTACK_COUNT 9/11`, `COOLDOWN 25/35`, and `CONTINUOUS 2/2`, with explicit text that a complete source catalog is not a complete runtime implementation, pending skills do not receive fabricated combat semantics, and the full monster skill table/cast frames remain unverified.
- Added Settings `待接入源技能复核` as a narrower pending-source review beside the runtime coverage panel. It keeps the remaining `60` source rows visible without implementing them: `BASEATTACK 48`, `BASEATTACK_COUNT 2`, `COOLDOWN 10`, all `60` currently have empty source `delivery`, damage buckets are `Physical 46`, `Fire 6`, `Cold 1`, and `Chaos 7`, and source ID prefix buckets are `1:16`, `2:21`, `3:23`. It exposes the complete 14-row pending elemental manifest, the complete 12-row pending triggered/cooldown source-ID manifest, and the checked triggered/cooldown value/range map so follow-up runtime work can target exact source rows instead of a loose count. The current value-readiness boundary is now `15 value / 15 未命名 / 15 空形态`: the 12 triggered/cooldown value pages plus the three same-prefix unmapped-monster candidates `200421`, `201211`, and `300441`, whose direct pages add only `value 1000` plus range/page-field proof. The value detail review now covers all 15 value-checked pages (`109021`, `109031`, `109041`, `109051`, `200421`, `201211`, `209021`, `209031`, `209041`, `209051`, `300441`, `309021`, `309031`, `309041`, `309051`) and expands them into 15 per-skill value/range evidence rows. The same review groups those rows into 8 source-value evidence queues with distribution `800:1,1000:3,1350:1,1500:5,1700:1,1800:1,2000:1,2300:2`; this queue is only a review order over source-page numeric fields and does not infer multipliers, damage, target rules, duration, projectile shape, action frames or SFX from `value`. The remaining catalog-only `BASEATTACK` evidence rows are now `45`, split as `9` non-Physical rows and `36` Physical rows. Every fetched value page still resolves to `Skill ID`, `当前数据快照没有该语言说明`, empty/`—` hit type, source range and source value only. The mutually exclusive maturity split is now `45` catalog-only rows, `15` value/range-only rows, and `0` minimum-evidence rows. The runtime gates remain localized identity, ownership/targeting, delivery/hit shape, formula/scaling, trigger cadence, animation/VFX and audio/SFX; those rows only define missing proof and explicitly do not generate skill effects, formulas, projectiles, action frames or sound effects. The visual-priority review remains overlapping by design: `22` queue entries map to `16/60` unique pending skills, with `6` overlaps and `44` pending rows outside the high-priority queue. That `44`-row diff remains explicit low-priority backlog coverage: `8` Physical value/range rows plus `36` Physical `BASEATTACK` catalog rows, all still with empty delivery. The combined high-priority plus backlog visual-review route remains guarded as `6` queues and `60/60` pending skills, with the boundary that source value, range, delivery, ID prefix, queue membership and visual priority do not fabricate base attacks, monster skills, projectiles, formulas, range geometry, VFX/SFX or cast-frame semantics.
- The same pending-source Settings review still exposes the complete 48-row pending `BASEATTACK` candidate manifest grouped by source ID prefix (`12` rows under prefix `1`, `17` under prefix `2`, and `19` under prefix `3`). Three same-prefix unmapped-monster candidates now have checked value/range pages, so the remaining catalog-only per-skill evidence view is `45` rows split as `9` non-Physical catalog rows plus `36` Physical catalog rows. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard the exact first/last IDs for each prefix group, the aggregate `pending_source_skill_base_attack_candidate_manifest 48`, `pending_source_skill_base_attack_evidence_rows 45`, `pending_source_skill_nonphysical_baseattack_evidence_rows 9`, and `pending_source_skill_physical_baseattack_evidence_rows 36`, keeping these data-only and value/range-only base attack candidates traceable without promoting them into runtime monster/base-attack behavior.
- Added a Settings `本地主动技能数值表` panel beside the 106-row source skill table. It derives rows directly from `HeroSkills.named(for:)`, exposes all 36 currently runtime-modeled named hero active skills, shows the source-backed skill ID, class, activation, element, delivery, cooldown, Lv1/Lv10 values, and the full ten-entry local value table for each row. `SelfTest` now guards the 36/36 row count, six rows per hero class, 36/36 source-ID backing, complete 10-level tables, and representative source-backed values such as `10101` 2500->4300, `20101` 1320->2400, `50101` 4840->9430, and `60201` 6200->9800. The panel text and local gameplay audit explicitly keep the boundary `仅展示当前运行时已建模的 36 个命名主动技能；其余源技能/基础攻击/怪物技能完整运行时语义待核对`, so it does not claim complete 106-source-row runtime parity.
- Added a Settings `原版复核边界` summary above the source tables. It shows the current runtime/source boundary as `46/106` source skills wired into runtime, `60` pending source skills, `197/197` Rune source rows preserved locally, `0/5760` exact per-rarity/per-affix item records, and `396/396` checked base gear progression icons. The same panel calls out that only two Rune gold costs are verified and 195 are not fully verified, including one approximate value, has a dedicated `技能特效边界` row that states the current skill VFX/SFX are local auditable substitutes while original per-skill action frames, hit presentation, trigger timing and original audio remain unverified, and now adds a `被动技能边界` row exposing `108/108` passive source rows plus `104/108` source-backed passive icons while keeping the missing `IncreaseProjectileDamage` / `SkillHealIncrease` icon families and original passive unlock paths/runtime semantics explicit. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard this UI boundary so source-data coverage cannot be mistaken for complete original runtime fidelity.
- Extended the Settings `原版合成/Cube/炼金规则` review so it no longer only says that local Rune multipliers exist. It now exposes the local checked reward-rune scaffold as `4/4` Cube XP / Alchemy Gold rows, `+10%` per node, and `+40%` current single-side max, while explicitly keeping Rune costs, Cube level rewards and the original alchemy economy curve unverified. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard the row so the local reward-multiplier scaffold cannot be mistaken for full original Cube/Alchemy economy parity.
- Added a Settings `支援成员公式复核` panel beside the existing fidelity boundary panels. It exposes the four current local support formula rows: attack uses `(base ATK + main-hero level bonus + all-hero attack) * all-hero attack multiplier * 35%`, HP uses `base HP + main-hero level * 10`, armor uses `base DEF + main-hero level + all-hero armor`, and speed uses `base SPD + all-hero move speed`. The panel, `SelfTest`, and `scripts/audit-local-gameplay-fidelity.sh` deliberately mark this as a current runtime scaffold: independent support levels, support equipment, support skill-level scaling, original growth curves and exact class/support formulas remain unverified.
- Connected the six checked hero `BASEATTACK` source rows (`10001` through `60001`) to battle-log element/delivery metadata for main-hero and support-member basic attacks. This lets visual cues distinguish, for example, Sorcerer fire projectiles and Ranger/Hunter physical projectiles instead of rendering all unnamed basic attacks as generic damage. Exact original base-attack formulas, cadence, projectile sprites, travel timing, hit keyframes, broader monster base attacks and isolated SFX remain unverified.
- Added a narrow source-backed monster attack metadata path for the four stage composition names that explicitly encode the source skill element quartet: `燃烧的地狱祭司` -> `301015` / fire, `冰冻的地狱祭司` -> `301025` / cold, `电流的地狱祭司` -> `301035` / lightning, and `混沌的地狱祭司` -> `301045` / chaos. `Monster` now carries an optional source skill ID, stage spawning preserves it for those four names, and monster attacks pass the derived element/delivery into battle logs, elemental dodge and incoming-damage resistance. Monster attack log entries also preserve the actual stage monster name, so multi-enemy logs no longer collapse every incoming hit to a generic `怪物` label when a current-wave source name is known. The battle log now renders the existing damage element metadata as compact labels (`物理`, `火`, `冰`, `电`, `混沌`) even when the hit has no named skill, so source-backed base attacks and monster hits remain readable in the scrolling log. This deliberately keeps unmapped monster skills generic and keeps chaos outside the fire/cold/lightning elemental-resistance path until stronger source evidence proves broader monster-skill semantics.
- Settings now also exposes an `原版怪物攻击映射` review panel for those four checked elemental priest base-attack rows. The panel derives each row from `SourceSkillCatalog.runtimeMonsterAttackMappings`, shows the source skill ID, source damage type, `BASEATTACK`, range `800`, runtime element label, and the explicit empty-delivery boundary `来源 delivery 为空；运行时不伪造投射物/范围形态`. It now also exposes 5 monster-attack evidence gates before expanding the monster skill system: monster skill roster, delivery/hit shape, trigger cadence/cooldown, target/formula, and animation/SFX. Those gates only define missing proof and explicitly do not generate monster skills, projectiles, formulas, action frames or sound effects. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard the 4/4 review surface, the 5-gate attack checklist, and the `完整怪物技能表/投射物/施法帧待核对` boundary, so this visible review surface cannot be mistaken for a complete monster skill system.
- The visible victory reward banner now shows the first dropped item with the same source-backed icon resolver used by inventory/equipment UI (`GameArt.itemIconName(for:)`), preserving the compact text summary for extra drops such as `+1`. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard the `source_gear_*` icon path through a checked `Scepter` fixture. This improves local reward feedback without claiming complete original drop-table parity, exact reward animation parity, or the full original 5,760-item variant database.
- Added a deterministic inventory-panel render fixture to the local screenshot audit. It drives the real `InventoryView` with source-backed gear, selected comparable equipment, Synthesis-ready inputs, a locked item, Cube progress, the direct expansion affordance and the worse-equipment handling picker, then measures control text, item-grid color, selected detail, comparison delta and rarity pixels. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` guard the fixture and its audit wiring so item comparison and source-backed inventory art cannot silently regress to a blank/text-only panel without local evidence.
- Added a separate player-side incoming-hit visual cue for monster damage logs. It is intentionally distinct from `BattleImpactCue`, so monster attacks still do not reuse player skill impact/trajectory/utility cues; source-mapped fire/cold/lightning/chaos monster hits instead render compact incoming colored strikes over the party side of the battle strip. The deterministic battle-scene audit now renders `monsterFireIncoming`, `monsterColdIncoming`, `monsterLightningIncoming` and `monsterChaosIncoming`, and gates the cues with `monster_fire_incoming_pixels=1590`, `monster_cold_incoming_pixels=1497`, `monster_lightning_incoming_pixels=2992`, and `monster_chaos_incoming_pixels=1437`. This is a local visual-feedback scaffold for the newly source-tagged monster elements, not evidence of exact original monster projectile sprites, travel timing, hit keyframes, or per-monster animation parity.
- Replaced the invalid `ss_03` background crops previously named `skill_*` with 12 real `ss_06` Rune Tree node icons and wired the character panel's skill list to show category icons through `GameArt.skillIconName(for:)`. These are source-cropped visual category icons for delivery/element types, not a claim that all 36 modeled active skills now have exact original per-skill icons.
- Added current source-backed passive skill icons: the app bundles the 27 exposed `Passive_*.png` icon families from the checked skills pages as `source_passive_*`, maps the 104 passive rows with exposed images to those assets, renders them in the character panel's passive-skill list, and leaves `IncreaseProjectileDamage` / `SkillHealIncrease` without substitute art because the current source pages expose no icon for those stat families. `ResourceSelfTest`, `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-passive-skill-icons.sh` guard the 104/108 mapping, 27 icon-family count, missing-stat list, mixed source dimensions, alpha coverage and unexpected duplicate payloads; the shell audit now also checks packaged app `source_passive_*` payload parity when `dist/TBH.app` exists and can optionally re-fetch the live source pages with `AUDIT_REMOTE_PASSIVE_SKILL_SOURCE=1`.
- Added a Settings `原版被动技能数据库` review panel beside the active-skill source table. It summarizes 108 checked passive rows, 30 source stat families, current source-icon coverage `104/108`, 27 packaged `source_passive_*` icon families, and the explicit missing-source-icon stats `IncreaseProjectileDamage` / `SkillHealIncrease`; the folded row table shows each passive ID, class, stat, value type, value and icon/source-icon status. `SelfTest` and `scripts/audit-local-gameplay-fidelity.sh` now guard this review panel so passive source coverage, source-art coverage and missing-icon boundaries stay visible without implying exact original passive unlock paths or full original runtime semantics.
- Added a conservative runtime hook for the checked `SkillRangeExpansion` passive stat. It currently expands focused main-hero damaging skills by one additional live enemy per checked 30% range-expansion step; `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` now verify that Knight `报应打击` stays focused without the passive and reaches an extra live enemy with passive `101081`. This is a local range-target scaffold, not proof of exact original projectile width, melee arc geometry, field radius, or per-skill range math.
- Added runtime regression coverage for core offensive passive stats reaching live base attacks. Knight `AttackDamage` passives `101001` + `101072` now have `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving they increase cumulative base-attack damage in a fixed no-auto-skill battle window, while Knight `AttackSpeed` passive `101061` proves a higher base-attack log count over the same window. The test deliberately equips `不屈意志` (`10601`) as the single active slot so automatic damage skills do not pollute the base-attack log sample. This verifies current base-attack damage/cadence hooks only; exact original attack interval rounding, animation lock, projectile travel, hit-frame timing and all class-specific base-attack formulas remain unverified.
- Added runtime regression coverage for checked defensive passive stats reaching live monster hits. Knight `DamageReduction` passive `101071`, Knight `AllElementalResistance` passive `101062` against source fire monster attack `301015`, and Priest `DamageAbsorption` passives `401012` + `401032` now have `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving lower cumulative monster-hit damage over fixed high-HP battle windows. This verifies the current incoming-damage hook reaches actual monster attack logs only; exact original armor ordering, block/dodge animation timing, elemental resistance/status ordering, shield interaction, caps, support targeting and damage variance remain unverified.
- Added runtime regression coverage for monster critical hits reaching live incoming logs. `Battle` already stores per-monster `critRate`; `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` now guard that a deterministic `critRate=1.0` monster hit is logged as critical, a matching `critRate=0` monster hit is not critical, and the critical sample is higher under the current local `1.5x` monster-crit scaffold. This verifies the stored-rate hook only; exact original monster critical-damage coefficient, DEF ordering, variance source and any per-monster critical modifiers remain unverified.
- Localized the in-scene floating critical-damage text from the earlier English `CRIT` stand-in to `暴击`, so monster and hero critical hits now use the same Chinese combat feedback language as the battle log. The same helper now keeps non-damage battle-lane feedback explicit: fallback healing renders as `治疗 +N`, named buffs render their skill name, and avoidance events render as stronger short cues `闪避!` / `格挡!`. The floating text style now has a guarded presentation helper: critical hits use a larger lifted label with stronger border/shadow emphasis, while dodge/block labels get a heavier bordered utility style so they read as battle events instead of ordinary small damage ticks. The deterministic render audit now includes `criticalFloating`, `dodgeFloating` and `blockFloating` fixtures; the latest local screenshot audit measured `critical_floating_pixels=748`, `dodge_floating_pixels=241` and `block_floating_pixels=343`, so local checks fail if those top-lane feedback labels disappear from the actual SwiftUI battle render. The detailed battle log now uses a shared `BattleLogActionText` helper for deterministic Chinese action rows and disambiguates monster incoming avoidance as `攻击被闪避` / `攻击被格挡`, instead of wording those rows as if the monster had dodged or blocked. `SelfTest`, `CombatStatsTests`, `scripts/audit-local-gameplay-fidelity.sh`, and `scripts/audit-local-battle-scene.sh` guard the shared `BattleFloatingDamageText` helper, the floating style helper, the screenshot fixtures and the log action helper for plain critical hits, skill critical hits, ordinary damage, healing, buff, dodge, block and critical labels. This is a presentation consistency guard only; exact original damage-number font, battle-log format, timing, path, easing and crit/dodge/block effect keyframes remain unverified.
- Added visible runtime logging for checked avoidance passive hooks. Ranger `DodgeChance` / `ElementalDodgeChance` passives can now record live monster attacks as `BattleLogEntry.Kind.dodge`, Knight `BlockChance` passives can record them as `BattleLogEntry.Kind.block`, and the battle log displays monster incoming rows as `攻击被闪避` / `攻击被格挡` instead of indistinct `0` damage or attacker-ambiguous avoidance text. `Battle` keeps default random rolls for normal gameplay and exposes deterministic roll providers only for self-test/Swift-test coverage; `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guard that forced live monster attacks preserve HP, source element metadata and distinct avoidance log kinds. This verifies the current pre-damage avoidance branch and log visibility only; exact original dodge/block animation timing, probability caps, block reduction versus full negation, ordering against shields/resistance and support-member avoidance behavior remain unverified.
- Added runtime regression coverage for checked sustain passive stats reaching visible battle healing logs. Knight `HpRegenPerSec` passive `101012`, Hunter `AddHpPerHit` passive `501072`, Ranger `HpLeech` passive `201052`, and Knight `AddHpPerKill` passives `101021` + `101051` now have `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving live combat writes hero-side healing rows named `生命恢复`, `击中回复`, `生命汲取`, and `击杀回复` while keeping `Battle.heroHP` synchronized with `Hero.currentHP`. This verifies the current sustain hooks and log visibility only; exact original regen tick cadence, heal ordering versus damage/kill completion, overheal handling, animation/audio timing and per-class support-member sustain semantics remain unverified.
- Added runtime regression coverage for the checked typed damage passive stats. Hunter passives `501021` (`FireDamagePercent`), `501022` (`ColdDamagePercent`), `501061` (`LightningDamagePercent`) and Slayer passive `601021` (`PhysicalDamagePercent`) now have `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving they increase matching live skill damage logs for `爆炸弩箭` (`50101`), `寒霜弩箭` (`50201`), `电击弩箭` (`50601`) and `猛击跳跃` (`60101`) in a fixed battle window with crit suppression. This verifies the current damage-type multiplier hook reaches real combat; exact original elemental status rules, resistance ordering, stacking caps, projectile collision geometry and per-skill hit-frame timing remain unverified.
- Added runtime regression coverage for the checked `IncreaseAreaOfEffectDamage` passive stat. Slayer passive `601051` now has `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving it increases actual `猛击跳跃` (`60101`) AOE damage logs. This verifies the damage hook only; exact original AOE radius, target selection and geometry for `AreaOfEffect` are still unverified.
- Added runtime regression coverage for the checked `IncreaseProjectileDamage` passive stat. Ranger passive `201022` now has `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving it increases actual `散弹射击` (`20201`) projectile damage logs. This verifies the projectile damage hook without claiming exact projectile travel, spread, collision width or original hit-frame timing.
- Added runtime regression coverage for the checked `SkillHealIncrease` passive stat. Priest passive `401022` now has `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving it increases actual main-Priest `圣域` (`40401`) healing-over-time battle logs. This verifies the current healing hook only; exact original field geometry, duration, support-member independent formulas, stacking/replacement rules and `治愈` target priority remain unverified.
- Added runtime regression coverage for the checked `SkillDurationIncrease` passive stat. Slayer passive `601072` now has `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving it extends the actual active main-Slayer `嗜血` (`60601`) battle buff duration from the checked local cooldown-length scaffold `18.0s` to `32.4s` under the current `+80%` source value. This verifies that the duration hook reaches a live battle buff; exact original buff duration rules, stacking/replacement timing and whether every original skill shares this duration formula remain unverified.
- Added runtime regression coverage for checked cooldown-speed passives reaching real combat cadence. Sorcerer passives `301002` + `301041` (`CooldownReduction`) and `301051` (`CastSpeed`) now have `SelfTest`, `CombatStatsTests`, and `scripts/audit-local-gameplay-fidelity.sh` guards proving they increase the actual live `火球术` (`30101`) damage-log cast count over a fixed 12-tick battle window when the loadout is forced to that one cooldown skill. This verifies the current cooldown timer hook; exact original cast-time animation, animation lock, server tick resolution, queued-cast behavior and whether support-member cooldowns use the same formula remain unverified.
- Added official activation metadata to named skills and split battle execution by trigger type: `COOLDOWN` skills rotate through cooldown timers, `BASEATTACK_COUNT` skills trigger after base attacks using a temporary 3-hit interval, `BASEATTACK` hooks are ready for future basic-attack skill rows, and `CONTINUOUS` blessings are represented without being spam-cast in combat. Priest `力量祝福` and `守护祝福` now apply as party-scoped continuous modifiers when they are equipped by either the main hero or an unlocked support Priest through the currently modeled active-skill-slot/loadout system, and those continuous modifiers now feed compact player-side status badges instead of remaining invisible. `守护祝福` has been narrowed to source elemental incoming damage (`Fire`/`Cold`/`Lightning`) instead of reducing physical or chaos attacks, matching its checked elemental-resistance role while preserving the limitation that the exact original resistance/status formula remains unverified.
- Added structured active-skill damage metadata for the currently modeled 36 named skills: physical/fire/cold/lightning elements plus delivery categories such as melee, projectile, range AOE, trap, summon projectile, heal, buff and resurrection. Charged Trap's elemental detonation check now uses that metadata instead of a hard-coded skill-id allowlist, while the broader original resistance/status/damage-pipeline model remains unimplemented.
- Replaced the runtime first-N active-skill-only stand-in with persisted per-class active skill loadouts: the character panel can choose checked skill IDs for each currently unlocked active slot, battles consume those selections for the main hero and support members, invalid/duplicate saved IDs fall back to source-order defaults, and reset clears the local loadout. Exact original skill equip UI, ordering restrictions and any cost/level gating beyond the currently modeled slot count remain unverified.
- Replaced runtime dependence on macOS system SFX with packaged, local generated, short pixel-style WAV files for all current audio events. `GameAudio` now loads those resources from the SPM bundle first and retains system sounds only as a fallback; `ResourceSelfTest` derives the SFX list from `GameAudioEvent`, rejects extra unreferenced bundled WAVs and exact duplicate WAV payload reuse, verifies every event file is present, playable, AVFoundation-parseable, 16-bit PCM, mono, 22050 Hz, within the same 0.05-0.75s duration bounds used by `scripts/audit-local-sfx.sh`, within the same broad RMS/peak dBFS bounds, checks representative `BattleEvent` routes through `GameEngine.audioEvent(for:)`, checks every event's runtime playback volume profile, checks that every event's `minimumInterval` stays in the local throttle profile range, and gates basic generated-SFX relationships such as critical hit being longer than attack, level-up being longer than preview, and item-consumed not being shorter/weaker than item-equipped. `sfx_manifest.tsv` now records every local SFX as `generated_substitute` with `officialAudio=false`, plus event mapping, format fields, duration, SHA-256 and byte count, so substitute provenance cannot silently drift into an implied official-audio claim. The local audit currently reports 11 covered sounds, all 7 `BattleEvent` cases routed, `0.110-0.360s` duration, `-22.7..-11.2 dBFS` RMS, and `-11.1..-6.5 dBFS` peak.
- Expanded `scripts/audit-local-sfx.sh` beyond generic WAV metadata into event-profile and provenance checks. The audit now reports and gates onset time, envelope activity, zero-crossing rate, coarse spectral centroid, high/low-frequency energy ratios and duplicate WAV payloads, plus cross-event relationships such as critical-hit SFX being longer/brighter/noisier than the basic attack, skill-cast SFX having a slower onset than attack, battle-lost reading lower than battle-won, level-up lasting longer than preview, generated-substitute manifest rows, and packaged app WAV plus manifest payload parity when `dist/TBH.app` exists. Current deterministic output reports centroid range `553..3173 Hz` and passes all profile/provenance checks. These checks protect the generated substitute set from obvious regressions and accidental provenance drift, but they are not evidence of exact original per-event SFX parity.
- Extended the local SFX audit to parse `GameAudioEvent.volume` and render deterministic runtime-volume sequence mixes for common short event clusters: `combat_burst` (`0.790s`, `-21.5 dBFS` RMS, `-14.0 dBFS` peak), `reward_chain` (`1.640s`, `-23.6 dBFS` RMS, `-16.0 dBFS` peak), and `defeat_chain` (`0.530s`, `-22.6 dBFS` RMS, `-14.0 dBFS` peak). This adds a repeatable check that generated event bursts keep useful headroom and audible energy relative to the official trailer baseline, while still treating them as generated substitutes rather than proof of original isolated attack, loot, victory or defeat SFX parity.
- Added Swift self-test coverage for `GameAudioEvent.minimumInterval`: every event must have a local throttle profile, stay in its category range, and keep victory/defeat/level-up cues spaced longer than repeatable attack/crit/skill/damage cues. This mirrors the shell audit's runtime-throttle guard inside the app's DEBUG self-test and remains a local regression check, not original cadence parity.
- Extended the release-safe `ResourceSelfTest` to cover representative `BattleEvent` audio routes, `GameAudioEvent.volume` profile ranges, the same `GameAudioEvent.minimumInterval` profile ranges and the combat-versus-terminal spacing relationship, so CI/package resource checks now catch missing/drifted battle SFX mappings, playback volumes or playback throttles even outside DEBUG self-test runs.
- Attached the refreshed stages page's 120-row runtime table to `StageDefinition`: each difficulty stage now carries mined level, waves, kill count, gold, XP, HP, monster/Boss name, and monster composition metadata. Progression uses mined kill targets, spawned monsters use mined HP and composition-selected names by encounter index, rewards use mined gold/XP, Boss HP comes from the same mined table, and self-tests assert the table count plus representative HP/composition/spawn-selection values.
- Added `StageEncounterState` and `StageEncounterPlan` so runtime combat exposes current encounter number, clear target, current wave, wave count, wave-local progress, composition-weight index, active composition monster, and the complete stage encounter list grouped by wave. Composition counts are now scaled across the mined kill target, which avoids over-repeating the final monster when the Wiki composition count total does not equal the stage's kill count. Battle and Settings UI now display current wave, wave-local progress and enemy state.
- Wired the encounter plan into runtime combat: `GameLoop` now starts battles with the remaining encounters in the current wave, `Battle` keeps that wave as simultaneous enemy states with individual HP/defeat flags and per-enemy attack cooldowns, victory rewards aggregate XP/gold/loot across the cleared encounters, and `ProgressTracker.advance(by:)` applies the cleared encounter count to stage progress.
- Exposed the active battle's current-wave enemy state (`enemyStates`, `activeEnemyState`, `aliveEnemyStates`, `waveMonsters`, `remainingWaveMonsters`, `upcomingWaveMonsters`) and rendered live side enemies as a small layered group with mini HP bars behind the focused target, with compact wave progress dots in the battle scene.
- Added the first exact stage monster/Boss idle sprites referenced by the refreshed stages table and mapped those monster IDs to bundled resources, replacing the earlier broad reuse of generic goblin/skeleton/insect/ghoul/golem sprites for those encounters.
- Added explicit approximate profile/art mappings for the remaining stage composition names that do not yet have exact extracted sprites, so non-slime encounters no longer degrade into the slime fallback. Self-test and Swift Testing now scan all 49 composition names, assert that only `史莱姆` may use slime art, and also assert that no stage battle monster maps to legacy full-screenshot UI crops such as `monster_slime_red`.
- Wired monster drops to the stage item-level threshold instead of deriving item level from XP reward, preventing high-XP later stages from producing runaway item levels outside the current original threshold table.
- Added Soul Stone item database metadata: Normal/Nightmare/Hell/Torment material IDs `190001`-`190004` and rarities Immortal/Arcana/Beyond/Celestial. Opened chests now grant the Soul Stone matching the chest difficulty bucket instead of using item level thresholds to jump to a higher difficulty stone.
- Replaced formula-generated chest IDs with explicit, checked source mappings for Normal Monster Box, Stage Boss Box and Act Boss Box entries. Non-Boss stage clears now grant both mapped Normal Monster and Stage Boss source chests; X-10 Boss clears now consume the required Soul Stone and grant mapped Normal Monster plus Act Boss source chests. `Stage Boss Box 6` at level 10 maps to `#920022` instead of the old formula's incorrect `#920101`. Exact chest contents, drop rates and probability rules remain intentionally unimplemented.
- Updated chest opening from a difficulty-bucket-only UI toward the source box families: saved chests can now be opened by concrete chest ID, and Settings lists each saved chest with its family display name, database ID, difficulty bucket, rarity and source stage. The older `openChest(kind:)` path remains for compatibility, but both routes share the same loot retention, Soul Stone, SFX and save behavior. Exact chest contents, drop rates and probability rules remain intentionally unimplemented.
- Added the individual active-skill page value tables for the 36 currently named hero skills. Battle damage now uses checked Lv1 percentage values for damage skills, Priest healing/continuous blessings read checked Lv1 utility values, and `天堂之怒` has been corrected back to `COOLDOWN` based on the current detail page. Attack-count cadence, buff durations and multi-hit/summon/status details remain intentionally incomplete.
- Added the checked passive-skill catalog from the current `/zh/skills/` and `/en/skills/` passive table rows: 108 rows total, 18 per hero-class prefix, preserving ID, English name, stat, value type and value. This is intentionally data-only; exact original passive unlock paths, stat-pipeline integration and runtime effects remain unimplemented.
- Added per-member support skill execution: each unlocked non-primary party member now makes visible support attacks from its own per-slot cooldown instead of waiting for the main hero attack window, rotates through available class `COOLDOWN` skills, contributes equipped `CONTINUOUS` Priest blessings to party attack and incoming-damage modifiers, keeps its own sustained summon/range state for support-cast `烈焰九头蛇`, `暴风雪`, and `弩炮塔`, and now maintains an independent support attack counter for modeled `BASEATTACK_COUNT` skills. Support Ranger/Hunter/Slayer/Knight attack-count skills reuse the current source-backed runtime scaffolds, including support-attributed `快速射击` volley hits and support-attributed `电击弩箭` lodged hit plus `电击弩箭电流` damage-over-time/status state. Support Ranger `散弹射击` and `箭雨`, plus support Sorcerer `陨石打击`, are explicitly guarded as support-attributed current-wave range damage using their checked physical/fire Lv1 value scaffolds, Support-Ranger `迅捷觉醒` now applies the checked Lv1 `+500%` attack-speed value as a support-scoped 6.0x speed buff instead of remaining a generic buff log, Support-Hunter `寒霜弩箭` casts keep the same checked cold projectile explosion/freeze scaffold as main-Hunter casts while remaining support-attributed in damage logs, support-Priest `天堂之怒` casts now activate a support-scoped attack-added lightning range-damage buff instead of falling through to immediate direct damage, and support-Slayer `嗜血` casts now consume that support member's current HP and apply a support-scoped attack multiplier instead of remaining a generic buff log. This is still a scaffold using shared hero level, Lv1 skill values, the current temporary 3-hit attack-count cadence and cooldown-length duration stand-ins until independent member levels/equipment, exact support cooldown formulas, exact attack-count timing, summon/current durations, replacement/stacking, projectile distribution, range geometry and support-specific field rules are verified.
- Support-Ranger `穿透之箭` and `穿刺射击` damage logs now explicitly carry the checked physical/projectile metadata, so battle-log element markers and piercing/lodged-arrow trajectory cues no longer rely only on skill-name inference. The current implementation still uses the existing current-wave pierce and lodged-arrow/bleed scaffolds; exact projectile geometry, lodged-arrow duration/reset behavior and bleed tick rules remain unverified.
- Added runtime support-member HP/down state, main/support Priest `复活` execution and support-Knight `不屈意志` self-rescue: defeated support members stop attacking/casting, `40601` waits for a fallen support target, the checked Lv1 value revives that support with `300%` max HP, support Priest casts revive another fallen support member with support-attributed logs, and a support Knight with `10601` equipped now self-revives once from lethal monster damage using the checked raw Lv1 `300%` support max-HP value. This remains a scaffold until exact original target priority, independent support stats/equipment/levels, support self-rescue/reset scope, animation timing and downed cooldown behavior are verified.
- Added the first Cube runtime hook: `CubeProgress` persists total infused item count and total XP, backpack items can be consumed through the Inventory panel's Cube action, and gained XP uses the checked `Rarity.cubeExperience` table. Locked items cannot be infused. This intentionally stops at checked XP accumulation and does not claim the original Cube level, reward, or effect rules are implemented.
- Added the first Alchemy runtime hook: backpack items can be consumed through the Inventory panel's Alchemy action, gained gold uses the checked `Rarity.alchemyGoldValue` table, and locked items are protected. This intentionally stops at checked rarity gold conversion and does not claim full original Alchemy flow, Synthesis interaction, economy context, or rate semantics.
- Added a Settings `原版合成/Cube/炼金规则` review panel. It shows the 10 checked rarity rows, the checked 9-item Synthesis input rule, the 9 currently modeled next-rarity transitions, the 10/10 Cube XP and Alchemy gold table coverage, and the current approximate skip-tier examples (`Common -> Rare ~60%`, `Uncommon -> Legendary ~40%`, `Rare -> Legendary ~20%`, `Legendary -> Immortal ~5%`). The panel explicitly marks complete probability/failure/skip tables, item-level downgrade formula, Synthesis level costs, and Cube level/reward formulas as `待核对`, so the runtime's guaranteed next-rarity Synthesis and XP/gold conversion cannot be mistaken for full original crafting parity.
- Moved the checked approximate Synthesis result examples into the core item model and surfaced the matching example in the backpack Synthesis preview. The preview now shows the selected 9-item recipe, output level, checked source base gear name/ID, locked-material count, and the relevant source example such as `普通 -> 稀有 ~60%`, while still leaving the actual runtime result as the conservative next-rarity scaffold until the complete probability/failure/downgrade table is verified.
- Added the first Synthesis runtime hook: the item model now persists `itemLevel`, the Inventory panel previews eligible Synthesis count, and `GameEngine.synthesizeItems(rarity:)` consumes 9 unlocked same-rarity backpack items into one next-rarity item while preserving the highest consumed item level and protecting locked items. The preview now shares the runtime input-selection rule, separates unlocked inputs from locked excluded items, shows the guaranteed output item level only when the checked 9-item recipe is ready, and flags the source-mentioned skip-tier / item-level downgrade variants as unverified instead of inventing probabilities. This intentionally stops at the checked 9-to-next-tier rule and does not claim the original skip probabilities, failure handling, Synthesis-level costs or level-downgrade formula.
- Added the first active cooldown-buff state: Hunter `快速装填` now applies a +50% attack-speed buff for the checked Lv1 value of 3 hero attacks, instead of only logging a generic buff event. Support-Hunter `快速装填` now has a support-scoped scaffold with the same checked 3 support-attack charges and a support-attributed buff log; support base attacks now run from per-slot independent cooldowns, so this buff shortens the Hunter support cooldown while charges remain instead of granting an immediate duplicate attack from the main hero attack window. Exact original support attack-speed timing, animation timing and support-specific stat formulas remain unverified. Other timed buffs still need exact original duration/state modeling.
- Reworked Knight `神盾领域` from a generic buff log into an active party damage shield for main-Knight and support-Knight casts: while active it applies the checked Lv1 `500` damage-block value as a consumable shield pool against incoming monster damage for the main hero and living support members, with support casts producing support-attributed buff logs. The exact original buff duration, field geometry, shared-pool versus per-ally shield semantics and independent support formulas are not exposed by the checked page, so duration remains a cooldown-length stand-in and field coverage remains a party-scaffold approximation.
- Reworked Knight `不屈意志` from a no-op cooldown entry into a lethal-damage rescue: it is no longer auto-cast by the cooldown rotation, triggers when monster damage would defeat the main Knight, restores the checked Lv1 `300%` max HP value above the normal HP cap, logs the skill, and persists one consumed stage key so rebuilt battles in the same stage do not refresh it. Support Knight casts now use the same source value as a once-per-battle-state self-rescue when that support member is defeated by monster damage, with a support-attributed heal log and without inventing independent support scaling. The Wiki page proves the visible `每关一次` wording but not retry/reset edge cases or support-specific reset scope, so exact reset semantics still need stronger evidence.
- Added the first kill-triggered secondary skill rider: Slayer `粉碎强击` now emits a `粉碎强击冲击波` damage log and applies `350%` attack damage to other alive enemies in the current wave when the main hit kills its focused target. That derived shockwave log now drives a compact shockwave-ring trajectory and shockwave-impact cue, while the primary `粉碎强击` hit keeps the ordinary physical slash cue. This is still a wave-wide stand-in for “surrounding enemies” until exact original AOE geometry, target spacing and shockwave keyframes are verified.
- Reworked Knight `盾牌冲锋` away from the overly broad current-wave melee collision scaffold: activation now applies the checked Lv1 `3000%` physical damage value only to the current focused collision target for both main-hero and support-member casts, because the checked source row says `Melee` rather than `Melee, AOE`. Its battle log still drives a dedicated charge-dash visual trail instead of using the generic no-trajectory melee treatment. Exact charge path, collision width/count, target ordering, timing, keyframes and any knockback/stun behavior remain unimplemented.
- Reworked Knight `穿透突刺` from generic focused damage into current-wave melee range damage: when its attack-count trigger fires, it now applies the checked Lv1 `2500%` physical damage value to alive enemies in the current wave. Exact thrust path, collision width/count, target ordering, timing and attack-count cadence remain unimplemented.
- Reworked Knight `报应打击` from generic single-target damage into focused melee multi-hit damage: when its attack-count trigger fires, it now applies checked Lv1 `1500%` physical damage per strike and increases strike count as current HP gets lower. The current two-to-five strike HP buckets are a monotonic scaffold because the checked pages do not expose exact original HP thresholds or strike caps.
- Reworked Slayer `猛击跳跃` from generic single-target damage into current-wave melee range damage: activation now applies the checked Lv1 `3100%` physical damage value to every alive enemy in the wave for both main-hero and support-member casts, and its battle log now drives a dedicated leap-arc visual trail matching the checked non-English description that the Slayer leaps to an enemy before the landing shockwave. The English Wiki description conflicts with a `60201`-style kill shockwave, so no extra kill rider was added until stronger evidence resolves that discrepancy.
- Reworked Slayer `将军怒吼` from a generic buff log into an active crit-coefficient/stun scaffold for main-Slayer and support-Slayer casts: activation now applies the checked Lv1 `+500%` party critical chance coefficient as a 6.0x crit-rate multiplier for main-hero and support attacks, then delays alive current-wave enemy cooldowns by at least one attack interval and displays that stand-in as a `眩晕` enemy badge. Support casts now produce support-attributed buff logs while using the same raw Lv1 source value. The exact original stun duration, AOE geometry, stacking rules, support independent formulas and critical-coefficient formula are not exposed by the checked page, so these parts remain placeholders.
- Reworked Slayer `大地强击` from generic single-target damage into current-wave range damage: activation now applies the checked Lv1 `3700%` physical damage value to every alive enemy in the wave, arms conservative local rock charges, and its battle log now drives a ground-rupture trajectory plus earthquake-impact cue instead of the generic physical slash. Later modeled physical AOE damage consumes those charges into a `大地强击岩石爆炸` physical melee/AOE damage log with a dedicated rock-burst trajectory and rock-explosion impact cue. The exact original rock entities, HP/positioning, trigger timing, explosion geometry, damage formula and complete physical-AOE interaction matrix remain unverified.
- Reworked Slayer `旋转斧` from generic single-target damage into an active per-second range-damage buff: while active it applies the checked Lv1 `1000%` physical damage value each second to every alive enemy in the current wave. Its hit tick now also marks surviving hit enemies as bleeding and exposes the same compact bleeding badge used by `穿刺射击`. Already-bleeding enemies now receive a separate `旋转斧流血追击` physical follow-up damage log for both main-hero and support-member casts, using a conservative local 25% follow-up scaffold because the checked page only states that attacking bleeding enemies deals extra damage and does not expose the exact probability, duration or formula.
- Reworked Knight `神圣之刃` from an instant-heal placeholder into an active buff for main-Knight and support-Knight casts: main casts apply the checked +50% attack modifier and heal Lv1 `20` HP when the hero attacks, while support casts apply the same checked attack modifier to that support member and heal that support member by the raw Lv1 `20` HP on support attacks. The exact original buff duration, support independent formulas and support targeting/stacking semantics are not exposed by the checked page, so the current duration remains a cooldown-length stand-in and support coverage remains a caster-self scaffold.
- Reworked Ranger `迅捷觉醒` from a no-op buff log into an active attack-speed buff: while active it applies the checked Lv1 `+500%` attack-speed value as a 6.0x attack-speed multiplier. The local pacing model now keeps the normal `1.0s` attack floor for unbuffed combat but allows hasted attacks down to the `0.25s` battle step, so main-Ranger and support-Ranger `迅捷觉醒` both increase base-attack frequency instead of only showing a buff icon/log. Support-Ranger casts apply the same checked value as a support-scoped 6.0x attack-speed buff with support-attributed battle logs. The exact original buff duration, support-member independent formulas and final attack-speed cap are not exposed by the checked page, so the current duration and cap remain local stand-ins.
- Reworked Priest `治愈` from a main-hero-only heal into a living-ally heal: main-hero and support-member casts now use the target ally's own max HP with the checked Lv1 `100%` value. The exact original target priority and cooldown behavior when no ally is wounded are not exposed by the checked page, so target selection currently uses a most-wounded living ally scaffold.
- Reworked Priest `圣域` from a main-hero-only over-time heal into a main/support living-party healing buff: main-Priest and support-Priest casts now apply the checked Lv1 `300` HP-per-second healing value to the main hero and living support members on battle ticks, with support casts keeping support-attributed heal logs. The exact original buff duration, field geometry, support-member independent formulas and stacking/replacement behavior are not exposed by the checked page, so duration remains a cooldown-length stand-in and field coverage remains a party-scaffold approximation.
- Reworked Priest `天堂之怒` from a one-shot direct-damage placeholder into an active attack-damage buff: while active, later main-hero or support-Priest attacks apply the checked Lv1 `4300%` lightning range-damage value across the live wave, with support-added damage staying support-attributed in battle logs. The exact original buff duration, support-member independent formulas and range geometry are not exposed by the checked page, so duration remains a cooldown-length stand-in and range is simplified to current-wave targets.
- Reworked Slayer `嗜血` from a generic buff log into an active sacrifice buff: main-Slayer activation consumes half of the hero's current HP, support-Slayer activation consumes half of that support member's current HP, and both apply the checked Lv1 `+4000%` attack-damage value as a 41.0x attack multiplier while active. The exact original buff duration, support-member independent formulas and low-HP edge cases are not exposed by the checked page, so duration remains a cooldown-length stand-in and support scaling uses the current shared support scaffold.
- Reworked Sorcerer `火球术` from generic single-target damage into current-wave range fire damage: activation now applies the checked Lv1 `2700%` fire damage value to every alive enemy in the wave for both main-hero and support-member casts. The exact original projectile travel, contact explosion timing, explosion geometry, elemental resistance interaction and fire-specific statuses remain unimplemented.
- Reworked Sorcerer `冰球术` from generic single-target damage into a current-wave cold multi-hit/slow scaffold: activation now emits two damage hits per alive wave enemy, splits the checked Lv1 `1500%` value across those hits, delays hit enemies' next attack tick, and displays the current slow stand-in as a `寒冷` enemy badge. Exact original projectile travel, multi-hit count/timing, slow amount/duration, cold resistance, freeze/chill behavior and explosion geometry remain unimplemented.
- Reworked Sorcerer `闪电术` from generic single-target damage into current-wave AOE lightning damage: activation now applies the checked Lv1 `2550%` lightning damage value to every alive enemy in the wave for both main-hero and support-member casts. Exact original forward electricity path, line/chain geometry, lightning resistance, shock/status behavior and falloff remain unimplemented.
- Reworked Sorcerer `烈焰九头蛇` from generic single-target damage into an active summon-damage scaffold: activation now records a summon buff, and battle ticks fire checked Lv1 `2300%` focused fire projectile damage from the hydra stand-in for both main-hero and support-member casts. Support-cast Hydra keeps support-attributed damage logs and visible deployable/status state. Exact original summon duration, hydra/fireball cadence, target selection, projectile travel, splash behavior, support summon replacement/stacking and fire status/resistance interactions remain unimplemented.
- Reworked Sorcerer `暴风雪` from generic single-target damage into an active cold range damage-over-time/cooling scaffold: while active, battle ticks apply checked Lv1 `500%` cold damage per second across alive current-wave enemies for both main-hero and support-member casts, delay hit enemies' next attack tick, and display the current cooling stand-in as a `寒冷` enemy badge. Exact original storm duration, ice-block cadence/count, field geometry, cooling/freeze/chill rules, support storm replacement/stacking and cold resistance interactions remain unimplemented.
- Reworked Sorcerer `陨石打击` from generic single-target damage into current-wave fire range damage: activation now applies the checked Lv1 `5500%` fire damage value to every alive enemy in the wave for both main-hero and support-member casts. Its battle log now drives a dedicated falling-meteor trajectory and meteor-impact cue rather than the generic range-field/fire-burst pair. Exact original meteor travel/fall timing, impact delay, impact geometry, fire resistance, burn/status behavior and falloff remain unimplemented.
- Reworked Ranger `箭雨` from generic single-target damage into current-wave physical range damage: activation now applies the checked Lv1 `2150%` physical damage value to every alive enemy in the wave for both main-hero and support-member casts. Exact original arrow count, fall timing, target distribution, impact geometry, physical resistance and falloff remain unimplemented.
- Reworked Ranger `散弹射击` from generic single-target damage into a tracking-projectile fan-out scaffold: activation now applies the checked Lv1 `1620%` physical damage value as one projectile hit to each alive current-wave enemy for both main-hero and support-member casts. Exact original arrow count, projectile distribution, target priority, travel timing, physical resistance and overkill/falloff behavior remain unimplemented.
- Reworked Ranger `快速射击` from generic single-target damage into a focused physical projectile volley scaffold: when its attack-count trigger fires, it now applies the checked Lv1 `1320%` physical damage value as multiple projectile hits instead of a single generic hit. Exact original arrow count, shot spacing, target retargeting and attack-count cadence remain unimplemented.
- Reworked Ranger `穿透之箭` from generic single-target damage into a physical projectile pierce scaffold: when its attack-count trigger fires, it now applies the checked Lv1 `2440%` physical damage value to every alive enemy in the current wave. Exact original pierce line geometry, collision width, target ordering and maximum pierce count remain unimplemented.
- Reworked Ranger `穿刺射击` from generic single-target damage into a lodged-arrow/bleeding scaffold: when its attack-count trigger fires, it adds a lodged arrow to the focused enemy, applies the checked Lv1 `+1000%` physical projectile damage per lodged arrow, marks bleeding after 3 lodged arrows, and now renders a compact red status badge for bleeding active/side enemies in the battle strip. Exact original lodged-arrow duration, bleed damage, bleed tick cadence, bleed duration and bleed stacking remain unimplemented.
- Reworked Hunter `爆炸弩箭` from generic single-target damage into a current-wave fire projectile explosion scaffold: when its attack-count trigger fires, it applies the checked Lv1 `4840%` fire damage value to every alive enemy in the wave. Exact original projectile travel, impact explosion geometry, fire resistance, burn/status behavior, falloff and attack-count cadence remain unimplemented.
- Reworked Hunter `寒霜弩箭` from generic single-target damage into a current-wave cold projectile explosion/freeze scaffold: on cooldown, main-Hunter and support-Hunter casts apply the checked Lv1 `2100%` cold damage value to every alive enemy in the wave, delay hit enemies' next attack tick as a freeze stand-in, and display the stand-in as a `冻结` enemy badge. Support casts keep support-attributed `寒霜弩箭` damage logs. Exact original projectile travel, impact explosion geometry, freeze duration/stacking, cold resistance, freeze/chill status behavior, support-specific scaling and falloff remain unimplemented.
- Reworked Hunter `充能陷阱` from a generic buff log into an active trap-trigger scaffold: activation arms a pending trap, and the next actual hero-side elemental damage log detonates it as `充能陷阱爆炸` physical current-wave damage using the checked Lv1 value table as the damage baseline. Direct elemental hits, support-attributed elemental hits and currently modeled elemental damage-over-time can trigger it only after damage is actually logged; physical damage logs and buff-only events leave the armed trap unspent. Support-Hunter casts now share that visible trap counter/deployable state and detonate with support-attributed explosion logs when their support-side trap is consumed. Exact original trap count, placement, duration, complete trigger-source matrix, support-specific scaling, explosion formula/radius and multi-trap behavior remain unimplemented.
- Reworked Hunter `弩炮塔` from generic immediate single-target damage into an active summon-damage scaffold: activation now records a turret buff, and battle ticks fire checked Lv1 `1750%` focused physical projectile damage from the turret stand-in for both main-hero and support-member casts. Exact turret duration, firing cadence, target selection, projectile travel, summon placement, support turret replacement/stacking and independent support cooldown semantics remain unimplemented.
- Reworked Hunter `电击弩箭` from generic single-target damage into a lodged bolt plus lightning-current scaffold: the attack-count trigger now logs the checked Lv1 `2700%` focused lightning projectile hit, then activates `电击弩箭电流` to apply checked Lv1 lightning range damage over time across alive current-wave enemies. Exact current duration, tick cadence, nearby-target geometry, lodged-target rules, projectile travel and lightning status/resistance behavior remain unimplemented.

## Next Fidelity Priorities

Additional local Settings-boundary guard in this pass: the deterministic render audit now includes an `originalFidelityPanel` fixture that renders the real `OriginalFidelityBoundaryView` instead of a synthetic summary. The latest local screenshot audit measured `original_fidelity_panel_non_dark_pixels=208820`, `original_fidelity_panel_pill_pixels=483`, `original_fidelity_panel_text_light_pixels=12635`, `original_fidelity_panel_status_green_pixels=330`, `original_fidelity_panel_status_orange_pixels=1864`, and `original_fidelity_panel_status_gap_pixels=1340`, so the render gate catches regressions where the high-level original-fidelity coverage pills, readable boundary text, covered/partial/gap state markers, or explicit local-substitute boundaries disappear from Settings. This verifies local visibility of the current evidence boundary UI; it does not prove exact original gameplay, exact Rune Tree economy, exact item records, exact monster roster/art, exact animation frames, or exact original SFX parity.

Additional Rune review visibility guard in this pass: the deterministic render audit now includes a `runeEvidencePanel` fixture that renders the real `SourceRuneEvidenceReviewView` and `LocalRuneCostReviewView` instead of relying only on static metric checks. The latest local screenshot audit measured `rune_evidence_panel_non_dark_pixels=151857`, `rune_evidence_panel_pill_pixels=3480`, `rune_evidence_panel_text_light_pixels=10733`, `rune_evidence_panel_orange_pixels=17259`, and `rune_evidence_panel_green_pixels=358`, so the render gate catches regressions where Rune evidence tiers, candidate-cost queues, pending-cost groups, readable text, or verified/pending state markers disappear from Settings. This verifies local readability of the current Rune evidence and cost review UI; it does not prove exact original Rune Tree economy, complete verified per-node costs, branch/path costs, reset/refund behavior, point economy, or exact original Rune Tree layout parity.

Additional skill review visibility guard in this pass: the deterministic render audit now includes a `skillEvidencePanel` fixture that renders the real `LocalSkillRuntimeCoverageView`, `SourceSkillDamageReviewView`, `SourceSkillRangeReviewView`, and `PendingSourceSkillReviewView` instead of relying only on static source metrics. The latest local screenshot audit measured `skill_evidence_panel_non_dark_pixels=120724`, `skill_evidence_panel_pill_pixels=23590`, `skill_evidence_panel_text_light_pixels=13909`, and `skill_evidence_panel_orange_pixels=30226`; green, blue, and purple marker counts are currently `0` because the visible review state is dominated by text, summary pills, and orange pending-source markers. This verifies local readability of the current source skill and pending-skill review UI; it does not prove exact original skill ownership, formulas, delivery geometry, target rules, cooldown timing, action frames, VFX/SFX, or full 106-source-row runtime parity.

Additional passive review visibility guard in this pass: the deterministic render audit now includes a `passiveEvidencePanel` fixture that renders the real `SourcePassiveSkillDatabaseView` and representative `SourcePassiveSkillRow` entries instead of relying only on static passive-source metrics. The latest local screenshot audit measured `passive_evidence_panel_non_dark_pixels=155972`, `passive_evidence_panel_pill_pixels=3770`, `passive_evidence_panel_text_light_pixels=14484`, `passive_evidence_panel_source_icon_pixels=21515`, `passive_evidence_panel_missing_icon_pixels=4511`, and `passive_evidence_panel_green_pixels=7436`, so the render gate catches regressions where passive source rows, source-backed icon samples, readable text, coverage pills, or explicit missing-source-icon markers disappear from Settings. This verifies local readability of the current passive source/icon review UI; it does not prove exact original passive unlock paths, passive tree layout, full runtime semantics, missing source icon families, or original passive-effect animation/SFX parity.

1. Replace placeholder chest contents/Soul Stone drop rules with exact original rates once a complete drop table or in-game capture is available.
2. Replace the remaining stage placeholders with exact original wave timing/targeting rules, exact monster ATK/DEF, exact box probability/conditional reward rules, exact sprites for every approximate monster mapping, and stage-specific monster animation frames/scale for every mined monster name.
3. Replace the current party/Rune Tree runtime scaffold with high-fidelity systems: executable costs/effects/layout for the 197-node source Rune Tree, exact reset price/refund/point semantics beyond the current local checked-cost refund, exact point/gold economy beyond checked formation command runes, approximate-cost Rune of Awakening, current no-price Rune of Repose/offline +10% boost nodes, current no-price first Rune of Expansion inventory-capacity scaffold, current no-price chest-family capacity `+1` scaffold, exact original active skill equip UI/order/cost constraints beyond the current per-class picker, independent member HP/equipment/skill levels, exact support cooldown/attack timing, and exact class/support formulas.
4. Expand items beyond the current taxonomy, source-page aggregate scaffold, and 396 base progression icons: exact per-rarity/per-affix 5,760 item records, exact stat affixes, full per-variant icon/name progression, exact original icon art for item variants, exact material use/effect systems beyond the checked material taxonomy, full Synthesis probabilities/skip/failure/level-downgrade semantics beyond the current checked 9-to-next-tier hook, full Cube level/reward/effect semantics beyond the current checked-XP infusion, and full Alchemy flow/context beyond the current checked rarity-gold conversion.
5. Expand skill execution from the current trigger-type scaffold to exact original rules: per-skill formulas, exact attack-count intervals, passive unlock/effect application, durations, summons, status effects, resurrection, support-member independent skill state and party targeting.
6. Use the deterministic local battle-scene render plus the Steam battle-scene audit as the baseline for rendered screenshot comparison, then tune exact animation timing, sprite scale, lane anchors, HP bar dimensions, stage pill offsets and hit/loot feedback.
7. Use the new Steam Trailer audio audit as a broad loudness/energy baseline, then obtain in-game captures or authorized assets before claiming exact original per-event SFX parity.
