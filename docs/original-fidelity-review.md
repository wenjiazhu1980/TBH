# TBH Original Fidelity Review

Last reviewed: 2026-06-15

## Source Quality

| Source | Scope | Confidence | Notes |
| --- | --- | --- | --- |
| Steam store page for `TBH: Task Bar Hero` | Official gameplay positioning, feature list, visual media links | High | Official product page, refreshed 2026-06-14. Confirms tiny taskbar idle RPG, pixel heroes, classes/items/skills/builds, Cube, 3 Acts, 4 difficulty tiers, 500+ items, 50+ monsters, 10 rarity grades. Direct stream checks found the official Trailer HLS exposes AAC 48 kHz stereo audio plus H.264 video variants, while the embedded Steam extras checked in this pass (`wishlist`, `interface`, `battlescene`, `cube`, and the `battlescene` WebM) expose video streams only. `docs/steam-audio-audit.md` now records the reproducible ffprobe/ffmpeg audit: the current 47s Trailer stream is AAC LC 48 kHz stereo, measured at about -15.3 LUFS integrated loudness, 4.6 LU loudness range and 0.0 dBFS true peak. `docs/steam-battle-scene-audit.md` now records the reproducible battle-scene audit: the current official `extras/battlescene` media is video-only, 776x180, 30 fps, about 6.133s, 184 frames, with a 4.31:1 strip composition, and the local app now has a deterministic `TBH --render-battle-scene` PNG path plus repeatable ground-strip geometry and primary/support party-layout checks for rendered `BattleSceneView` output. These media prove broad official visual/audio presentation, but not isolated per-event SFX or exact battle logic. |
| `taskbarhero.org/zh` Wiki | Detailed mechanics, database pages, Chinese labels | Medium | Refreshed 2026-06-15. Page footer states it is an unofficial fan Wiki. Useful for detailed database alignment, but should not override Steam or in-game evidence. The items page cross-checks Steam's 10-grade rarity ladder with Chinese labels, colors, Alchemy value, Cube XP, and D/E/I slot counts; it also lists four Soul Stone material rows (`190001`-`190004`) and 59 stage chest rows across Normal Monster Box, Stage Boss Box and Act Boss Box families. The Chinese and English items pages both state that Synthesis combines 9 same-rarity items into 1 item of the next tier, can occasionally skip a tier, and that low Synthesis level can downgrade output item level; the same table gives approximate examples such as Common -> Rare `~60%`, Uncommon -> Legendary `~40%`, Rare -> Legendary `~20%`, and Legendary -> Immortal `~5%`, but it does not expose the complete probability table, failure/skip resolution, Synthesis-level progression costs or item-level downgrade formula. The drops tool states its current local snapshot has no complete item drop table and that box-stage relations are inferred by box family, Boss status and stage level; currently checked source examples include `1101` -> `Normal Monster Box 1` / `Stage Boss Box 1` / `HP 470` / `哥布林盗贼:1, 史莱姆:5, 哥布林:5`, `1109` -> `Normal Monster Box 3` / `Stage Boss Box 6` / `HP 6340`, `1110` -> `Normal Monster Box 3` / `Act Boss Box 1` / `HP 1050` / `骷髅王:1`, and `4310` -> `Normal Monster Box Lv90` / `Act Boss Box Lv90` / `HP 3550` / `执政官莫尔卡:1`. The stages page lists all 120 difficulty stages with level, waves, kill count, gold, XP and monster/Boss names, plus Boss HP and item-level thresholds. The guide page documents Alt+click item locking and prioritizing Rune Tree party slots for deploying heroes 2 and 3. The skills page lists 106 active/attack skills, 108 passive skills, and `CONTINUOUS`, `COOLDOWN`, `BASEATTACK`, `BASEATTACK_COUNT` activation types; direct page-source parsing of `/zh/skills/` and `/en/skills/` produced the same 108 passive rows with matching `data-id`, `data-stat`, `data-type` and `data-value`, distributed as 18 rows per class prefix. Current checked examples include `10101`/`10301`/`20101`/`20501`/`20601`/`50101`/`50601`/`60201`/`60401` as attack-count skills and `40201`/`40501` as continuous blessing skills, plus passive rows such as `101001 AttackDamage FLAT 1`, `201011 CriticalChance ADDITIVE 200`, `501021 FireDamagePercent FLAT 150`, and `601072 SkillDurationIncrease ADDITIVE 80`. Individual active-skill pages expose 10-level value tables for the 36 currently named hero skills, including `10101` Lv1/Lv10 `2500/4300`, `20101` `快速射击` with `BASEATTACK_COUNT`/攻击次数 trigger, physical projectile hit type, range `1150`, Lv1/Lv10 `1320/2400`, and description text stating that it fires multiple arrows with each dealing `1320%` physical damage, `20501` `穿透之箭` with `BASEATTACK_COUNT`/攻击次数 trigger, physical projectile hit type, range `1200`, Lv1/Lv10 `2440/3880`, and description text stating that it fires a piercing arrow dealing `2440%` physical damage to hit enemies, `20601` `穿刺射击` with `BASEATTACK_COUNT`/攻击次数 trigger, physical projectile hit type, range `1200`, Lv1/Lv10 `1000/3700`, and description text stating that each lodged arrow increases damage by `1000%` and 3+ lodged arrows cause bleeding, `50101` `爆炸弩箭` with `BASEATTACK_COUNT`/攻击次数 trigger, fire damage, projectile hit type, range `1100`, Lv1/Lv10 `4840/9430`, and description text stating that it fires a bolt that explodes on hit to deal `4840%` range fire damage, `50201` `寒霜弩箭` with `COOLDOWN`/冷却 trigger, cold damage, projectile hit type, range `1100`, Lv1/Lv10 `2100/3450`, and description text stating that it fires an exploding bolt dealing `2100%` range cold damage and freezing hit enemies, `10401` `神盾领域` with `COOLDOWN` trigger, range `150`, Lv1/Lv10 `500/1850`, and description text stating that it blocks `500` damage for allies in the field, `10601` `不屈意志` with `COOLDOWN` trigger, range `150`, Lv1/Lv10 `300/1200`, and description text stating that once per stage it stands back up with `300%` HP when near death, `20401` Lv1/Lv10 attack speed `500/1400`, `40101` Lv1 `100`, `40201` Lv1 `500`, `40301` `天堂之怒` with `COOLDOWN` trigger, lightning range `1050`, Lv1/Lv10 `4300/7900`, and description text stating that attacks deal `4300%` lightning range damage while active, `40401` `圣域` with `COOLDOWN` trigger, range `1100`, Lv1/Lv10 `300/1920`, and description text stating that allies in the field recover `300` HP per second, `60201` Lv1/Lv10 `6200/9800`, `60301` `将军怒吼` with `COOLDOWN` trigger, physical AOE, range `150`, Lv1/Lv10 `500/950`, and description text stating that it stuns surrounding enemies and raises party critical chance coefficient by `500%`, and `60601` `嗜血` with `COOLDOWN` trigger, range `900`, Lv1/Lv10 `4000/6700`, and description text stating that it consumes half of current HP and increases attack damage by `4000%` for a time; the `10501` page states `神圣之刃` is a cooldown buff with `攻击力+50%` and Lv1/Lv10 on-hit healing `20/200`, while the `60201` page states that killing an enemy with the hit creates a shockwave dealing `350%` damage to surrounding enemies. Inspected pages do not expose the hidden attack-count cadence, exact `20101` arrow count/timing, exact `20501` pierce line geometry/hit count, exact `20601` lodged-arrow duration/reset rules or bleed damage/duration, exact `50101` explosion radius/impact point/projectile timing/fire status/resistance rules, exact `50201` freeze duration/stacking/explosion radius/projectile timing/cold status/resistance rules, passive unlock paths/runtime application semantics, buff durations for `10401`/`10501`/`20401`/`40301`/`40401`/`60301`/`60601`, or exact stun duration/critical-coefficient formula for `60301`, so the current 3-hit cadence, the `20101` minimum two-arrow scaffold, the `20501` current-wave pierce scaffold, the `20601` lodged-arrow/bleed marker scaffold, the `50101` current-wave explosion scaffold, the `50201` current-wave freeze scaffold, passive effects, those buff durations and the `60301` stun timing remain placeholders. |
| Current macOS app source | Implemented behavior | High | Local repository is authoritative for current implementation. |

Additional refreshed Rune Tree check in this pass: `/zh/runes/` and `/en/runes/` both describe the Rune Tree as an account-wide passive system with 197 nodes, 195 connections, hero Level 3 unlock, and full reset support. The formation branch lists `Rune of Command (2nd hero — 50,000g)` and `Rune of Command (3rd hero — 150,000g)`, while the full rune table contains two max-level-1 command rows using `UnlockArrangeSlotCount.png`: Chinese `指挥符文` IDs `21` and `24`, English `Rune of Command` IDs `21` and `24`. The same formation branch lists `2nd Active Skill Slot (~50,000g)`, and the full rune table contains the matching max-level-1 `Rune of Awakening` / `觉醒符文` row with ID `27` and icon `UnlockSkillSlotCount.png`. The offline branch says `Offline — unlock and boost offline rewards (8h cap)` / `离线 — 解锁并提升离线奖励（上限 8 小时）` and lists `Rune of Repose (unlock offline reward)`, `Offline Gold +10%`, and `Offline XP +10%`; the full rune table contains the matching max-level-1 `Rune of Repose` / `安息符文` row with ID `11001`, next nodes `110011, 110012`, and icon `UnlockOfflineReward.png`, plus `Rune of Hoarding` / `储藏符文` ID `110011` using `OfflineRewardGoldPercent.png` and `Rune of Training` / `训练符文` ID `110012` using `OfflineRewardExpPercent.png`. The same full table includes multiple `Rune of Expansion` / `扩张符文` rows with `MaxInventorySlot` icon family, including source ID `22` in the formation branch. Current local `SourceRuneCatalog` preserves all 197 checked table rows, 195 checked `Next` edges, 11 sparse `Previous` references plus their exact sparse source-node map, the checked max-level distribution (`1:62`, `2:1`, `3:43`, `5:89`, `10:2`), the 39 referenced rune icon families and their checked per-family distribution as a data-only source catalog, while the runtime-unlockable `RuneTreeNode` enum remains limited to the 7 nodes with locally modeled behavior. Grok Search returned no independent sources for these active-skill-slot/offline/inventory-expansion rune names or reset/refund rules. Confidence remains Medium because this comes from one unofficial fan-Wiki source family. The branch text proves the two formation-slot effects/costs, that Rune of Awakening grants a second active skill slot with only an approximate `~50,000g` cost, that offline rewards are unlocked by Rune of Repose, and that first offline gold/XP boost effects are +10%; the full table proves a MaxInventorySlot expansion-node category exists, but it does not expose exact inventory-slot increments, costs, repeated-node stacking, reset price, refund behavior, point economy, exact active-skill-slot cost, full active skill loadout/equip semantics, per-node costs/effects for most of the 197 nodes, or full original reset semantics. The table alone does not prove the exact mapping of ID `21` versus ID `24` to the second or third hero slot, and neither branch nor table exposes exact gold/point costs for Rune of Repose, Rune of Hoarding, Rune of Training, or Rune of Expansion.

Additional refreshed item page checks in this pass: `/zh/items/` and `/en/items/` both list 20 equipment types, 5,760 gear items, 396 gear level progressions, 174 material/chest rows, and the same 10-grade rarity distribution totals. Direct checks of all 20 English type pages under `/en/items/gear/{type}/` expose each type's aggregate gear count, per-rarity count distribution, level progression names, base item IDs and icons. Direct parsing of the live `v1.00.09` item page tables exposes 115 material rows across Decoration 36, Engraving 33, Inscription 10, Crafting 22, Offering 10 and Soul Stone 4, plus 59 stage chest rows across Common 19, Rare 29 and Legendary 11. The same HTML exposes 115 unique material image paths under `/assets/tbhdb/game/items/materials/Item_<ID>.png` and 3 stage-box family image paths under `/assets/tbhdb/game/items/boxes/Item_910011.png`, `Item_920011.png` and `Item_930011.png`. The local `SourceItemCatalog` now preserves those 20 type-page rows, the checked aggregate total of 5,760 gear entries, the checked 396 level progression IDs, the checked 115 material rows, the checked 59 stage chest rows, and aggregate rarity counts such as Common 320, Uncommon 760 and Cosmic 320. The resource bundle now also carries those 115 checked material icons as `source_material_*` plus the 3 checked chest-family icons as `source_stage_chest_*`; release resource self-test verifies their 16x16 and 64x64 source dimensions. This is still one fan-Wiki source family, so confidence remains Medium. These pages do not expose a complete per-rarity/per-affix 5,760-row item database, original stat rolls, drop tables, per-item variant IDs beyond the base progression IDs, exact material use/effect systems beyond their taxonomy, exact chest contents/rates, or the full original 5,760-item icon database, so the current catalog is an aggregate/source-progression/material/chest baseline rather than exact item-record parity.

Additional local equipment-icon check in this pass: the previous `item_0_0.png` through `item_3_4.png` runtime resources were clean by size and transparent-corner checks, but still visually embedded 28x28 inventory UI tiles containing grid edges, adjacent item fragments and panel backgrounds. The current asset generator now preserves the 20 checked equipment-type categories as 20 independent `32x32` transparent pixel icons instead of directly cropping `ss_02.jpg` inventory slots into runtime art. `GameArt.itemIconName(for:)`, `SelfTest`, `InventoryTests`, and `ResourceSelfTest` now require every `EquipmentType` to resolve to its own `item_*` asset, and `ResourceSelfTest` rejects suspicious near-solid 32x32 tiles by visible-pixel coverage so UI-tile crops cannot silently return. This is a local readability and packaging-quality improvement for the type-level item scaffold; it does not prove exact original per-item icon art, full per-rarity/per-affix icon variants, or the original 5,760 item records.

Additional refreshed skill-page checks in this pass: `50401` `充能陷阱` / `Charge Trap` has matching Chinese and English Wiki pages listing cooldown activation, physical damage, trap delivery, range `1150`, Lv1/Lv10 `1000/5500`, and description text that traps explode when struck by elemental damage. `50501` `弩炮塔` / `Crossbow Turret` has matching Chinese and English Wiki pages listing cooldown activation, physical damage, projectile/summon delivery, range `1100`, Lv1/Lv10 `1750/3190`, and description text that an automated crossbow fires bolts dealing `1750%` physical damage. `50601` `电击弩箭` / `Shock Bolt` has matching Chinese and English Wiki pages listing attack-count activation, lightning damage, projectile delivery, range `1100`, Lv1/Lv10 `2700/4500`, and description text that the bolt lodges in an enemy and briefly releases lightning currents dealing `2700%` lightning damage to nearby enemies. This is still a single fan-Wiki source family, not an independent official confirmation. The pages do not expose trap duration, exact trap-count semantics, explosion damage formula, trigger source, placement, radius, resistance interaction, maximum active traps, turret duration, turret firing cadence, target selection, projectile travel, support-member turret state or summon replacement/stacking rules, Shock Bolt current duration, current tick cadence, nearby-target geometry, lodged-target retention, lightning resistance/status behavior or exact attack-count cadence.

Additional refreshed Slayer check in this pass: `60101` `猛击跳跃` / `Slam Jump` is present at `/zh/skills/active/60101-slam-jump/`, `/zh-tw/skills/active/60101-slam-jump/`, `/ja/skills/active/60101-slam-jump/`, and `/en/skills/active/60101-slam-jump/`. The structured page fields list cooldown activation, physical damage, melee/AOE delivery, range `850`, Lv1/Lv10 `3100/5350`, and order `400`. The Chinese, Traditional Chinese and Japanese descriptions say the Slayer leaps to an enemy and creates a landing shockwave dealing `3100%` physical damage to surrounding enemies. The English description conflicts by describing a focused `3100%` hit plus a `350%` kill shockwave, which matches the already-checked `60201` `Crushing Blow` rider more closely. Confidence remains Medium because all pages are one fan-Wiki source family; the implementation follows the repeated structured fields and non-English descriptions while documenting the English text conflict. Exact leap travel timing, landing delay, AOE radius/geometry, target selection and physical resistance/falloff remain unverified.

Additional refreshed Knight check in this pass: `10201` `盾牌冲锋` / `Shield Charge` has matching Chinese, English and Japanese fan-Wiki pages listing cooldown activation, physical damage, melee delivery, range `900`, Lv1/Lv10 `3000/5700`, and description text that the Knight raises a shield, charges forward, and deals `3000%` physical damage to enemies collided with. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact charge path, travel timing, collision width/count, target ordering, knockback/stun behavior or physical resistance/falloff.

Additional refreshed Knight check in this pass: `10301` `报应打击` / `Retribution Strike` has matching Chinese, English and Japanese fan-Wiki pages listing attack-count activation, physical damage, melee delivery, range `150`, Lv1/Lv10 `1500/3300`, order `600`, and description text that the Knight strikes enemies multiple times, each strike dealing `1500%` physical damage, with more strikes as the hero's HP gets lower. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact attack-count cadence, HP threshold table, minimum/maximum strike count, retargeting behavior after overkill, physical resistance/falloff or support-member behavior.

Additional refreshed Knight check in this pass: `10101` `穿透突刺` / `Piercing Thrust` has matching Chinese, English and Japanese fan-Wiki pages listing attack-count activation, physical damage, melee/AOE delivery, range `200`, Lv1/Lv10 `2500/4300`, order `400`, and description text that the Knight thrusts the sword deeply forward to deal `2500%` physical damage to enemies in range. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact attack-count cadence, thrust path, collision width, maximum target count, target ordering, travel timing, physical resistance/falloff or support-member behavior.

Additional refreshed Knight check in this pass: Grok Search returned no sources for `Taskbar Hero 10401 Aegis Field blocks damage allies field`, so direct page checks used `/zh/skills/active/10401-aegis-field/`, `/en/skills/active/10401-aegis-field/`, and `/ja/skills/active/10401-aegis-field/`. The Chinese, English and Japanese pages match on cooldown activation, physical damage category, range/AOE delivery, range `150`, Lv1/Lv10 `500/1850`, order `600`, and description text that the field/aura blocks `500` damage for allies within the area. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose exact duration, field geometry, whether the `500` value is a shared pool or per-ally shield, whether downed allies are included, support-member field-cast state, damage-type interaction, stacking/replacement rules, or visual timing.

Additional refreshed Priest check in this pass: Grok Search returned no sources for `Taskbar Hero 40601 Revive skill details`, so the direct page checks used the Wiki sitemap and the `/zh/skills/active/40601-resurrection/`, `/en/skills/active/40601-resurrection/`, and `/ja/skills/active/40601-resurrection/` pages. The Chinese, English and Japanese pages match on cooldown activation, physical damage category, no delivery type, range `1100`, Lv1/Lv10 `300/750`, order `900`, and description text that it revives one fallen party member with `300%` of that member's max HP. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose target priority, whether the Priest can revive self, whether cooldown is held until a valid fallen target exists, independent party member HP/level/equipment formulas, over-max-HP cap behavior after revival, animation timing, or whether revived support cooldowns continue ticking while down.

Additional refreshed Priest healing check in this pass: Grok Search returned no sources for `Taskbar Hero 40101 Heal skill details` or `Taskbar Hero 40401 Sanctuary skill details`, so direct page checks used `/zh/skills/active/40101-heal/`, `/en/skills/active/40101-heal/`, `/zh/skills/active/40401-sanctuary/`, and `/en/skills/active/40401-sanctuary/`. The Chinese and English `40101` pages match on cooldown activation, physical damage category, no delivery type, range `950`, Lv1/Lv10 `100/280`, order `400`, and description text that it restores one ally by `100%` of that ally's maximum HP. The Chinese and English `40401` pages match on cooldown activation, physical damage category, range/AOE delivery, range `1100`, Lv1/Lv10 `300/1920`, order `700`, and description text that all allies inside the sanctuary regenerate `300` HP per second. This still comes from a single fan-Wiki source family, so confidence remains Medium. The pages do not expose `治愈` target priority, whether full-health allies are skipped before cooldown consumption, `圣域` duration, exact field geometry, support-member independent HP/equipment/skill-level formulas, or support-member field-cast state.

Additional refreshed guide interaction check in this pass: Grok Search returned no indexed snippets for the guide query, so direct page-source checks used `/zh/guide/` and `/en/guide/`. The Chinese guide says to use `Alt+点击` to lock any item the player wants to keep before Alchemy, and the English guide says `Alt+Click to lock anything you want to keep first`. This still comes from a single fan-Wiki source family, so confidence remains Medium. The macOS app maps that original `Alt+Click` affordance to an `Option`-click item-grid action because Option is the macOS Alt key; exact original lock icon styling, modifier-key handling on Windows, and broader item safety UX remain unverified.

Additional refreshed skill-catalog check in this pass: direct page-source parsing of `/en/skills/` and `/zh/skills/` found 106 active/base/monster skill rows in both languages, 106 unique IDs in each page, matching ID order, and matching technical fields (`data-id`, `data-activation`, `data-damage`, `data-delivery`, `data-range`). The extracted source distribution is `BASEATTACK:58`, `BASEATTACK_COUNT:11`, `CONTINUOUS:2`, `COOLDOWN:35`; damage types are `Physical:77`, `Fire:12`, `Chaos:8`, `Cold:5`, and `Lightning:4`; delivery values include empty source delivery, `AOE`, `Melee`, `Melee, AOE`, `Projectile`, `Projectile, AOE`, `Projectile, Summon`, and `Trap`. This is now preserved in `SourceSkillCatalog` as a data-only source catalog. It does not imply that all 106 skills execute in combat: only the 36 named hero active skills currently have runtime behavior and 10-level value tables in `HeroSkills`, while base attacks and monster skills remain source-catalog entries until their formulas, cadence, targeting, status effects, and animation/audio behavior are verified.

## Local Gameplay Coverage Audit

Run:

```bash
scripts/audit-local-gameplay-fidelity.sh
```

Local item icon art audit:

```bash
scripts/audit-local-item-icons.sh
```

The script is a local source-coverage guard, not an external fact source. It reads the current Swift model files plus `BattleView.swift` and compares them to the already-recorded Steam/Wiki baselines so regressions in currently modeled gameplay scaffolds and their visible battle feedback are caught while known incomplete areas remain explicit gaps. Current output: 6/6 hero classes, 106/106 active/base/monster source skill catalog rows, 36/106 runtime-modeled named hero active skills, 36/36 runtime active-skill value tables, 108/108 passive skill catalog rows, 197/197 checked Rune Tree source rows, 195/195 checked Rune Tree source `Next` edges, 5/5 checked Rune Tree `Next` out-degree distribution keys, 11/11 checked sparse Rune Tree source `Previous` refs, 7/7 checked sparse `Previous` source-node entries, 5/5 checked Rune Tree max-level distribution keys, 197/197 checked Rune Tree icon-family distribution rows, 7 runtime-unlockable Rune Tree nodes, 4 modeled local Rune Tree prerequisite edges, 3/3 Acts, 30/30 display stages, 120/120 difficulty-stage runtime rows, 120/120 stage-composition rows, 49 unique stage-composition monster names, 4/4 difficulty tiers, 10/10 rarity grades, 20/20 equipment types, 20/20 checked gear type pages, 5,760/5,760 checked per-type aggregate gear entries, 396/396 checked base item level progression IDs, 5,760/5,760 checked per-type rarity-count totals, 115/115 checked material rows, 6/6 checked material categories, 59/59 checked source stage chest rows, 0/5,760 exact per-rarity/per-affix item records, 59/59 current chest catalog rows, 3/3 chest families, 4/4 Soul Stone kinds, the checked 9-item Synthesis input count, 16 compact player-side battle status badge cases, 14 active buff/summon/trap status mappings, 2/2 source `CONTINUOUS` Priest blessing status mappings, and 3 deployable field markers. It also prints source skill activation/damage/delivery distributions, runtime modeled skill source coverage, modeled runtime skill activation types (`baseAttackCount`, `continuous`, `cooldown`), damage elements (`physical`, `fire`, `cold`, `lightning`, plus `none` for utility skills), runtime deliveries, six runtime active skills per hero-class prefix, passive value types (`ADDITIVE`, `FLAT`), 30 passive stat kinds, 18 passive rows per hero-class prefix, source Rune Tree icon-family coverage, next-edge out-degree distribution, previous-ref count, exact sparse previous-reference map, max-level distribution and icon-family distribution, source gear rarity-count totals, source material category/rarity totals, source stage chest rarity totals, source Soul Stone IDs, current local Rune Tree prerequisite edges, player status badge cases, active status skill names, continuous status skill names (`力量祝福`, `守护祝福`), and deployable marker skill names. This gives future gameplay work a repeatable baseline for deciding whether an edit expands fidelity or silently erodes an already-modeled part.

The item icon art audit is a local visual-resource guard, not an external fact source. It parses `EquipmentType` and `GameArt.itemIconName(for equipmentType:)`, then checks that the 20 source equipment types resolve one-to-one to 20 packaged `item_*` PNG files. It verifies `32x32` RGBA dimensions, transparent corners, nonblank and non-solid visible-pixel coverage, no duplicate pixel payloads, no missing mapped resources, and no unmapped `item_*` extras. This catches the specific regression where runtime equipment icons become cropped inventory UI tiles with grid edges or adjacent item fragments, while keeping the documented limitation that these are clean type-level placeholders rather than original per-item icon parity.

## Original Gameplay Baseline

| Area | Original Evidence | Current macOS State | Gap |
| --- | --- | --- | --- |
| Core loop | Deploy party, auto fight waves, collect gold/XP/equipment/chests, use Alchemy/Synthesis/Cube, invest Rune Tree/skill points, push stages and Act Bosses. | A primary hero now leads a three-slot party scaffold; the settings Rune Tree path still spends checked Rune of Command formation-slot costs (`50,000G` and `150,000G`) after the Lv.3 Rune Tree gate, while the character panel now also offers direct support-slot unlock buttons that spend the same checked formation gold without requiring navigation through the Rune Tree UI. It earns aggregated XP/gold and retained loot from defeated encounters, advances through the new Act/stage/difficulty scaffold by the number of cleared encounters, grants source-mapped Normal + Stage Boss chests from non-Boss stage clears and Normal + Act Boss chests from X-10 Boss clears, records verified per-stage monster composition metadata, and gates X-10 Boss stages behind Soul Stone counts from opened chests. The active battle now maintains per-enemy HP/defeat state for the current wave, lets every alive enemy attack on its own cooldown, keeps the first alive enemy as the focused target for hero/support attacks, and exposes the live enemy group to the UI. A DEBUG `GameEngine.runSelfTestTick()` hook lets `TBH --self-test` drive the same private runtime tick used by the timer; the `[GameEngine runtime]` suite now proves a live tick can produce combat events, win the battle, settle mined XP/gold, advance encounter progress, accumulate play time, record statistics, play victory SFX, keep loot SFX consistent with retained loot, and start the next battle. Soul Stone metadata now uses the item database's four material IDs and rarities, and opened chests no longer promote Soul Stone type by item level. The local chest catalog now covers all 59 Wiki item-page stage-box rows; the stage reward path still filters to the 51 box IDs that the current drops stage table actually maps to stages, so duplicate early `Stage Boss Box` catalog IDs do not overwrite checked stage rewards such as `1109 -> Stage Boss Box 6 #920022`. The settings UI now lists concrete saved chest instances by source family/database ID/stage source and can open a specific `Normal Monster`, `Stage Boss`, or `Act Boss` chest instead of only opening the first chest in a difficulty bucket. Inventory now has conservative Cube, Alchemy and Synthesis entries: unlocked items can be consumed into a persisted `CubeProgress` total using the checked rarity `Cube XP` values, converted to hero gold using the checked rarity `Alchemy value` table, or combined as 9 unlocked same-rarity backpack items into 1 next-rarity item. Locked items are protected from discard/Cube/Alchemy/Synthesis, and the item grid now maps macOS `Option`-click to the source guide's `Alt+Click` lock toggle before selection handling; `TBH --self-test` guards that interaction mapping. The first modeled `Rune of Expansion` / `扩张符文` source row with `MaxInventorySlot` category now increases local backpack capacity by a conservative `+10` scaffold after the modeled formation prerequisite, without claiming exact original cost or stacking semantics. Reset clears the local Cube progress. Settings now exposes a conservative unlocked-stage selector plus a current-stage retry action; the tracker keeps a high-water unlocked stage so revisiting earlier stages does not erase forward progress. `SourceRuneCatalog` now preserves the checked 197-row Rune Tree table as source data, but most nodes are not executable runtime effects yet. | Missing exact original simultaneous combat timing/targeting, full Cube leveling/reward/effect semantics beyond checked XP accumulation, full Alchemy UI/context/rate semantics beyond checked rarity gold conversion, full Synthesis skip/failure probabilities and Synthesis-level item-level downgrade semantics beyond the checked 9-to-next-tier scaffold, runtime costs/effects/layout/point economy for most of the 197-node Rune Tree beyond the currently modeled seven nodes, exact Rune of Expansion cost/slot increment/stacking/layout semantics, full skill-point tree, exact Soul Stone box drop rates, exact chest contents/rates, exact box drop probabilities/conditional timing beyond the current source-mapped clear rewards, exact original item-lock visual styling/modifier handling beyond macOS Option-click mapping, and exact original stage-selection UI/autofarm/retry semantics beyond the current high-water selector. |
| Party and heroes | Six heroes: Knight, Ranger, Sorcerer, Priest, Hunter, Slayer. Up to three deployed; second and third slots unlock through Rune Tree. | `HeroClass` exposes the six original classes with role/grade/base stats and art mapping. `HeroParty` persists three candidate slots with a default Knight/Priest/Ranger lineup and starts with one deployed slot. Settings still exposes the checked Rune Tree route for slots 2/3 after Lv.3 using checked `50,000G`/`150,000G` Rune of Command formation costs; the character panel now also lets players directly unlock locked support positions from the party rows, spending the same checked formation gold and, when unlocking position 3 from a one-slot party, opening position 2 at the same time so the party remains contiguous. Unlocked non-primary members emit visible per-member support attacks and rotate through their own class cooldown skills using checked Lv1 skill values. `SourceRuneCatalog` preserves the checked 197-node / 195-`Next` / 11-sparse-`Previous` Rune Tree source table, while `RuneTree` currently executes the checked `Rune of Command`, `Rune of Awakening`, `Rune of Repose`, `Rune of Hoarding`, `Rune of Training`, and first `Rune of Expansion` / `MaxInventorySlot` scaffold subset only. `RuneTree` models `Rune of Awakening` as a Lv.3-gated second active skill slot with approximate-cost labeling only, and runtime battles receive that slot count instead of enabling every modeled active skill at once. The first modeled `Rune of Expansion` is also Lv.3-gated, requires the modeled second party-slot prerequisite, and raises local backpack capacity from 50 to 60 as a conservative runtime scaffold because the checked source row proves category but not exact cost or per-node increment. Active skill slots now read a persisted per-class loadout, so the character panel can choose which checked active skill occupies each unlocked slot; invalid/old selections fall back to the source-order defaults. The settings panel can reset currently modeled Rune Tree nodes, re-lock party, active skill slot and inventory expansion state, refresh battle state, and refund only the locally checked formation gold costs. | Need runtime behavior for the rest of the 197-node Rune Tree source catalog, non-formation costs/effects, exact reset price/refund/point semantics beyond the currently checked local formation-cost refund, exact point/gold economy beyond the two checked formation command runes and approximate-cost `Rune of Awakening`, exact Rune of Expansion cost/capacity-increment/stacking rules, exact original active skill equip UI/order/cost constraints beyond the current persisted per-class slot picker, independent HP/equipment/skill levels per party member, exact support cooldown/attack timing, party-targeting effects, and exact original class balance. |
| Stages | Steam officially confirms 3 Acts and 4 difficulty tiers. Wiki gives the detailed 3 Acts x 10 stages x 4 difficulties = 120-stage shape, Normal/Nightmare/Hell/Torment labels, X-10 Act Boss/Soul Stone rules, per-stage levels, waves, kill counts, gold, XP, monster/Boss names, Boss HP, monster compositions, and item-level thresholds. | The app keeps the 30 display stages for navigation, but now attaches a 120-row mined runtime table keyed by difficulty/act/stage. Progress bars and advancement use the mined kill count, spawned monsters use mined monster/Boss names and HP, and the active monster name is selected by scaling the stored composition weights across the mined kill target instead of repeating the final composition entry when counts differ. `StageEncounterState` exposes current encounter, wave number, wave count and wave-local encounter progress from the mined data, while `StageEncounterPlan` materializes the full per-stage encounter list and wave buckets. `GameLoop` now starts a battle with all remaining encounters in the current wave, `Battle` keeps those monsters alive together with per-enemy HP/defeat state, aggregates rewards/loot as each enemy dies, and `ProgressTracker` advances by the cleared encounter count. `ProgressTracker` also exposes unlocked stage selections up to the current high-water stage, rejects future locked stages, and can restart the current stage by clearing encounter progress; `GameEngine` rebuilds the active battle after either action. The battle/settings UI surfaces the current wave, wave-local progress, enemy, highest unlocked stage, target-stage picker and retry action. Rewards use mined gold/XP, Boss HP also comes from the same mined table, offline rewards use the current difficulty row, regular monster drops and chest item rewards use the stage item-level threshold, stage clear now persists both source-mapped chest families for that stage (`Normal Monster` + `Stage Boss`, or `Normal Monster` + `Act Boss` on X-10), and Boss Soul Stone gates still consume only on successful clear. | Still missing exact original simultaneous combat timing/targeting, exact monster ATK/DEF formulas, exact chest/box Soul Stone drop rates, exact chest contents/rates, exact box drop probability rules, exact original stage-selection/autofarm behavior, and exact visual assets for every listed monster. |
| Combat stats | Wiki lists 63 tracked stats, ordered flat/additive/multiplicative bonuses, dodge/block/resistance/armor/damage reduction/absorption/floor, status effects, elements and delivery types. | Basic ATK/DEF/SPD/crit HP model with simple damage calculator. The modeled active skills now carry structured damage-element metadata (`physical`, `fire`, `cold`, `lightning`) and delivery metadata (`melee`, `projectile`, `rangeAOE`, `trap`, `summonProjectile`, heal/buff/resurrection categories). Runtime charged-trap triggering now reads this metadata instead of a hard-coded skill-id list, so elemental-trigger behavior has a concrete data hook. The checked `穿刺射击` and `旋转斧` bleeding scaffolds now feed battle status UI: once 3 lodged arrows mark a target bleeding, or a `旋转斧` tick hits a live enemy, the active enemy and side-enemy views expose a red status badge guarded by self-test. The current cold-delay scaffolds for `冰球术`, `暴风雪` and `寒霜弩箭` now also feed compact enemy status badges (`寒冷` / `冻结`) so visible combat feedback matches the already-modeled slow/freeze stand-ins. The current `将军怒吼` stun-delay scaffold now feeds a compact `眩晕` enemy status badge. | Need expanded stat model before high-fidelity combat: full resistances, armor, dodge/block, exact stun/cold/freeze/bleed durations, additive/multiplicative stat ordering, exact status damage/expiry rules and exact damage pipeline remain unimplemented. |
| Items | 10 rarity grades: Common, Uncommon, Rare, Legendary, Immortal, Arcana, Beyond, Celestial, Divine, Cosmic. 20 gear types across weapon/offhand/armor/accessory categories, materials, boxes, D/E/I slots, Alchemy value, Cube XP, and Synthesis. | The 10-grade rarity ladder is modeled with Chinese labels, original-style colors, Alchemy value, Cube XP, D/E/I slot metadata, and next-tier Synthesis progression. The item model now carries the 20 source equipment types and persists `itemLevel` instead of only embedding it in description text: Sword/Bow/Staff/Scepter/Crossbow/Axe, Shield/Arrow/Orb/Tome/Bolt/Hatchet, Helmet/Armor/Gloves/Boots, and Amulet/Earring/Ring/Bracer. `SourceItemCatalog` now preserves all 20 checked gear type pages, their aggregate 5,760 gear-entry count, per-type rarity distributions, 396 checked base level progression IDs/names such as Sword `300001 Long Sword` through `300020 Radiant Sword`, 115 checked material rows across six material categories, and 59 checked stage chest rows. These map to 10 macOS loadout slots with item locking, auto-equip, live comparison preview, persisted chest counts, manual opening for individual saved chest instances, stage-threshold-capped monster drops, a first Cube infusion scaffold that consumes unlocked backpack items into a saved Cube XP total, a first Alchemy action that consumes unlocked backpack items for checked rarity gold, and a first Synthesis action that consumes 9 unlocked same-rarity backpack items into 1 item of the next rarity tier while preserving the highest consumed item level. The Synthesis preview now uses the same input-selection rule as runtime synthesis, shows unlocked/locked same-rarity material counts, shows the guaranteed output item level when the checked 9-item recipe is ready, and labels the still-unverified skip-tier/item-level downgrade variants instead of inventing probabilities. Runtime item art now prefers `Item.equipmentType` and maps all 20 equipment types to independent clean `32x32` transparent `item_*` icons, with `ResourceSelfTest` and `scripts/audit-local-item-icons.sh` guarding one-to-one mapping, transparent corners, non-solid visible-pixel coverage and duplicate payloads so UI-tile crops cannot silently return; `SourceItemCatalog` material rows map to 115 source `source_material_*` icons, `SoulStoneKind` uses the four checked Soul Stone material icons, and Settings chest rows use the three checked source chest-family icons instead of a single generic box. Unknown non-equipment items still retain the older generic `official_item_*` fallback. Old generic accessory saves migrate to Ring. | Need exact per-rarity/per-affix 5,760 item database rows beyond type-page aggregate counts, exact material use/effect systems beyond the checked taxonomy, full Synthesis probability/skip/failure table, Synthesis level costs and item-level downgrade formula, full Cube level/reward/effect execution beyond checked rarity XP accumulation, full Alchemy flow/context beyond checked rarity gold conversion, exact chest contents/rates, original per-item icon progression for every variant beyond the checked material/chest source icons, and original item stat affixes. |
| Skills | Wiki page lists 106 active/attack skills and 108 passive nodes. Skills include physical/fire/cold/lightning, melee/projectile/AOE/summon/DoT/trap delivery, buffs, heals, resurrection, continuous blessings, cooldowns, base attacks, and attack-count triggers. | `SourceSkillCatalog` now preserves all 106 checked active/base/monster rows as a data-only source catalog with ID, English name, activation, damage type, delivery and range; this includes base attacks and monster skills but does not make them executable combat logic. `HeroSkills` contains the 36 runtime-modeled named hero active skills, with six per class, activation metadata and the checked 10-level value tables from the individual skill pages. `PassiveSkills` now preserves all 108 checked passive rows as a data-only catalog with ID, English name, stat, value type and value, with 18 rows per class prefix; those passive nodes are not yet unlocked or applied to runtime stats. Runtime battles now restrict the main hero and support-member skill lists by the modeled active skill slot count and a persisted per-class `ActiveSkillLoadouts` selection. With no saved selection, the app keeps the earlier source-order fallback; with a selection, the chosen checked skills fill the unlocked slots and invalid/duplicate saved IDs are ignored or replaced by defaults. This is closer to original build/loadout play than the old first-N-only stand-in, but it is still not a verified copy of the original equip-selection UI or ordering rules; isolated self-tests still create max-slot battles when validating individual later-skill formulas. Main-hero cooldown skills execute from cooldown timers; attack-count skills such as `穿透突刺` and `粉碎强击` trigger after base attacks using the still-placeholder 3-hit cadence; continuous Priest blessings are modeled as metadata and now apply Lv1 source-value modifiers (`力量祝福` uses +500% attack as a 6.0x attack multiplier, while `守护祝福` uses +10 elemental resistance as a temporary 10% incoming-damage-reduction stand-in until the elemental resistance model exists). Damage skills now use Lv1 percent values such as `穿透突刺` 2500%, `天堂之怒` 4300% and `粉碎强击` 6200% instead of earlier placeholder multipliers; Priest `治愈` now restores one wounded living ally using that ally's own max HP and checked Lv1 `100%` value, with target priority represented by a most-wounded scaffold. `神盾领域` now creates an active party damage-shield buff: while active it applies the checked Lv1 `500` damage-block value as a consumable shield pool against incoming monster damage to the main hero and living support members, with duration still represented by the cooldown-length stand-in and field geometry simplified to the current party scaffold until exact rules are verified. `不屈意志` is now modeled as a lethal-damage rescue instead of a normal cooldown cast: when incoming monster damage would defeat the Knight, it triggers once for the current stage key, logs the skill, restores Lv1 `300%` max HP even above the normal max HP cap, and persists the consumed stage key so later battles in the same stage do not refresh it. `神圣之刃` now creates an active cooldown buff instead of an instant heal: it applies the checked +50% attack modifier and heals Lv1 `20` HP on hero attacks while active, with duration still represented by the current cooldown-length stand-in. `迅捷觉醒` now creates an active cooldown buff and applies its checked Lv1 `+500%` attack-speed modifier as a 6.0x attack-speed multiplier, also using the cooldown-length duration stand-in until exact duration is verified. `天堂之怒` now creates an active attack-damage buff instead of an instant direct hit: while active, later hero attacks apply the checked Lv1 `4300%` lightning range-damage value across the live wave, with duration still represented by the cooldown-length stand-in and range simplified to the current wave until exact geometry is verified. `圣域` now creates an active over-time healing buff: while active it applies the checked Lv1 `300` HP-per-second healing value to the main hero and living support members on battle ticks, with duration still represented by the current cooldown-length stand-in and field geometry simplified to the current party scaffold until exact rules are verified. `粉碎强击` now applies the checked kill rider: if the main hit kills its focused target, a `350%` attack shockwave damages the other alive enemies in the current wave. `将军怒吼` now creates an active crit-coefficient buff: while active it maps the checked Lv1 `+500%` party critical chance coefficient into a 6.0x crit-rate multiplier for main-hero and support attacks, delays all alive current-wave enemy cooldowns by at least one attack interval as a stun stand-in, and exposes that stand-in as a compact `眩晕` enemy status badge. `嗜血` now consumes half of the Slayer's current HP on activation and applies the checked Lv1 `+4000%` attack-damage value as a 41.0x active attack multiplier, with duration still represented by the cooldown-length stand-in. `快速装填` now creates an active attack-count buff: at Lv1 it grants +50% attack speed for the checked 3 hero attacks. Unlocked support members now also rotate through the `COOLDOWN` skills in their current active loadout, log those skill names as support actions, and route support skill casts through the skill-cast audio event. | Need exact original active skill equip UI/order/cost constraints beyond the current persisted per-class slot picker, exact skill-slot ordering, attack-count intervals, passive unlock paths and runtime stat application, exact base-attack and monster skill execution, exact buff durations for `神盾领域`, `神圣之刃`, `迅捷觉醒`, `天堂之怒`, `圣域`, `将军怒吼`, `嗜血`, and the remaining buffs, exact `治愈` target priority/cooldown behavior when no ally is wounded, exact `神盾领域` field geometry and shared-pool versus per-ally shield semantics, exact `将军怒吼` stun duration/stacking and critical-coefficient math, elemental resistance/status effects, exact resurrection target priority/self-revive/cooldown behavior, summoned entities, exact support-member independent skill levels/cooldowns/equipment, exact range geometry for `天堂之怒`, exact field geometry and support-member cast state for `圣域`, exact AOE geometry for “surrounding enemies”, and exact semantics for whether `不屈意志` resets on stage retry versus stage identity. The current source catalogs, Lv1 value use and passive catalog are data baselines, not a complete skill-level, base-attack, monster-skill or passive-effect system. |
| Offline | Wiki states offline rewards grant gold + XP only, no chests, capped at 8 hours, using last logged-out stage; the Rune Tree page also states that Rune of Repose unlocks offline rewards and that the first visible offline branch boosts are `Offline Gold +10%` and `Offline XP +10%`. | Offline rewards grant gold + XP only, cap at 8 hours, use the current stage rather than only the broad Act, and stay disabled until the Lv.3 Rune Tree gate plus `安息符文` / Rune of Repose unlock. `储藏符文：离线金币 +10%` and `训练符文：离线经验 +10%` now require Rune of Repose and independently apply 1.10x gold or XP multipliers. Because the checked page exposes these nodes/effects but not their exact costs, the macOS implementation labels their costs as unverified and does not invent gold prices. | Exact original offline formula, Rune of Repose / Hoarding / Training costs, repeated boost node stacking beyond the first checked +10%, and full offline branch reset/economy semantics are still not proven. |
| Controls | Wiki lists `Shift+F11` scale reset, `Shift+F12` recenter, `Alt+Left-Click` item lock. | macOS menu-bar UI has tabs and settings. Inventory now supports Option-click item locking, explicit lock/unlock buttons, lock badges, and discard protection for locked items. Settings now exposes a menu-bar translation of scale control: the panel can be scaled between `85%` and `125%` in deterministic `5%` steps, and `重置面板尺寸` returns it to the default `342x420` baseline guarded by self-test. Because the app uses `MenuBarExtra.window`, popover placement remains system-anchored to the status item rather than a freely positioned original taskbar window. | Need a stronger original capture before adding more keyboard-driven controls. `Shift+F12` recenter is currently not mapped because the macOS popover has no independent saved window position to recenter. |

### Current Skill Detail Addendum

- `10201` `盾牌冲锋`: The checked fan Wiki pages list cooldown trigger, physical damage, melee hit type, range `900`, Lv1/Lv10 `3000/5700`, and description text stating that the Knight raises a shield, charges forward, and deals `3000%` physical damage to enemies collided with. The macOS app now maps this to a current-wave melee collision scaffold using the checked Lv1 `3000%` value for both main-hero and support-member casts. Exact original charge travel timing, path/width, collision target count, target ordering, knockback/stun rules, physical resistance interaction and damage falloff remain unimplemented.
- `10101` `穿透突刺`: The checked fan Wiki pages list attack-count trigger, physical damage, melee/AOE hit type, range `200`, Lv1/Lv10 `2500/4300`, and description text stating that the Knight thrusts forward and deals `2500%` physical damage to enemies in range. The macOS app now maps this to checked Lv1 `2500%` physical melee range damage across alive current-wave enemies when the attack-count trigger fires. Exact original attack-count cadence, forward thrust path, range geometry, collision width, maximum target count, target ordering, travel timing, physical resistance interaction and falloff remain unimplemented.
- `10301` `报应打击`: The checked fan Wiki pages list attack-count trigger, physical damage, melee hit type, range `150`, Lv1/Lv10 `1500/3300`, and description text stating that it strikes multiple times, each hit dealing `1500%` physical damage, with more strikes as the hero's HP gets lower. The macOS app now maps this to focused melee multi-hit damage using the checked Lv1 `1500%` value per hit, with a monotonic HP-ratio scaffold: at least two strikes, increasing through lower HP buckets up to five strikes. Exact original attack-count cadence, HP thresholds, strike-count cap, retargeting after overkill, melee geometry, physical resistance interaction and support-member behavior remain unimplemented.
- `10401` `神盾领域`: The checked fan Wiki pages list cooldown trigger, physical damage category, range/AOE hit type, range `150`, Lv1/Lv10 `500/1850`, and description text stating that it deploys a protective field/aura that blocks `500` damage for allies within the area. The macOS app now maps this to a checked Lv1 `500` consumable party damage-shield pool that can absorb incoming monster damage for the main hero and living support members. Exact original duration, field geometry, shared-pool versus per-ally shield semantics, stacking/replacement behavior, damage-type interaction, downed-ally inclusion and support-member field-cast state remain unimplemented.
- `20101` `快速射击`: The checked fan Wiki page lists attack-count trigger, physical damage, projectile hit type, range `1150`, Lv1/Lv10 `1320/2400`, and description text stating that the Ranger continuously fires multiple arrows, with each arrow dealing `1320%` physical damage. The macOS app now maps this to checked Lv1 `1320%` physical projectile damage and emits a minimum two-hit focused-target volley when the attack-count trigger fires. Exact original arrow count, attack-count cadence, shot spacing, target retargeting rules after overkill, travel timing and physical resistance interaction remain unimplemented.
- `20201` `散弹射击`: The checked fan Wiki page lists cooldown trigger, physical damage, projectile hit type, range `1650`, Lv1/Lv10 `1620/3060`, and description text stating that it fires multiple arrows that track enemies, with each arrow dealing `1620%` physical damage. The macOS app now maps this to checked Lv1 `1620%` physical tracking-projectile damage, distributing one projectile hit to each alive current-wave enemy for both main-hero and support-member casts. Exact arrow count, projectile distribution, target priority, travel timing, physical resistance interaction and overkill/falloff behavior remain unimplemented.
- `20301` `箭雨`: The checked fan Wiki page lists cooldown trigger, physical damage, range hit type, range `1300`, Lv1/Lv10 `2150/4490`, and description text stating that the Ranger shoots many arrows into the sky to deal `2150%` physical damage to enemies in a wide range. The macOS app now maps this to checked Lv1 `2150%` physical range damage across alive current-wave enemies for both main-hero and support-member casts. Exact arrow count, fall timing, target distribution, impact geometry, physical resistance interaction and damage falloff remain unimplemented.
- `20501` `穿透之箭`: The checked fan Wiki page lists attack-count trigger, physical damage, projectile hit type, range `1200`, Lv1/Lv10 `2440/3880`, and description text stating that it fires a piercing arrow to deal `2440%` physical damage to enemies hit. The macOS app now maps this to checked Lv1 `2440%` physical projectile pierce damage across alive current-wave enemies. Exact original pierce line geometry, maximum pierce count, target ordering, travel timing, collision width, physical resistance interaction and falloff remain unimplemented.
- `20601` `穿刺射击`: The checked fan Wiki page lists attack-count trigger, physical damage, projectile hit type, range `1200`, Lv1/Lv10 `1000/3700`, and description text stating that it fires a deep-wounding arrow; each lodged arrow increases damage by `1000%`, and 3+ lodged arrows cause bleeding. The macOS app now tracks lodged arrows per enemy, scales the checked Lv1 physical projectile damage by the lodged-arrow count, marks the target as bleeding at 3 lodged arrows, and exposes that bleeding state as a compact red battle status badge on active and side enemies. Exact lodged-arrow duration/reset rules, bleed damage, bleed tick cadence, bleed duration, bleed stacking, target retention after stage/wave changes, physical resistance interaction and projectile travel remain unimplemented.
- `30101` `火球术`: The checked fan Wiki page lists cooldown trigger, fire damage, projectile/AOE hit type, range `950`, Lv1/Lv10 `2700/4950`, and description text stating that a fireball explodes on contact with an enemy and deals `2700%` range fire damage. The macOS app now maps the checked projectile/AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `2700%` value for both main-hero and support-member casts. Exact projectile travel, contact timing, explosion radius/positioning, elemental resistance interaction and fire-specific status rules remain unimplemented.
- `30201` `冰球术`: The checked fan Wiki page lists cooldown trigger, cold damage, projectile/AOE hit type, range `950`, Lv1/Lv10 `1500/2580`, and description text stating that an ice orb is hurled at enemies, slows enemies hit, and deals `1500%` multi-hit cold damage. The macOS app now maps this to two range-damage hit logs across all alive current-wave enemies, splitting the checked Lv1 `1500%` value across those hits, delays hit enemies' next attack tick as a slow stand-in, and exposes that stand-in as a compact `寒冷` enemy status badge. Exact projectile travel, multi-hit count/timing, slow amount/duration, cold resistance interaction, freeze/chill status rules and AOE geometry remain unimplemented.
- `30301` `闪电术`: The checked fan Wiki page lists cooldown trigger, lightning damage, AOE hit type, range `1050`, Lv1/Lv10 `2550/4980`, and description text stating that electricity is unleashed forward to deal `2550%` lightning damage to enemies. The macOS app now maps the checked AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `2550%` value for both main-hero and support-member casts. Exact forward electricity path, chain/line geometry if any, lightning resistance interaction, shock/status behavior and target falloff remain unimplemented.
- `30401` `烈焰九头蛇`: The checked fan Wiki page lists cooldown trigger, fire damage, projectile/summon hit type, range `1100`, Lv1/Lv10 `2300/3650`, and description text stating that it summons a hydra which fires fireballs dealing `2300%` damage. The macOS app now maps this to an active summon buff for the main Sorcerer: after activation, the hydra stand-in fires checked Lv1 `2300%` focused fire projectile damage on battle ticks. The page does not expose exact summon duration, hydra count, projectile cadence, target selection, travel time, fireball splash behavior, fire resistance interaction or support-member summon state, so those remain placeholders or unimplemented.
- `30501` `暴风雪`: The checked fan Wiki page lists cooldown trigger, cold damage, range hit type, range `1100`, Lv1/Lv10 `500/1940`, and description text stating that it summons many ice blocks, deals `500%` cold damage per second to enemies in range, and cools enemies. The macOS app now maps this to an active range damage-over-time buff for the main Sorcerer: while active, battle ticks apply checked Lv1 `500%` cold range damage per second across alive current-wave enemies, delay hit enemies' next attack tick as a cooling stand-in, and expose that stand-in as a compact `寒冷` enemy status badge. The page does not expose exact storm duration, ice-block cadence/count, cooling amount/duration, freeze/chill rules, field geometry, cold resistance interaction or support-member storm state, so those remain placeholders or unimplemented.
- `30601` `陨石打击`: The checked fan Wiki page lists cooldown trigger, fire damage, range hit type, range `1100`, Lv1/Lv10 `5500/9550`, and description text stating that it summons a meteor to deal `5500%` damage to enemies in a wide range. The macOS app now maps this to checked Lv1 `5500%` fire range damage across alive current-wave enemies for both main-hero and support-member casts. Exact meteor travel/fall timing, impact delay, impact geometry, fire resistance interaction, burn/status behavior and damage falloff remain unimplemented.
- `40101` `治愈`: The checked fan Wiki pages list cooldown trigger, physical damage category, no hit type, range `950`, Lv1/Lv10 `100/280`, and description text stating that it restores one ally by `100%` of that ally's maximum HP. The macOS app now maps this to a living-ally heal for main-hero and support-member casts, using each target's own max HP and choosing the most wounded living ally by missing-HP ratio as a scaffold. Exact original target priority, cooldown behavior when no ally is wounded, independent support-member HP/equipment/skill-level formulas and any overheal/cap edge cases remain unimplemented.
- `40401` `圣域`: The checked fan Wiki pages list cooldown trigger, physical damage category, range/AOE hit type, range `1100`, Lv1/Lv10 `300/1920`, and description text stating that allies inside the sanctuary recover `300` HP per second. The macOS app now maps this to an active over-time healing buff for the main Priest: while active, battle ticks heal the main hero and all living support members by the checked Lv1 `300` HP-per-second value. Exact original duration, field geometry, inclusion/exclusion rules for downed allies, support-member field-cast state and independent support-member HP/equipment/skill-level formulas remain unimplemented.
- `40601` `复活`: The checked fan Wiki pages list cooldown trigger, physical damage category, no hit type, range `1100`, Lv1/Lv10 `300/750`, and description text stating that it revives one fallen party member with `300%` of that member's max HP. The macOS app now keeps runtime HP/defeat state for support members, prevents defeated support members from attacking or casting, and maps `复活` to reviving the first fallen support member for checked Lv1 `300%` max HP. Exact target priority, self-revive rules, whether cooldown waits for a valid target, independent support HP/equipment/skill-level formulas, over-max-HP cap behavior, animation timing and downed-member cooldown ticking remain unimplemented.
- `50101` `爆炸弩箭`: The checked fan Wiki page lists attack-count trigger, fire damage, projectile hit type, range `1100`, Lv1/Lv10 `4840/9430`, and description text stating that the Hunter fires a bolt that explodes on hit and deals `4840%` range fire damage. The macOS app now maps this to checked Lv1 `4840%` fire projectile explosion damage across alive current-wave enemies when the attack-count trigger fires. Exact explosion radius, impact point, projectile travel timing, fire resistance interaction, burn/status rules, target falloff and attack-count cadence remain unimplemented.
- `50201` `寒霜弩箭`: The checked fan Wiki page lists cooldown trigger, cold damage, projectile hit type, range `1100`, Lv1/Lv10 `2100/3450`, and description text stating that the Hunter fires a bolt that explodes on hit, deals `2100%` range cold damage, and freezes hit enemies. The macOS app now maps this to checked Lv1 `2100%` cold projectile explosion damage across alive current-wave enemies for both main-hero and support-member casts, then delays hit enemies' next attack tick as a freeze stand-in and exposes that stand-in as a compact `冻结` enemy status badge. Exact freeze duration, freeze stacking, projectile travel, impact timing, explosion radius/positioning, cold resistance interaction, freeze/chill status rules and target falloff remain unimplemented.
- `50401` `充能陷阱`: The checked fan Wiki page lists cooldown trigger, physical damage, trap hit type, range `1150`, Lv1/Lv10 `1000/5500`, and description text stating that the Hunter launches traps that explode when struck by elemental damage. The macOS app now maps this to an active trap buff: activation arms one pending trap, and the next checked elemental skill hit from the party triggers `充能陷阱爆炸`, applying a checked Lv1 physical range-damage scaffold across alive current-wave enemies before consuming the pending trap. Exact original trap count semantics, trap duration, placement, explosion formula/radius, which elemental sources can trigger it, resistance interaction, maximum active traps and multi-trap detonation behavior remain unimplemented.
- `50501` `弩炮塔`: The checked fan Wiki page lists cooldown trigger, physical damage, projectile/summon hit type, range `1100`, Lv1/Lv10 `1750/3190`, and description text stating that the Hunter sets an automated crossbow that fires bolts dealing `1750%` physical damage. The macOS app now maps this to an active summon buff for the main Hunter: after activation, the turret stand-in fires checked Lv1 `1750%` focused physical projectile damage on battle ticks. Exact turret duration, firing cadence, target selection, projectile travel, bolt count, summon placement, physical resistance interaction, replacement/stacking behavior and support-member turret state remain unimplemented.
- `50601` `电击弩箭`: The checked fan Wiki page lists attack-count trigger, lightning damage, projectile hit type, range `1100`, Lv1/Lv10 `2700/4500`, and description text stating that the Hunter fires a crossbow bolt that lodges in an enemy and briefly releases lightning currents dealing `2700%` lightning damage to nearby enemies. The macOS app now maps this to a checked Lv1 `2700%` lightning projectile hit on the focused target, then activates an `电击弩箭电流` range damage-over-time scaffold across alive current-wave enemies. Exact current duration, tick cadence, nearby-target geometry, lodged-target persistence, projectile travel, lightning resistance/status behavior, support-member state and attack-count cadence remain unimplemented.
- `60101` `猛击跳跃`: The checked fan Wiki pages list cooldown trigger, physical damage, melee/AOE hit type, range `850`, Lv1/Lv10 `3100/5350`, and Chinese/Traditional Chinese/Japanese description text stating that the Slayer leaps to an enemy and creates a landing shockwave dealing `3100%` physical damage to surrounding enemies. The English page currently conflicts with a focused hit plus `350%` kill shockwave description, so this remains a documented source conflict rather than a second rider. The macOS app now maps the checked melee/AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `3100%` value for both main-hero and support-member casts. Exact leap travel timing, impact delay, landing radius, positioning, physical resistance interaction and damage falloff remain unimplemented.
- `60401` `大地强击`: The checked fan Wiki page lists attack-count trigger, physical damage, melee/AOE hit type, range `300`, Lv1/Lv10 `3700/5950`, and description text stating that it creates an earthquake dealing `3700%` physical damage and that earthquake rocks explode when hit by physical AOE attacks. The macOS app now maps the checked melee/AOE hit type to range damage across all alive current-wave enemies using the checked Lv1 `3700%` value. Exact rock spawn, rock HP/positioning, explosion trigger timing, explosion geometry and the physical-AOE interaction rules remain unimplemented.
- `60501` `旋转斧`: The checked fan Wiki page lists cooldown trigger, physical damage, melee/AOE hit type, range `150`, Lv1/Lv10 `1000/1720`, and description text stating that Axe Spin deals `1000%` physical damage per second to surrounding enemies, may make them bleed, and attacks against bleeding enemies deal extra damage. The macOS app now models the checked per-second range damage across alive current-wave enemies and marks hit live enemies with the existing bleeding status badge. Exact spin duration, bleed chance, bleed duration, bleed damage and the extra damage formula against bleeding enemies remain unimplemented.

## Original Art And Animation Baseline

| Area | Original Evidence | Current macOS State | Gap |
| --- | --- | --- | --- |
| Presentation | Steam confirms tiny taskbar idle RPG with adorable pixel heroes. Wiki and Steam media expose pixel heroes, monsters, gear icons, taskbar-themed presentation. The reproducible battle-scene audit records the official strip as 776x180, 30 fps, 184 frames, about 4.31:1 aspect ratio, with a low active lane, large dark negative space above it, a simple upper-left stage pill reading `1-6`, no decorative card frame, and a short lower warm platform whose sampled width ratio is `0.700` with x range `0.137-0.836`. | App uses extracted pixel sprites for hero, monsters, item icons, app icon. Battle view now follows the Steam battle-scene strip more closely: horizontal unframed arena, upper-left platform pill showing only the stage code, party on the left platform side, enemy side on the right, compact overhead HP bars, a large focused target, and smaller live wave enemies with mini HP bars behind it. The arena has been compressed from the previous tall about 1.8:1 popover battlefield to a 72 pt strip inside the expected 310 pt content width, giving about 4.31:1 like the official strip, with scene HP numbers removed, decorative border styling removed, local wave/progress counters removed from the in-scene stage pill, and the lower warm ground changed from a full-width band to a centered `0.707` width platform with dark side margins. The local audit script now supports both saved screenshots and a deterministic `TBH --render-battle-scene` SwiftUI render path; the current deterministic `620x144` render reports a `440x38` warm ground platform at `x=90`, with `warm_pixels=11576`, `ground_width_to_image=0.710`, an estimated local strip ratio of about `4.31:1` matching the official `4.31:1` source ratio, `flame_motion_pixels=1649`, `primary_hero_steel_pixels=3213`, a `122.3px` primary-to-support centroid gap, `stage_pill_text_pixels=99`, `stage_pill_dark_pixels=798`, `main_hp_pixels=732`, `support_hp_pixels=185`, `enemy_hp_frame_span=69.0`, `deployable_teal_pixels=452`, `impact_cold_pixels=1953`, and `trajectory_cold_pixels=1599`. The guard now also rejects regressions where the short central platform, upper-left platform pill, overhead HP bars, deployable marker, deterministic cold projectile trajectory or deterministic cold skill-impact cue disappear or drift out of the official-style lane. | Broad style direction is aligned. Need more exact class/monster/stage mappings, direct live screenshot capture is still permission/state-dependent, and frame-by-frame rendered comparison is still needed before claiming exact original crop, timing, sprite scale, lane anchors, HP bar dimensions or stage pill offsets. |
| Heroes | Wiki links official-style portraits for six heroes, and Steam's battle-scene media shows the Knight as a compact white-cloak, red-shield front-line combat figure rather than a gray armor stand-in. | Resource folder includes six `official_hero_*` UI sprites plus six edge-transparent `battle_hero_*` combat sprites. All six `battle_hero_*` files now use the corresponding complete `official_hero_*` transparent sprite as an identity-safe battle figure, preserving each class's recognizable silhouette after earlier screenshot/portrait trimming made some battle-tab heroes read as the wrong class. The legacy `battle_*` / `hero_*` broad screenshot crops remain reference-only because they include HP bars, background, effects or UI fragments. Self-test and release resource self-test both assert that battle hero mappings use dedicated transparent compact `battle_hero_*` resources; the release resource self-test also requires exact class-to-resource identity, unique hero/battle sprites, transparent corners, a fully transparent outer edge, registered source dimensions, reasonable visible-pixel coverage, raw-sample provenance against `official_hero_*` connected-frame removal, no embedded HP-bar green runs, visible Knight color markers and a connected Knight subject so swapped classes, empty transparent files, white-backed portraits, oversized screenshot blocks, HP-bar screenshot crops, fragmented equipment crops and undocumented visible-pixel edits are rejected. | Need exact original party spacing/scale audit, class-specific combat animation timing, and unobscured original battle-pose sprites for all six classes before claiming original combat-pose parity. |
| Monsters | Steam states 50+ monster types; Wiki stages reference many specific monster sprites. | The stage runtime table now preserves 49 monster/Boss composition names across the 120 listed stages. Extracted stage sprites cover the currently bundled exact IDs for core goblin/orc/skeleton/rat/beast/undead/Boss encounters, while the remaining composition names use explicit type-near mappings to existing insect, rat, kobold, golem, ghoul, archer, dragon and voidcaller art instead of silently falling back to slime. Self-test and release resource self-test both scan all 49 composition names; the release resource self-test also verifies each sampled battle monster sprite can be loaded from the package, rejects non-slime monsters that resolve to slime art, and pins the three Act Bosses to `stage_monster_skeleton_king`, `stage_monster_sibuna`, and `stage_monster_voidcaller`. | Need the remaining 50+ full monster roster beyond the stage table, exact per-name sprites for the approximate mappings, exact animation frame sets, hit/attack/death frames, and stage-specific scale/spacing tuning. |
| Animation | `scripts/audit-steam-battle-scene.sh` extracts and probes the official `extras/battlescene` MP4/WebM from Steam. Current evidence: video-only, 776x180, 30 fps, about 6.133s, 184 frames. Sampled frames show a left-to-right strip with top-left `1-6` stage pill, grounded combatants, overhead HP bars, a centered warm lava/fire platform with dark side margins, dark upper negative space, visible flame/background movement, and no central `VS` label. | Battle view now uses a similar single-lane composition, hit-triggered nudge/flash effects, floating damage text, compact HP bars above combatants, a low-rate animated pixel flame band in the warm platform strip, and a self-tested compact strip geometry (`BattleSceneMetrics`) that keeps the popover presentation near the official horizontal-strip ratio and platform width. Active `烈焰九头蛇`, `充能陷阱`, and `弩炮塔` states now also render as compact in-lane deployables between the party and enemy cluster, so the currently modeled summon/trap scaffolds have visible battle-scene presence instead of existing only in logs or the bottom status row. Damage logs now also drive compact in-scene trajectory and impact cues from `SkillDamageElement`, `SkillDelivery` and source-checked skill identity, covering projectile, range-field, summon-projectile, trap-arc, `盾牌冲锋` charge-dash, `猛击跳跃` leap-arc, `陨石打击` falling-meteor, `大地强击` ground-rupture and `粉碎强击冲击波` shockwave-ring trajectories plus physical slash, shockwave impact, earthquake impact, fire burst, meteor impact, cold burst, lightning spark, trap burst and summon-projectile hits without changing combat mechanics. Ordinary melee damage such as `穿透突刺` and the primary `粉碎强击` hit still avoid movement/shockwave trails, and ordinary fire range skills such as `火球术` still use the generic fire burst instead of the heavier meteor impact. Non-damage logs now drive separate heal, resurrection, shield and sacred-blade utility cues, with deterministic snapshot fixtures for `治愈`, `复活`, `神盾领域`, and `神圣之刃`. `BattleSceneSnapshot` renders the real SwiftUI `BattleSceneView` to an audit-ready PNG; self-test checks the rendered PNG signature, dimensions, short central platform ratio, distinct damage-cue fixtures and distinct utility-cue fixtures, plus a separate `playerStatusRow` fixture for the real bottom status row. The shell visual audit now also checks upper-left platform pill presence/position, overhead HP-bar presence, the deterministic deployable trap marker, fixed cold projectile/impact cues, explosive-bolt fire pixels, falling-meteor fire pixels plus vertical span, lightning-impact pixels, shield-charge dash pixels, slam-jump leap pixels, heal-pulse, resurrection-rise, shield-field and sacred-blade utility cue pixels in the player lane, and status-row non-dark/text/gold/teal/green pixels for modeled blessing/shield/trap feedback. | Need frame-by-frame timing comparison, exact attack/idle/hit/death keyframes, exact summon/trap/projectile/original impact sprites, exact charge/leap/meteor/shockwave/earthquake keyframes and travel timing, exact heal/revive/shield/blade utility keyframes, exact original buff/status bar layout, icon art, duration/stacking presentation, exact explosive-bolt and meteor impact delay/placement, exact lightning arc geometry, exact ground-rock spawn/explosion visuals, exact projectile/trap trigger animation, exact sprite scale, x/y lane anchors, HP bar dimensions and stage pill offsets measured against stronger original captures before claiming parity. |
| Taskbar integration | Original is a Windows taskbar docked mini window. | macOS app is a menu-bar extra with popover. | Product translation is intentional, but should preserve tiny glanceable status and avoid oversized menu-bar icon. |

## Original Audio Baseline

| Area | Original Evidence | Current macOS State | Gap |
| --- | --- | --- | --- |
| Audio presence | Direct `ffprobe` check of Steam's official Trailer HLS found AAC 48 kHz stereo audio streams. Direct checks of the page's embedded extras videos found video-only streams for the 776x180 battle-scene MP4/WebM, 776x200 interface MP4, 780x468 cube MP4 and 780x62 wishlist MP4, so those clips do not prove battle-specific SFX by themselves. `docs/steam-audio-audit.md` classifies the current Trailer audio technically as a continuous mastered promo mix: 47s, -15.3 LUFS integrated loudness, 4.6 LU loudness range, near-full-scale peak, and no sustained silence at -45 dB for 0.2s. | Current implementation packages original generated pixel-style SFX for game events and keeps system sounds only as a fallback. `scripts/audit-local-sfx.sh` now measures the local SFX set against broad technical boundaries and statically checks that all local audio events are routed by gameplay/UI code, with current RMS range `-19.5..-11.2 dBFS`, peak range `-11.1..-6.5 dBFS`, coarse spectral-centroid range `553..3173 Hz`, and runtime minimum-interval range `0.18-0.50s`. | Trailer analysis can guide broad loudness/energy for substitutes, and the local SFX now avoid obvious technical failures such as clipping, silence, overlong events, duplicate payloads, major event-profile drift, and missing runtime throttles that would make repeatable battle events stack into noise. It still cannot prove original per-event attack, hit, loot, equip, victory or defeat sounds. Exact original mix and per-event sounds still need in-game capture or authorized assets. |
| Event coverage | Expected game moments: attacks, crits, taking damage, victory, loot, defeat, level-up, equipment actions. | Added event-driven SFX for hero attack, crit, damage, victory, loot, defeat, level-up, equip, and preview. The app now prefers packaged, original generated, short pixel-style WAV files and falls back to macOS system sounds if a resource is missing. `ResourceSelfTest` derives the required resource list from `GameAudioEvent`, rejects extra unreferenced bundled WAVs, verifies representative `BattleEvent` routes through `GameEngine.audioEvent(for:)`, verifies each event WAV exists, is playable by `NSSound`, parses through `AVAudioFile`, is 16-bit PCM, is mono, uses 22050 Hz sample rate, stays within the same 0.05-0.75s duration bounds used by the local SFX audit, falls within the same broad RMS/peak dBFS bounds, validates each event's runtime playback volume against local category profiles, and validates each event's minimum playback interval against the local category profiles. `scripts/audit-local-sfx.sh` derives the expected file list from `GameAudioEvent`, checks for extra unreferenced WAVs, reports duration/channel/rate/bit-depth/RMS/peak/onset/zero-crossing/coarse spectral-centroid/high-low ratios for every event, verifies every `GameAudioEvent` case is played by runtime code, verifies all 7 `BattleEvent` cases route through the shared `GameEngine.audioEvent(for:)` mapping to attack/crit, skill/crit, damaged, victory, or defeat SFX, guards broad event profiles such as brighter/noisier critical hits, slower-onset skill casts, lower-pitched defeat, and longer level-up cues, and parses `GameAudioEvent.minimumInterval` to ensure every event has runtime throttling in the expected category range. `TBH --self-test` now verifies the same battle-event mapping, local playback volume profiles and local minimum playback interval profiles in Swift, drives a real tick-based victory path with an injected recording audio client, and proves preview, equip, chest opening, Synthesis, offline level-up, mute suppression, and re-enable behavior. The current files are 16-bit PCM mono WAVs at 22050 Hz, about `0.110-0.360s` long, with repeatable combat-event throttles at `0.18s`, inventory/preview throttles at `0.25s`, and victory/defeat/level-up throttles at `0.50s`. | The packaged WAV files are style-aligned substitutes, not original game audio assets. Exact original mix, loudness and per-event sounds still need in-game capture or authorized assets. |
| User controls | Original Steam page does not prove exact volume/mute controls from fetched evidence. | Added sound effects toggle and test button in Settings. | Volume slider can be added if direct evidence or product need justifies it. |

## Implemented In This Pass

- Added `GameAudio` service with event-driven macOS system SFX.
- Added `BattleEvent` emission for hero attack, crit, damage, victory and defeat.
- Added GameEngine audio routing for battle, loot, level-up and equip events, with battle-event routing centralized in `GameEngine.audioEvent(for:)`.
- Added a DEBUG-only `GameEngine.runSelfTestTick()` hook and `[GameEngine runtime]` self-test coverage for the real timer tick path: live battle update, combat SFX, victory SFX, mined XP/gold settlement, encounter progress, play-time/statistics update, retained-loot SFX consistency, and next-battle restart.
- Added `scripts/audit-steam-audio.sh` and `docs/steam-audio-audit.md` so official Steam Trailer audio checks are reproducible without storing official audio or derived clips in the repository.
- Added persisted `soundEffectsEnabled` setting with old-save default compatibility.
- Added Settings UI controls for `播放战斗音效` and `测试音效`.
- Updated self-test and Swift Testing coverage for sound setting persistence, runtime audio-event routing, preview/equip/chest/Synthesis/level-up event emission, and mute/re-enable behavior.
- Changed offline reward cap from 24 hours to the original 8-hour cap and added test coverage.
- Added six original hero classes, class role/grade/base stat metadata, matching sprite mappings, class selection UI, old `战士` save compatibility, and six named active skills per class as a data baseline.
- Added original stage scaffold: 3 Acts, 10 stages per Act, Normal/Nightmare/Hell/Torment, X-10 Boss flags, current-stage UI, stage-based monster scaling, and stage-based offline reward estimation.
- Added Soul Stone gating for X-10 Boss stages: persisted stone counts by difficulty, Boss lock UI, and stone consumption only on successful Boss clear.
- Added persisted chest inventory, source-mapped stage chest rewards, manual chest opening, Soul Stone grants from opened chests, chest item drops, and Settings UI controls for chest counts/opening.
- Expanded `Rarity` from the original MVP's 4 tiers to the original 10-tier ladder: Common/Uncommon/Rare/Legendary/Immortal/Arcana/Beyond/Celestial/Divine/Cosmic, with Chinese display labels, colors, Alchemy value, Cube XP, D/E/I slot metadata, updated item detail UI, and higher-tier placeholder loot rolls.
- Added persisted item locking: Option-click in inventory toggles lock state, selected item details expose a lock/unlock command, locked items show a lock badge, and locked items cannot be discarded. Old saves decode item locks as disabled.
- Added a macOS menu-bar equivalent for the original scale-reset control: Settings now exposes panel scaling from `85%` to `125%` in deterministic `5%` steps plus `重置面板尺寸`, which returns the `MenuBarExtra.window` popover to the default `342x420` baseline. This intentionally does not implement `Shift+F12` recenter, because the current macOS translation is system-anchored to the menu-bar status item and does not persist an independent window position.
- Added a persisted three-slot `HeroParty` scaffold with default Knight/Priest/Ranger candidates, party slot selection in the character panel, battle-scene party rendering, and real support damage/log/audio events from unlocked non-primary members. Old saves derive a party from the saved hero class.
- Added persisted `RuneTree` party-slot, active-skill-slot and first inventory-expansion nodes: new games start with one deployed slot, one active skill slot and base 50-slot backpack capacity; hero Level 3 gates the Settings Rune Tree route, checked Rune of Command costs (`50,000G` for slot 2 and `150,000G` for slot 3) unlock party slots 2 and 3 in order there, and approximate-cost `Rune of Awakening` unlocks the modeled second active skill slot without treating `~50,000g` as an exact verified cost. The first modeled `Rune of Expansion` maps to checked source row `22` / `MaxInventorySlot` and raises local backpack capacity by a conservative `+10` scaffold after the modeled party-slot prerequisite; exact original cost, per-node increment and stacking remain unverified. The character panel now also exposes direct support-slot unlock buttons that spend the same checked formation costs without requiring the player to open Settings first; directly unlocking position 3 from a one-slot party spends both missing formation costs and opens positions 2 and 3 together. Runtime `Battle` instances now receive `runeTree.activeSkillSlotCount`, so the main hero and support members no longer activate every locally modeled skill before the second slot is unlocked; the temporary loadout rule takes the first N checked skills by current local order. Locked party slots do not contribute support damage, and old party saves infer unlocked slots for compatibility. The settings panel now exposes a Rune Tree reset action for currently modeled nodes; it clears node state, re-locks party, active skill slot and inventory expansion state, refreshes active battle state, and refunds only checked local formation gold costs because the checked page proves reset support but not original reset price/refund/point semantics. The remaining runtime implementation of the full Rune Tree, non-formation rune costs/effects, exact active skill slot cost/equipment semantics, exact reset semantics and broader point/gold economy remain incomplete until verified against stronger original evidence.
- Added `SourceRuneCatalog` as a data-only baseline for the full checked Rune Tree table: it preserves 197 source rows, 195 `Next` edges, 11 sparse `Previous` references, Chinese and English rune names, the checked max-level distribution (`1:62`, `2:1`, `3:43`, `5:89`, `10:2`), checked `Next` out-degree distribution (`0:79`, `1:63`, `2:35`, `3:18`, `4:2`), sparse previous links, next links and 39 icon families from the refreshed `/zh/runes/` and `/en/runes/` pages. Self-test and the local gameplay audit now guard the source row count, connection count, next out-degree distribution, previous-reference count, exact sparse previous-reference map, max-level distribution, icon-family distribution, duplicate IDs, dangling next-node references, dangling previous-node references and the mapping from currently executable runtime nodes back to source IDs.
- Added the first non-formation Rune Tree nodes: `安息符文` / Rune of Repose now gates offline rewards, and its checked follow-up `储藏符文：离线金币 +10%` / `训练符文：离线经验 +10%` nodes independently raise offline gold or XP by 10%. The exact costs are not exposed by the checked evidence, so these nodes are shown as `成本待核对` and unlock without fabricated gold prices until stronger evidence is available.
- Expanded item taxonomy from five generic equipment slots to the 20 equipment types listed on the refreshed items page, grouped as 6 weapons, 6 offhands, 4 armor pieces, and 4 accessories. The runtime loadout now exposes 10 concrete slots, generated loot keeps its concrete type, auto-equip evaluates the expanded slots, item details show the type line, and legacy `饰品` saves migrate into the Ring slot.
- Replaced slot-level equipment icon fallback with type-level item art: `GameArt.itemIconName(for item:)` now prefers `Item.equipmentType`, maps all 20 modeled equipment types to independent clean `32x32` transparent `item_*` icons, keeps generic `official_item_*` art only as an unknown non-equipment fallback, and release resource self-test plus `scripts/audit-local-item-icons.sh` guard one-to-one mapping, cleaned dimensions, transparent corners, non-solid visible-pixel coverage and duplicate payloads. This is still a type-level placeholder, not the exact 5,760-item icon database.
- Added `SourceItemCatalog` as a data-only baseline for checked item pages: it preserves all 20 equipment type pages, aggregate 5,760 gear-entry counts, 396 base level progression IDs/names, per-type rarity distributions, 115 material rows across six material categories, and 59 stage chest rows. The resource bundle now includes the checked 115 material icons (`source_material_*`, 16x16) and 3 stage-box family icons (`source_stage_chest_*`, 64x64), Settings renders saved chests with the source family box icon, and Soul Stone counters render the checked material icon for each difficulty. This intentionally stops at source-page aggregate/progression/material/chest data because the checked pages do not expose the complete per-rarity/per-affix 5,760-row item database, stat affixes, exact material effects, or exact chest contents/rates.
- Verified official Steam media streams: the main Trailer HLS includes AAC audio, while the embedded battle-scene MP4 is video-only at 776x180/30fps. Reworked the macOS battle view toward that official battle strip: single horizontal arena, top-left stage pill, party cluster on the left, enemy on the right, overhead HP bars, floating damage, and larger bundled battle/boss sprites.
- Added `scripts/audit-steam-battle-scene.sh` and `docs/steam-battle-scene-audit.md` so the official Steam `extras/battlescene` media can be re-probed for stream metadata, frame count and sampled timeline frames without storing official video frames in the repository.
- Compressed the macOS battle arena toward the official `776x180` strip: `BattleSceneMetrics` now models the official 4.31:1 source ratio, the popover arena uses a 72 pt compact height and 310 pt expected content width for about 4.31:1, scene-level numeric HP labels were removed in favor of compact overhead bars, and the lower ground is a centered short platform using a `0.707` width ratio and `0.263` height ratio to preserve the official-style dark side margins and dark negative space above the lane.
- Removed the battle strip's visible rounded card stroke so the combat scene no longer contradicts the official sampled frames' lack of a decorative card frame. `SelfTest` now guards that battle-scene corner radius and border line width stay at zero.
- Simplified the in-scene stage pill to show only the stage display code, matching the sampled official `1-6` pill. The richer wave and encounter counters remain in the surrounding macOS header instead of inside the battle strip.
- Added `scripts/audit-local-battle-scene.sh` for local rendered screenshot checks. It opens the TBH menu-bar popover, captures a temporary screenshot, rejects black/blank captures, detects the warm battle ground strip, estimates the rendered strip ratio, verifies the official-style short central platform width, and also supports `SCREENSHOT_PATH=...` for analyzing saved local screenshots when live `screencapture` is unavailable. Current deterministic analysis uses a static opening battle fixture with a fixed `寒霜弩箭` damage log and reports a `440x38` warm ground platform at `x=90`, `warm_pixels=11576`, `ground_width_to_image=0.710`, an estimated `4.31:1` local strip ratio, `flame_motion_pixels=1649`, `primary_hero_steel_pixels=3213`, a `122.3px` primary-to-support centroid gap, `stage_pill_text_pixels=99`, `stage_pill_dark_pixels=798`, `main_hp_pixels=732`, `support_hp_pixels=185`, `enemy_hp_frame_span=69.0`, `deployable_teal_pixels=452`, `impact_cold_pixels=1953`, and `trajectory_cold_pixels=1599`.
- Added `TBH --render-battle-scene <path>` for deterministic SwiftUI battle-strip rendering. `scripts/audit-local-battle-scene.sh` now supports `RENDER_BATTLE_SCENE=1` and automatically uses the snapshot renderer when no running TBH process is available, while `SelfTest` guards that the renderer produces an audit-ready `620x144` PNG.
- Corrected battle-scene hero art mapping so all deployed hero combatants use dedicated edge-transparent compact `battle_hero_*` figures. All six battle hero files now preserve the corresponding complete `official_hero_*` figure as an identity-safe fallback, replacing the deterministic gray combat-lane template and later partial screenshot/portrait trims that could read as the wrong hero in the battle tab. The battle party layout places the primary hero on the left front lane with support heroes to the right so the first visible hero is no longer a support class. `SelfTest` and `ResourceSelfTest` guard that battle hero sprites resolve to dedicated transparent battle art, the release resource self-test rejects swapped class mappings, duplicate class sprites, oversized battle hero crops, opaque corners/edges, blank transparent sprites, suspiciously full screenshot blocks, embedded HP-bar green runs, registered-size drift, missing Knight color markers, fragmented Knight subjects and undocumented visible/alpha drift from the `official_hero_*` connected-frame-removal source, and the local battle-scene audit now rejects the pre-fix party layout where the support/portrait cluster sits too close to or ahead of the primary hero lane.
- Added compact player-side battle status badges in the Battle Tab bottom status row for the active state scaffolds that already exist in `Battle`: `神盾领域`, `神圣之刃`, `天堂之怒`, `烈焰九头蛇`, `暴风雪`, `迅捷觉醒`, `圣域`, `将军怒吼`, `快速装填`, `充能陷阱`, `弩炮塔`, `电击弩箭电流`, `旋转斧` and `嗜血`. The same status row now also exposes source-checked `CONTINUOUS` Priest blessings as `力量祝福` / `守护祝福` badges when those modifiers are equipped by the main hero or an unlocked support Priest. The badges show live shield and trap counters where those values exist, hide depleted shields and spent traps, keep source blessings first, and share a self-tested first-five plus `+N` folding rule with the SwiftUI row. This is visible feedback for current modeled active buffs, continuous blessings, summons and traps, not a claim that the original game's buff bar layout, icon art, exact duration or stack rules are fully replicated.
- Added deterministic visual auditing for the player-side battle status row. `BattleSceneSnapshot` now supports a `playerStatusRow` fixture that renders the actual `BattleOngoingStatusView` with Priest `力量祝福` / `守护祝福` continuous badges plus live `神盾领域` and `充能陷阱` counters, plus a `playerStatusRowCrowded` fixture that activates all currently modeled player-side statuses through the DEBUG battle buff path. `TBH --self-test` now guards the normal fixture's exact badge count/order (`力量`, `守护`, `神盾`, `陷阱`), verifies crowded-row folding, and confirms both status-row fixtures render distinct PNGs. `scripts/audit-local-battle-scene.sh` gates the normal row with current metrics `status_row_non_dark_pixels=9212`, `status_row_gold_pixels=160`, `status_row_teal_pixels=701`, `status_row_green_pixels=474`, and `status_row_light_pixels=1020`; it also gates the crowded row with `crowded_status_row_non_dark_pixels=12473`, `crowded_status_row_light_pixels=900`, and `crowded_status_row_overflow_light_pixels=85`, so the local checks catch blank, unreadable, color-regressed, order-regressed, or folded-overflow-regressed status feedback in addition to the static badge mapping checks.
- Added compact in-lane deployable visuals for active `烈焰九头蛇`, unspent `充能陷阱`, and active `弩炮塔` states. The deterministic battle-scene snapshot now activates those deployables through a DEBUG fixture while keeping the primary Knight party layout intact, and `scripts/audit-local-battle-scene.sh` rejects renders where the trap marker disappears from the expected mid-lane region. These are intentionally pixel-style placeholders until exact original summon/trap sprites, placement, projectile timing and trigger animations are captured.
- Added compact in-scene skill trajectory and impact cues driven by existing `BattleLogEntry.damageElement`, `delivery` metadata and source-checked skill identity. Current trajectory cues cover generic projectile, range-field, summon-projectile and trap-arc deliveries, plus dedicated Ranger arrow trails for `快速射击` multi-arrow volley, `散弹射击` tracking volley, `箭雨` falling arrows, `穿透之箭` piercing shot and `穿刺射击` lodged-arrow hit. Hunter bolt skills now also get source-text-aligned cues: `爆炸弩箭` renders an explosive bolt trail and impact, `寒霜弩箭` renders a frost bolt trail and cold explosion, `电击弩箭` renders a shock bolt trail/impact, and the derived `电击弩箭电流` log renders a current-arc trajectory plus current impact. They also cover dedicated charge-dash and leap-arc movement trails for `盾牌冲锋` and `猛击跳跃`, a dedicated falling-meteor trail for `陨石打击`, a ground-rupture trail for `大地强击`, and a shockwave-ring trail only for the derived `粉碎强击冲击波` log. Ordinary melee damage such as `穿透突刺` and the primary `粉碎强击` hit still do not render movement/shockwave trails. Current impact cues cover physical slash, shockwave impact, earthquake impact, fire burst, explosive-bolt impact, meteor impact, cold burst, frost-bolt impact, lightning spark, shock-bolt impact, shock-current impact, trap burst and summon-projectile hits; ordinary fire skills such as `火球术` still use the generic fire burst while `陨石打击` uses the heavier meteor impact. Non-damage logs now use separate utility cues instead of pretending to be impact/trajectory damage: `治愈` shows a heal pulse, `圣域` shows a sanctuary pulse, `复活` and `不屈意志` show a rising revive cue, `神盾领域` shows a shield field, `神圣之刃` shows a sacred-blade glow, and other modeled buff logs get a compact aura. The deterministic battle-scene snapshot appends a fixed `寒霜弩箭` damage log, and `scripts/audit-local-battle-scene.sh` now rejects renders where the cold projectile trajectory, cold impact cue, shield-charge dash trajectory, or slam-jump leap trajectory disappears from the battle lane. The latest deterministic audit measured `damage_shield_charge_pixels=2297` and `damage_slam_jump_pixels=1448`. These are visual feedback placeholders until exact original projectile/impact/movement/utility sprites, travel timing, arrow/bolt counts, explosion radii, current arcs, charge/leap keyframes, heal/revive keyframes and hit-frame captures are available.
- Expanded deterministic battle-scene fixture coverage for the existing compact cues: `BattleSceneSnapshot` can now render `trapBurst`, `summonProjectile`, `shockCurrent`, `shieldCharge`, `slamJump`, `earthquakeImpact`, and `shockwaveImpact` fixtures in addition to the earlier frost/explosive/meteor/lightning and utility fixtures. `scripts/audit-local-battle-scene.sh` now measures and gates `damage_trap_teal_pixels=847`, `damage_summon_fire_pixels=1812`, `damage_shock_current_pixels=2299`, `damage_shield_charge_pixels=2297`, `damage_slam_jump_pixels=1448`, `damage_earthquake_pixels=1612`, and `damage_shockwave_pixels=4782` in the current deterministic render. This makes local regression checks cover the visible `充能陷阱爆炸`, `弩炮塔`, `电击弩箭电流`, `盾牌冲锋`, `猛击跳跃`, `大地强击`, and derived `粉碎强击冲击波` cues, while preserving the existing limitation that exact original sprite art, projectile timing, charge/leap timing, ground-rock entities, shockwave radius/keyframes and hit-frame placement remain unverified.
- Added a low-rate animated pixel flame band to the battle scene backdrop so the macOS strip reflects the official battle media's visible fire/background movement without changing the compact 72 pt scene geometry or claiming exact frame-by-frame parity.
- Extended `ResourceSelfTest` from static monster file presence checks to a runtime stage-art scan: it walks every stage, difficulty and encounter through `StageDefinition.spawnMonster`, verifies all 49 mined composition names are sampled, ensures every resulting battle monster sprite loads from packaged resources, rejects non-slime monsters that fall back to slime art, and guards the three Act Boss sprite mappings.
- Added the first skill execution layer: main-hero skills now maintain cooldown timers inside `Battle`, damage skills emit named skill damage logs/events, Priest healing-style utility skills restore HP, non-damage utility skills log buffs, battle floating text/log rows show skill names, and `GameAudio` has a separate skill-cast event. Runtime skill execution is now limited by the modeled active skill slot count, with full-slot `Battle` construction reserved for isolated local skill formula tests. This is explicitly a scaffold, not the exact original 106-skill ruleset or final skill equip UI.
- Added `SourceSkillCatalog` as a data-only catalog for all 106 active/base/monster skill rows exposed by the checked `/en/skills/` and `/zh/skills/` pages. The local gameplay audit now reports `source_skill_catalog_entries 106/106` separately from `runtime_active_skills 36/106`, so source-data coverage can improve without falsely claiming that base attacks and monster skills execute with original semantics.
- Replaced the invalid `ss_03` background crops previously named `skill_*` with 12 real `ss_06` Rune Tree node icons and wired the character panel's skill list to show category icons through `GameArt.skillIconName(for:)`. These are source-cropped visual category icons for delivery/element types, not a claim that all 36 modeled active skills now have exact original per-skill icons.
- Added official activation metadata to named skills and split battle execution by trigger type: `COOLDOWN` skills rotate through cooldown timers, `BASEATTACK_COUNT` skills trigger after base attacks using a temporary 3-hit interval, `BASEATTACK` hooks are ready for future basic-attack skill rows, and `CONTINUOUS` blessings are represented without being spam-cast in combat. Priest `力量祝福` and `守护祝福` now apply as party-scoped continuous modifiers when they are equipped by either the main hero or an unlocked support Priest through the currently modeled active-skill-slot/loadout system, and those continuous modifiers now feed compact player-side status badges instead of remaining invisible.
- Added structured active-skill damage metadata for the currently modeled 36 named skills: physical/fire/cold/lightning elements plus delivery categories such as melee, projectile, range AOE, trap, summon projectile, heal, buff and resurrection. Charged Trap's elemental detonation check now uses that metadata instead of a hard-coded skill-id allowlist, while the broader original resistance/status/damage-pipeline model remains unimplemented.
- Replaced the runtime first-N active-skill-only stand-in with persisted per-class active skill loadouts: the character panel can choose checked skill IDs for each currently unlocked active slot, battles consume those selections for the main hero and support members, invalid/duplicate saved IDs fall back to source-order defaults, and reset clears the local loadout. Exact original skill equip UI, ordering restrictions and any cost/level gating beyond the currently modeled slot count remain unverified.
- Replaced runtime dependence on macOS system SFX with packaged, original generated, short pixel-style WAV files for all current audio events. `GameAudio` now loads those resources from the SPM bundle first and retains system sounds only as a fallback; `ResourceSelfTest` derives the SFX list from `GameAudioEvent`, rejects extra unreferenced bundled WAVs, verifies every event file is present, playable, AVFoundation-parseable, 16-bit PCM, mono, 22050 Hz, within the same 0.05-0.75s duration bounds used by `scripts/audit-local-sfx.sh`, within the same broad RMS/peak dBFS bounds, checks representative `BattleEvent` routes through `GameEngine.audioEvent(for:)`, checks every event's runtime playback volume profile, and checks that every event's `minimumInterval` stays in the local throttle profile range. The local audit currently reports 10 covered sounds, all 7 `BattleEvent` cases routed, `0.110-0.360s` duration, `-19.5..-11.2 dBFS` RMS, and `-11.1..-6.5 dBFS` peak.
- Expanded `scripts/audit-local-sfx.sh` beyond generic WAV metadata into event-profile checks. The audit now reports and gates onset time, envelope activity, zero-crossing rate, coarse spectral centroid, high/low-frequency energy ratios and duplicate WAV payloads, plus cross-event relationships such as critical-hit SFX being longer/brighter/noisier than the basic attack, skill-cast SFX having a slower onset than attack, battle-lost reading lower than battle-won, and level-up lasting longer than preview. Current deterministic output reports centroid range `553..3173 Hz` and passes all profile checks. These checks protect the generated substitute set from obvious regressions, but they are not evidence of exact original per-event SFX parity.
- Extended the local SFX audit to parse `GameAudioEvent.volume` and render deterministic runtime-volume sequence mixes for common short event clusters: `combat_burst` (`0.790s`, `-21.5 dBFS` RMS, `-14.0 dBFS` peak), `reward_chain` (`1.440s`, `-23.2 dBFS` RMS, `-16.0 dBFS` peak), and `defeat_chain` (`0.530s`, `-22.6 dBFS` RMS, `-14.0 dBFS` peak). This adds a repeatable check that generated event bursts keep useful headroom and audible energy relative to the official trailer baseline, while still treating them as generated substitutes rather than proof of original isolated attack, loot, victory or defeat SFX parity.
- Added Swift self-test coverage for `GameAudioEvent.minimumInterval`: every event must have a local throttle profile, stay in its category range, and keep victory/defeat/level-up cues spaced longer than repeatable attack/crit/skill/damage cues. This mirrors the shell audit's runtime-throttle guard inside the app's DEBUG self-test and remains a local regression check, not original cadence parity.
- Extended the release-safe `ResourceSelfTest` to cover representative `BattleEvent` audio routes, `GameAudioEvent.volume` profile ranges, the same `GameAudioEvent.minimumInterval` profile ranges and the combat-versus-terminal spacing relationship, so CI/package resource checks now catch missing/drifted battle SFX mappings, playback volumes or playback throttles even outside DEBUG self-test runs.
- Attached the refreshed stages page's 120-row runtime table to `StageDefinition`: each difficulty stage now carries mined level, waves, kill count, gold, XP, HP, monster/Boss name, and monster composition metadata. Progression uses mined kill targets, spawned monsters use mined HP and composition-selected names by encounter index, rewards use mined gold/XP, Boss HP comes from the same mined table, and self-tests assert the table count plus representative HP/composition/spawn-selection values.
- Added `StageEncounterState` and `StageEncounterPlan` so runtime combat exposes current encounter number, clear target, current wave, wave count, wave-local progress, composition-weight index, active composition monster, and the complete stage encounter list grouped by wave. Composition counts are now scaled across the mined kill target, which avoids over-repeating the final monster when the Wiki composition count total does not equal the stage's kill count. Battle and Settings UI now display current wave, wave-local progress and enemy state.
- Wired the encounter plan into runtime combat: `GameLoop` now starts battles with the remaining encounters in the current wave, `Battle` keeps that wave as simultaneous enemy states with individual HP/defeat flags and per-enemy attack cooldowns, victory rewards aggregate XP/gold/loot across the cleared encounters, and `ProgressTracker.advance(by:)` applies the cleared encounter count to stage progress.
- Exposed the active battle's current-wave enemy state (`enemyStates`, `activeEnemyState`, `aliveEnemyStates`, `waveMonsters`, `remainingWaveMonsters`, `upcomingWaveMonsters`) and rendered live side enemies as a small layered group with mini HP bars behind the focused target, with compact wave progress dots in the battle scene.
- Added the first exact stage monster/Boss idle sprites referenced by the refreshed stages table and mapped those monster IDs to bundled resources, replacing the earlier broad reuse of generic goblin/skeleton/insect/ghoul/golem sprites for those encounters.
- Added explicit approximate profile/art mappings for the remaining stage composition names that do not yet have exact extracted sprites, so non-slime encounters no longer degrade into the slime fallback. Self-test and Swift Testing now scan all 49 composition names and assert that only `史莱姆` may use slime art.
- Wired monster drops to the stage item-level threshold instead of deriving item level from XP reward, preventing high-XP later stages from producing runaway item levels outside the current original threshold table.
- Added Soul Stone item database metadata: Normal/Nightmare/Hell/Torment material IDs `190001`-`190004` and rarities Immortal/Arcana/Beyond/Celestial. Opened chests now grant the Soul Stone matching the chest difficulty bucket instead of using item level thresholds to jump to a higher difficulty stone.
- Replaced formula-generated chest IDs with explicit, checked source mappings for Normal Monster Box, Stage Boss Box and Act Boss Box entries. Non-Boss stage clears now grant both mapped Normal Monster and Stage Boss source chests; X-10 Boss clears now consume the required Soul Stone and grant mapped Normal Monster plus Act Boss source chests. `Stage Boss Box 6` at level 10 maps to `#920022` instead of the old formula's incorrect `#920101`. Exact chest contents, drop rates and probability rules remain intentionally unimplemented.
- Updated chest opening from a difficulty-bucket-only UI toward the source box families: saved chests can now be opened by concrete chest ID, and Settings lists each saved chest with its family display name, database ID, difficulty bucket, rarity and source stage. The older `openChest(kind:)` path remains for compatibility, but both routes share the same loot retention, Soul Stone, SFX and save behavior. Exact chest contents, drop rates and probability rules remain intentionally unimplemented.
- Added the individual active-skill page value tables for the 36 currently named hero skills. Battle damage now uses checked Lv1 percentage values for damage skills, Priest healing/continuous blessings read checked Lv1 utility values, and `天堂之怒` has been corrected back to `COOLDOWN` based on the current detail page. Attack-count cadence, buff durations and multi-hit/summon/status details remain intentionally incomplete.
- Added the checked passive-skill catalog from the current `/zh/skills/` and `/en/skills/` passive table rows: 108 rows total, 18 per hero-class prefix, preserving ID, English name, stat, value type and value. This is intentionally data-only; exact original passive unlock paths, stat-pipeline integration and runtime effects remain unimplemented.
- Added per-member support skill execution: each unlocked non-primary party member now makes its own visible support attack, rotates through available class `COOLDOWN` skills, and contributes equipped `CONTINUOUS` Priest blessings to party attack and incoming-damage modifiers, with support skill log rows and skill-cast audio routing. This is still a scaffold using shared hero level and Lv1 skill values until independent member levels/equipment/cooldowns are verified.
- Added runtime support-member HP/down state and Priest `复活` execution: defeated support members stop attacking/casting, `40601` waits for a fallen support target, and the checked Lv1 value revives that support with `300%` max HP. This remains a scaffold until exact original target priority, independent support stats/equipment/levels, animation timing and downed cooldown behavior are verified.
- Added the first Cube runtime hook: `CubeProgress` persists total infused item count and total XP, backpack items can be consumed through the Inventory panel's Cube action, and gained XP uses the checked `Rarity.cubeExperience` table. Locked items cannot be infused. This intentionally stops at checked XP accumulation and does not claim the original Cube level, reward, or effect rules are implemented.
- Added the first Alchemy runtime hook: backpack items can be consumed through the Inventory panel's Alchemy action, gained gold uses the checked `Rarity.alchemyGoldValue` table, and locked items are protected. This intentionally stops at checked rarity gold conversion and does not claim full original Alchemy flow, Synthesis interaction, economy context, or rate semantics.
- Added the first Synthesis runtime hook: the item model now persists `itemLevel`, the Inventory panel previews eligible Synthesis count, and `GameEngine.synthesizeItems(rarity:)` consumes 9 unlocked same-rarity backpack items into one next-rarity item while preserving the highest consumed item level and protecting locked items. The preview now shares the runtime input-selection rule, separates unlocked inputs from locked excluded items, shows the guaranteed output item level only when the checked 9-item recipe is ready, and flags the source-mentioned skip-tier / item-level downgrade variants as unverified instead of inventing probabilities. This intentionally stops at the checked 9-to-next-tier rule and does not claim the original skip probabilities, failure handling, Synthesis-level costs or level-downgrade formula.
- Added the first active cooldown-buff state: Hunter `快速装填` now applies a +50% attack-speed buff for the checked Lv1 value of 3 hero attacks, instead of only logging a generic buff event. Other timed buffs still need exact original duration/state modeling.
- Reworked Knight `神盾领域` from a generic buff log into an active party damage shield: while active it applies the checked Lv1 `500` damage-block value as a consumable shield pool against incoming monster damage for the main hero and living support members. The exact original buff duration, field geometry and shared-pool versus per-ally shield semantics are not exposed by the checked page, so duration remains a cooldown-length stand-in and field coverage remains a party-scaffold approximation.
- Reworked Knight `不屈意志` from a no-op cooldown entry into a lethal-damage rescue: it is no longer auto-cast by the cooldown rotation, triggers when monster damage would defeat the Knight, restores the checked Lv1 `300%` max HP value above the normal HP cap, logs the skill, and persists one consumed stage key so rebuilt battles in the same stage do not refresh it. The Wiki page proves the visible `每关一次` wording but not retry/reset edge cases, so exact reset semantics still need stronger evidence.
- Added the first kill-triggered secondary skill rider: Slayer `粉碎强击` now emits a `粉碎强击冲击波` damage log and applies `350%` attack damage to other alive enemies in the current wave when the main hit kills its focused target. That derived shockwave log now drives a compact shockwave-ring trajectory and shockwave-impact cue, while the primary `粉碎强击` hit keeps the ordinary physical slash cue. This is still a wave-wide stand-in for “surrounding enemies” until exact original AOE geometry, target spacing and shockwave keyframes are verified.
- Reworked Knight `盾牌冲锋` from generic focused damage into a current-wave melee collision scaffold: activation now applies the checked Lv1 `3000%` physical damage value to every alive enemy in the wave for both main-hero and support-member casts, and its battle log now drives a dedicated charge-dash visual trail instead of using the generic no-trajectory melee treatment. Exact charge path, collision width/count, target ordering, timing, keyframes and any knockback/stun behavior remain unimplemented.
- Reworked Knight `穿透突刺` from generic focused damage into current-wave melee range damage: when its attack-count trigger fires, it now applies the checked Lv1 `2500%` physical damage value to alive enemies in the current wave. Exact thrust path, collision width/count, target ordering, timing and attack-count cadence remain unimplemented.
- Reworked Knight `报应打击` from generic single-target damage into focused melee multi-hit damage: when its attack-count trigger fires, it now applies checked Lv1 `1500%` physical damage per strike and increases strike count as current HP gets lower. The current two-to-five strike HP buckets are a monotonic scaffold because the checked pages do not expose exact original HP thresholds or strike caps.
- Reworked Slayer `猛击跳跃` from generic single-target damage into current-wave melee range damage: activation now applies the checked Lv1 `3100%` physical damage value to every alive enemy in the wave for both main-hero and support-member casts, and its battle log now drives a dedicated leap-arc visual trail matching the checked non-English description that the Slayer leaps to an enemy before the landing shockwave. The English Wiki description conflicts with a `60201`-style kill shockwave, so no extra kill rider was added until stronger evidence resolves that discrepancy.
- Reworked Slayer `将军怒吼` from a generic buff log into an active crit-coefficient/stun scaffold: activation now applies the checked Lv1 `+500%` party critical chance coefficient as a 6.0x crit-rate multiplier for main-hero and support attacks, then delays alive current-wave enemy cooldowns by at least one attack interval and displays that stand-in as a `眩晕` enemy badge. The exact original stun duration, AOE geometry, stacking rules and critical-coefficient formula are not exposed by the checked page, so these parts remain placeholders.
- Reworked Slayer `大地强击` from generic single-target damage into current-wave range damage: activation now applies the checked Lv1 `3700%` physical damage value to every alive enemy in the wave, and its battle log now drives a ground-rupture trajectory plus earthquake-impact cue instead of the generic physical slash. The source page also describes earthquake rocks exploding when hit by physical AOE attacks, but rock entities, rock HP/positioning, explosion triggers and explosion geometry are not modeled yet.
- Reworked Slayer `旋转斧` from generic single-target damage into an active per-second range-damage buff: while active it applies the checked Lv1 `1000%` physical damage value each second to every alive enemy in the current wave. Its hit tick now also marks surviving hit enemies as bleeding and exposes the same compact bleeding badge used by `穿刺射击`. Bleed chance, bleed duration, bleed damage and extra damage against bleeding enemies remain unimplemented because the checked page does not expose the exact probability, duration or formula.
- Reworked Knight `神圣之刃` from an instant-heal placeholder into an active buff: while active it applies the checked +50% attack modifier and heals Lv1 `20` HP when the hero attacks. The exact original buff duration is not exposed by the checked page, so the current duration remains a cooldown-length stand-in.
- Reworked Ranger `迅捷觉醒` from a no-op buff log into an active attack-speed buff: while active it applies the checked Lv1 `+500%` attack-speed value as a 6.0x attack-speed multiplier. The exact original buff duration is not exposed by the checked page, so the current duration remains a cooldown-length stand-in.
- Reworked Priest `治愈` from a main-hero-only heal into a living-ally heal: main-hero and support-member casts now use the target ally's own max HP with the checked Lv1 `100%` value. The exact original target priority and cooldown behavior when no ally is wounded are not exposed by the checked page, so target selection currently uses a most-wounded living ally scaffold.
- Reworked Priest `圣域` from a main-hero-only over-time heal into a living-party healing buff: while active it applies the checked Lv1 `300` HP-per-second healing value to the main hero and living support members on battle ticks. The exact original buff duration, field geometry and support-member field-cast state are not exposed by the checked page, so duration remains a cooldown-length stand-in and field coverage remains a party-scaffold approximation.
- Reworked Priest `天堂之怒` from a one-shot direct-damage placeholder into an active attack-damage buff: while active, later hero attacks apply the checked Lv1 `4300%` lightning range-damage value across the live wave. The exact original buff duration and range geometry are not exposed by the checked page, so duration remains a cooldown-length stand-in and range is simplified to current-wave targets.
- Reworked Slayer `嗜血` from a generic buff log into an active sacrifice buff: activation now consumes half of current HP and applies the checked Lv1 `+4000%` attack-damage value as a 41.0x attack multiplier while active. The exact original buff duration is not exposed by the checked page, so duration remains a cooldown-length stand-in.
- Reworked Sorcerer `火球术` from generic single-target damage into current-wave range fire damage: activation now applies the checked Lv1 `2700%` fire damage value to every alive enemy in the wave for both main-hero and support-member casts. The exact original projectile travel, contact explosion timing, explosion geometry, elemental resistance interaction and fire-specific statuses remain unimplemented.
- Reworked Sorcerer `冰球术` from generic single-target damage into a current-wave cold multi-hit/slow scaffold: activation now emits two damage hits per alive wave enemy, splits the checked Lv1 `1500%` value across those hits, delays hit enemies' next attack tick, and displays the current slow stand-in as a `寒冷` enemy badge. Exact original projectile travel, multi-hit count/timing, slow amount/duration, cold resistance, freeze/chill behavior and explosion geometry remain unimplemented.
- Reworked Sorcerer `闪电术` from generic single-target damage into current-wave AOE lightning damage: activation now applies the checked Lv1 `2550%` lightning damage value to every alive enemy in the wave for both main-hero and support-member casts. Exact original forward electricity path, line/chain geometry, lightning resistance, shock/status behavior and falloff remain unimplemented.
- Reworked Sorcerer `烈焰九头蛇` from generic single-target damage into an active summon-damage scaffold: activation now records a summon buff, and battle ticks fire checked Lv1 `2300%` focused fire projectile damage from the hydra stand-in. Exact original summon duration, hydra/fireball cadence, target selection, projectile travel, splash behavior, support-member summon state and fire status/resistance interactions remain unimplemented.
- Reworked Sorcerer `暴风雪` from generic single-target damage into an active cold range damage-over-time/cooling scaffold: while active, battle ticks apply checked Lv1 `500%` cold damage per second across alive current-wave enemies, delay hit enemies' next attack tick, and display the current cooling stand-in as a `寒冷` enemy badge. Exact original storm duration, ice-block cadence/count, field geometry, cooling/freeze/chill rules, support-member storm state and cold resistance interactions remain unimplemented.
- Reworked Sorcerer `陨石打击` from generic single-target damage into current-wave fire range damage: activation now applies the checked Lv1 `5500%` fire damage value to every alive enemy in the wave for both main-hero and support-member casts. Its battle log now drives a dedicated falling-meteor trajectory and meteor-impact cue rather than the generic range-field/fire-burst pair. Exact original meteor travel/fall timing, impact delay, impact geometry, fire resistance, burn/status behavior and falloff remain unimplemented.
- Reworked Ranger `箭雨` from generic single-target damage into current-wave physical range damage: activation now applies the checked Lv1 `2150%` physical damage value to every alive enemy in the wave for both main-hero and support-member casts. Exact original arrow count, fall timing, target distribution, impact geometry, physical resistance and falloff remain unimplemented.
- Reworked Ranger `散弹射击` from generic single-target damage into a tracking-projectile fan-out scaffold: activation now applies the checked Lv1 `1620%` physical damage value as one projectile hit to each alive current-wave enemy for both main-hero and support-member casts. Exact original arrow count, projectile distribution, target priority, travel timing, physical resistance and overkill/falloff behavior remain unimplemented.
- Reworked Ranger `快速射击` from generic single-target damage into a focused physical projectile volley scaffold: when its attack-count trigger fires, it now applies the checked Lv1 `1320%` physical damage value as multiple projectile hits instead of a single generic hit. Exact original arrow count, shot spacing, target retargeting and attack-count cadence remain unimplemented.
- Reworked Ranger `穿透之箭` from generic single-target damage into a physical projectile pierce scaffold: when its attack-count trigger fires, it now applies the checked Lv1 `2440%` physical damage value to every alive enemy in the current wave. Exact original pierce line geometry, collision width, target ordering and maximum pierce count remain unimplemented.
- Reworked Ranger `穿刺射击` from generic single-target damage into a lodged-arrow/bleeding scaffold: when its attack-count trigger fires, it adds a lodged arrow to the focused enemy, applies the checked Lv1 `+1000%` physical projectile damage per lodged arrow, marks bleeding after 3 lodged arrows, and now renders a compact red status badge for bleeding active/side enemies in the battle strip. Exact original lodged-arrow duration, bleed damage, bleed tick cadence, bleed duration and bleed stacking remain unimplemented.
- Reworked Hunter `爆炸弩箭` from generic single-target damage into a current-wave fire projectile explosion scaffold: when its attack-count trigger fires, it applies the checked Lv1 `4840%` fire damage value to every alive enemy in the wave. Exact original projectile travel, impact explosion geometry, fire resistance, burn/status behavior, falloff and attack-count cadence remain unimplemented.
- Reworked Hunter `寒霜弩箭` from generic single-target damage into a current-wave cold projectile explosion/freeze scaffold: on cooldown, it applies the checked Lv1 `2100%` cold damage value to every alive enemy in the wave, delays hit enemies' next attack tick as a freeze stand-in, and displays the stand-in as a `冻结` enemy badge. Exact original projectile travel, impact explosion geometry, freeze duration/stacking, cold resistance, freeze/chill status behavior and falloff remain unimplemented.
- Reworked Hunter `充能陷阱` from a generic buff log into an active trap-trigger scaffold: activation arms a pending trap, and the next party elemental damage skill detonates it as `充能陷阱爆炸` physical current-wave damage using the checked Lv1 value table as the damage baseline. Exact trap count, placement, duration, trigger filtering, explosion formula/radius and multi-trap behavior remain unimplemented.
- Reworked Hunter `弩炮塔` from generic immediate single-target damage into an active summon-damage scaffold: activation now records a turret buff, and battle ticks fire checked Lv1 `1750%` focused physical projectile damage from the turret stand-in. Exact turret duration, firing cadence, target selection, projectile travel, summon placement and support-member turret state remain unimplemented.
- Reworked Hunter `电击弩箭` from generic single-target damage into a lodged bolt plus lightning-current scaffold: the attack-count trigger now logs the checked Lv1 `2700%` focused lightning projectile hit, then activates `电击弩箭电流` to apply checked Lv1 lightning range damage over time across alive current-wave enemies. Exact current duration, tick cadence, nearby-target geometry, lodged-target rules, projectile travel and lightning status/resistance behavior remain unimplemented.

## Next Fidelity Priorities

1. Replace placeholder chest contents/Soul Stone drop rules with exact original rates once a complete drop table or in-game capture is available.
2. Replace the remaining stage placeholders with exact original wave timing/targeting rules, exact monster ATK/DEF, exact box probability/conditional reward rules, exact sprites for every approximate monster mapping, and stage-specific monster animation frames/scale for every mined monster name.
3. Replace the current party/Rune Tree runtime scaffold with high-fidelity systems: executable costs/effects/layout for the 197-node source Rune Tree, exact reset price/refund/point semantics beyond the current local checked-cost refund, exact point/gold economy beyond checked formation command runes, approximate-cost Rune of Awakening, current no-price Rune of Repose/offline +10% boost nodes, current no-price first Rune of Expansion inventory-capacity scaffold, exact original active skill equip UI/order/cost constraints beyond the current per-class picker, independent member HP/equipment/skill levels, exact support cooldown/attack timing, and exact class/support formulas.
4. Expand items beyond the current taxonomy, source-page aggregate scaffold, and clean type-level icon placeholders: exact per-rarity/per-affix 5,760 item records, exact stat affixes, full per-variant icon/name progression, exact original icon art for item variants, exact material use/effect systems beyond the checked material taxonomy, full Synthesis probabilities/skip/failure/level-downgrade semantics beyond the current checked 9-to-next-tier hook, full Cube level/reward/effect semantics beyond the current checked-XP infusion, and full Alchemy flow/context beyond the current checked rarity-gold conversion.
5. Expand skill execution from the current trigger-type scaffold to exact original rules: per-skill formulas, exact attack-count intervals, passive unlock/effect application, durations, summons, status effects, resurrection, support-member independent skill state and party targeting.
6. Use the deterministic local battle-scene render plus the Steam battle-scene audit as the baseline for rendered screenshot comparison, then tune exact animation timing, sprite scale, lane anchors, HP bar dimensions, stage pill offsets and hit/loot feedback.
7. Use the new Steam Trailer audio audit as a broad loudness/energy baseline, then obtain in-game captures or authorized assets before claiming exact original per-event SFX parity.
