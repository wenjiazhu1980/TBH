# Steam Battle Scene Audit

Last reviewed: 2026-06-18

## Scope

This note records reproducible technical checks for the official `battlescene` media currently exposed by the `TBH: Task Bar Hero` Steam store page. It does not store official video, derived clips, or extracted frames in the repository.

Primary source:

- [Valve/Steam, 2026-06-16, Store page HTML for AppID 3678970: `https://store.steampowered.com/app/3678970/TBH_Task_Bar_Hero/`]
- [Valve/Steam CDN, 2026-06-16, `extras/battlescene` MP4/WebM extracted from the Store page]

Source limitation: these are official Steam/Steam-CDN sources, but they are one source family, not independent confirmations. Confidence is High for stream metadata and visible layout observations at review time; confidence is Medium for animation semantics because the source is a short promotional loop rather than instrumented gameplay capture.

## Reproduction

Run:

```bash
scripts/audit-steam-battle-scene.sh
```

Optional sampled frames for local inspection:

```bash
KEEP_FRAMES=1 scripts/audit-steam-battle-scene.sh
```

Local app screenshot audit:

```bash
scripts/audit-local-battle-scene.sh
```

Local battle-hero sprite audit:

```bash
scripts/audit-local-hero-sprites.sh
```

Deterministic local render audit without opening the menu-bar app:

```bash
RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh
```

Render a deterministic local scene PNG directly:

```bash
swift run --disable-sandbox TBH --render-battle-scene /tmp/tbh-rendered-battle-scene.png
```

Analyze an existing local screenshot without taking a new one:

```bash
SCREENSHOT_PATH=/tmp/tbh-battle-hero-art-check.png scripts/audit-local-battle-scene.sh
```

The script:

- Fetches the official Steam page into a temporary directory.
- Extracts the current `extras/battlescene` MP4/WebM URLs.
- Uses `ffprobe` to print stream metadata and counted frames.
- Uses `ffmpeg showinfo` to sample the timeline without writing frames by default.
- Extracts temporary sample frames and reports lower-ground platform geometry, including x/y ratios and platform width ratio.
- Extracts two temporary motion frames, currently frame `0` and frame `8`, and reports broad frame-to-frame motion pixels for the full frame, lower platform, and non-platform regions.
- Deletes temporary files on exit unless `KEEP_FRAMES=1` is explicitly set.

The local screenshot script:

- Opens the running TBH menu-bar popover, captures a temporary screenshot, and deletes it by default.
- If `RENDER_BATTLE_SCENE=1` is set, or if no running TBH process is found, renders deterministic SwiftUI battle scene snapshots through `TBH --render-battle-scene` and analyzes those PNGs instead of depending on desktop capture permissions.
- Retries once because clicking the status item can close an already-open popover.
- Fails explicitly if `screencapture` returns a blank/black image.
- Detects the warm battle ground strip and estimates local scene ratio from the configured `0.14` lower-ground height ratio and `0.90` local platform width ratio.
- Fails if the estimated local battle-strip ratio falls outside `0.40-4.65` or if the warm ground band exceeds `40%` of its detected platform width.
- For deterministic renders, also checks the battle-lane regression guard: the primary hero lane must contain enough foreground pixels, enough steel-gray Knight silhouette pixels, and a clear left-to-right centroid gap between the primary hero and support heroes. It also checks that the upper-left stage pill has visible text and a dark backing, that the primary/support HP bars are present, that the enemy-side top HP frame spans a stable width, and that deterministic cold skill trajectory and impact cues appear in the battle lane. `CHECK_PARTY_LAYOUT=1 SCREENSHOT_PATH=...` enables the same guard for a saved screenshot.
- For deterministic renders, also renders `playerStatusRow` and `playerStatusRowCrowded` fixtures through the same app snapshot path. The normal row verifies visible non-dark pixels, light text/icon pixels, gold blessing pixels, teal warding/trap pixels and green active/shield pixels; the crowded row verifies that the shared first-five plus overflow-count presentation remains visibly rendered. The DEBUG self-test also verifies the fixture's badge count/order and the shared first-five plus overflow-count logic used by the SwiftUI row.
- For deterministic renders, also renders a `battleLogPanel` fixture through the real SwiftUI `BattleLogPanel`. The audit verifies non-dark panel content, light title/text pixels, visible hero blue rows, support purple rows, monster red rows, and critical-hit orange pixels so the compact battle log cannot regress to a blank or monster-only-looking panel without failing the screenshot gate.
- For deterministic renders, also renders `victoryRewardBanner` and `victoryLevelCapBanner` fixtures through the real SwiftUI `BattleResultBanner`. The audit verifies visible success, reward text, source-backed loot-icon/color and level-cap warning pixels so local reward explanations cannot regress to blank, dim or missing states without failing the screenshot gate.
- For deterministic renders, also renders a `completionSettlement` fixture through the real SwiftUI `CompletionSettlementView`. The audit verifies visible crown/title/stat/action/panel pixels so the local通关结算 and next-playthrough controls cannot regress to a blank, dim or action-less state without failing the screenshot gate.
- For deterministic renders, also renders an `inventoryPanel` fixture through the real SwiftUI `InventoryView`. The audit verifies visible backpack controls, worse-equipment handling, source-backed item-grid icon color, selected-item detail, equipment comparison deltas, synthesis preview context and rarity colors so the local inventory panel cannot regress to blank, text-only or non-comparable feedback without failing the screenshot gate.
- Supports `SCREENSHOT_PATH=...` so the same analysis can be run against a saved local screenshot when live screen capture is unavailable.
- Uses relative warm-band thresholds so both the deterministic `1872x4608` rendered PNG and larger desktop screenshots are accepted when their geometry matches the expected enlarged strip.

The local hero-sprite script:

- Checks all current `battle_hero_*` sprites for compact dimensions, alpha isolation, transparent edges, visible-pixel coverage and HP-bar green runs, and now reconstructs each expected `battle_hero_*` from its corresponding `official_hero_*` sprite to verify the documented identity-safe fallback provenance.
- Prints the legacy broad `battle_*` screenshot crops separately as reference-only contamination evidence instead of treating them as runtime resources.
- Fails when current runtime battle heroes look like oversized screenshot blocks, opaque UI crops, blank/mostly transparent files, or sprites containing embedded HP-bar green runs.

## Current Media Evidence

Extracted official URLs:

```text
https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/3678970/extras/8d14259cbca180cea4e366f03ffbce01.mp4?t=1780512075
https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/3678970/extras/8d14259cbca180cea4e366f03ffbce01.webm?t=1780512075
```

Current stream metadata:

```text
MP4: HEVC, yuv420p, 776x180, 30/1 fps, 6.133333s, 184 frames, no audio stream
WebM: VP9, yuv420p, 776x180, 30/1 fps, 6.133000s, 184 counted frames, no audio stream
Display aspect ratio: 194:45, about 4.31:1
```

Sampled timeline frames:

```text
0.0s, 0.5s, 2.0s, 3.0s, 5.5s, 6.1s
```

Sampled lower-ground platform geometry:

```text
0.5s: lower_ground_bbox=x:106,y:132,w:543,h:39, x_ratio=0.137-0.836, width_ratio=0.700, y_ratio=0.733-0.950
3.0s: lower_ground_bbox=x:106,y:132,w:543,h:39, x_ratio=0.137-0.836, width_ratio=0.700, y_ratio=0.733-0.950
5.5s: lower_ground_bbox=x:106,y:122,w:543,h:49, x_ratio=0.137-0.836, width_ratio=0.700, y_ratio=0.678-0.950
summary: width_ratio=0.700-0.700, x_start=0.137-0.137, x_end=0.836-0.836, y_start=0.678-0.733, y_end=0.950-0.950
```

Frame-to-frame motion sample from official video frame `0` to frame `8`:

```text
motion_frame_delta=8
motion_frame_interval_seconds=0.267
official_motion_pixels=26623
official_platform_motion_pixels=11920
official_non_platform_motion_pixels=14703
official_motion_percent=0.1906
```

Manual visual observations from sampled frames:

- The battle scene is a very wide strip, not a square or card-like arena.
- The top area is mostly dark empty sky/background.
- The active ground is a low warm lava/fire platform along the bottom, centered with dark side margins rather than filling the full video width.
- The stage pill appears at the upper-left of the active lane and reads `1-6` in the sampled loop.
- Combatants are grounded near the bottom line, with HP bars directly above units.
- The left side shows player-side combatants and the right side shows a small enemy-side object/target cluster.
- There is no central `VS` label, no decorative card frame, and no large vertical battlefield composition in the sampled frames.
- The loop contains visible flame/background animation and combatant movement/hit poses. The frame `0` -> frame `8` sample measures `26,623` changed pixels overall, including `11,920` on the lower-platform region and `14,703` outside it, so broad official motion is directly measured instead of only described visually. This does not expose exact attack rules, event timings, keyframe identity, or sprite-frame cadence by itself.

## App Implications

Current macOS app state:

- `BattleSceneView` uses a single horizontal arena, upper-left pill showing only the stage code, left party cluster, right enemy cluster, overhead HP bars, floating damage, metadata-driven skill trajectory and impact cues, source-checked Ranger arrow trails for `快速射击` / `散弹射击` / `箭雨` / `穿透之箭` / `穿刺射击`, source-checked Hunter bolt trails and impacts for `爆炸弩箭` / `寒霜弩箭` / `电击弩箭` / `电击弩箭电流`, source-checked movement trails for `盾牌冲锋` and `猛击跳跃`, a source-checked falling-meteor cue for `陨石打击`, source-checked ground/shockwave cues for `大地强击` and `粉碎强击冲击波`, and hit-triggered nudge/flash effects.
- The macOS popover battle scene now uses a readable `2304` pt viewport inside the expected `936` pt content width, giving about a `0.41:1` configured presentation ratio. This intentionally gives the macOS combat lane more vertical display area than the previous local layout and much more height than the original compact strip while keeping the bottom tab bar below the content, and constraining combatants with an independent visual-scale cap plus a `0.92` lower-lane baseline so the primary hero no longer crops into an oversized portrait-like figure or sinks below the HP-bar audit region.
- The battle strip now keeps a square, unframed scene edge with zero border line width, matching the sampled official media's lack of a decorative card frame.
- `BattleArenaBackdrop` now animates a low-rate pixel flame band across the warm platform strip, matching the official media's confirmed fire/background movement at a broad presentation level while keeping exact timing/keyframes unclaimed.
- Deployed heroes in the battle strip now use dedicated edge-transparent compact `battle_hero_*` figures. All six current `battle_hero_*` runtime files are generated from their corresponding `official_hero_*` sprites with connected portrait-frame/background pixels removed, preserving class identity as an identity-safe fallback after earlier partial screenshot trims could make battle-tab heroes read as the wrong class. `ss_04.jpg` still provides useful reference-only broad battle crops, but those `battle_*` resources contain HP bars, background, effects or UI fragments and are not used directly for deployed hero combatants. The primary hero is anchored on the left front lane and support heroes are arranged to the right, matching the official media's left-to-right party read more closely than the previous centered primary with a support figure on the far left. Release resource self-test now also rebuilds each expected `battle_hero_*` from raw `official_hero_*` samples, ignores only hidden RGB bytes behind fully transparent pixels, and rejects visible/alpha provenance drift, embedded HP-bar green runs, missing Knight color markers and fragmented Knight subjects.
- Scene HP numbers were removed from the arena layer, leaving compact HP bars above combatants because the official sampled frames show bars rather than numeric HP labels.
- Local wave and encounter counters were removed from the in-scene pill because the sampled official pill reads as a simple stage code (`1-6`); those counters remain available in the surrounding macOS header.
- The lower ground now uses a fixed `0.14` height ratio and `0.90` local platform width ratio. This keeps only subtle dark side margins in the enlarged macOS battle tab without letting the warm platform become vertically dominant; the official sampled media still records a narrower `0.700` lower-platform ratio.
- `scripts/audit-local-battle-scene.sh` can now analyze local rendered screenshots without storing them in the repository. It fails blank/black captures, requires a detected warm ground strip, requires the estimated local strip ratio to stay inside `0.40-4.65`, rejects over-tall warm ground bands, and rejects ground platforms that drift away from the local subtle-side-margin platform width. The lower bound is deliberately wider than the official `4.31:1` source ratio because the current macOS layout increases viewport height for readability.
- The local battle-scene audit's HP-bar sampling windows now match the enlarged `936x2304` viewport: the primary top HP bar is checked around the mid-height combatant lane, while support and enemy HP bars are checked around the current lower-lane cluster. The latest deterministic render keeps HP-bar visibility guarded after the battle-area enlargement.
- `scripts/audit-local-hero-sprites.sh` reports all current `battle_hero_*` sprites as compact transparent runtime resources with clear edges, no HP-bar green runs, and `source=True` provenance checks against rebuilt `official_hero_*` frame-removal outputs. It also quantifies why the older broad `battle_*` crops remain reference-only: `battle_priest.png` and `battle_ranger.png` include `hp_run=40` green HP-bar segments, while `battle_knight.png` and `battle_sorcerer.png` are oversized fully opaque screenshot blocks.
- `TBH --render-battle-scene <path>` now renders deterministic `1872x4608` PNGs from the same SwiftUI `BattleSceneView` used by the app. The default snapshot fixture uses a static opening battle state plus a fixed `寒霜弩箭` damage log so the visual audit does not fluctuate when randomized combat damage hits support members. Additional damage fixtures `rapidVolley`, `scatterShot`, `arrowRain`, `piercingArrow`, `skewerShot`, `explosiveBolt`, `meteorStrike`, `lightningStrike`, `shockBolt`, `shieldCharge`, `slamJump`, `axeSpin`, and `axeSpinBleedFollowUp` render representative source-category logs for `快速射击`, `散弹射击`, `箭雨`, `穿透之箭`, `穿刺射击`, `爆炸弩箭`, `陨石打击`, `闪电术`, `电击弩箭`, `盾牌冲锋`, `猛击跳跃`, `旋转斧`, and `旋转斧流血追击`, while utility fixtures render representative non-damage logs for `治愈`, `圣域`, `复活`, `神盾领域`, `天堂之怒`, `神圣之刃`, `迅捷觉醒`, `快速装填`, `将军怒吼`, `嗜血`, plus explicit floating critical and avoidance labels for `暴击`, `闪避!` and `格挡!`. The latest local render audit measured `critical_floating_pixels=748`, `dodge_floating_pixels=241` and `block_floating_pixels=343`, so the deterministic screenshot gate now verifies those top-lane feedback labels in the actual battle render rather than only checking SwiftUI style constants. The local full-frame motion percent is not directly equivalent to the official `0.1906` because the current macOS render intentionally uses a taller `0.41:1` configured viewport than the official media, but the normalized fields make future same-render regressions visible without depending only on absolute pixel counts. The party guard still checks that support heroes stay to the right of the primary hero, while the platform-width and pill-position guards cover regressions that the old full-width-ground layout could not detect. This gives CI and local shell runs a repeatable visual audit path even when macOS desktop capture is unavailable.
- Snapshot fixture handling now has an explicit coverage guard: `BattleSceneSnapshot.Fixture` is enumerable, invalid or missing `--render-battle-scene-fixture` values fail the CLI instead of silently falling back to `frostBolt`, and the base/motion render calls explicitly pass `frostBolt`. `scripts/audit-local-gameplay-fidelity.sh` statically compares every fixture case to the fixture names rendered by `scripts/audit-local-battle-scene.sh`, and the DEBUG self-test verifies that every fixture is categorized as scene, damage cue, utility cue, status row, log panel, reward banner, settlement, or full layout output. This protects the local deterministic visual audit from fixture typos or uncategorized new cues; it is not evidence of exact official animation parity.
- Hero-class snapshot selection now uses the same strict CLI boundary: omitted `--render-battle-scene-hero-class` still renders the default Knight scene, but missing or unknown explicit values fail instead of silently producing a Knight screenshot. The DEBUG self-test resolves every `HeroClass` case name and Chinese display name through the CLI parser, while `scripts/audit-local-gameplay-fidelity.sh` reports `battle_scene_snapshot_hero_class_cli_guard`. This protects class-specific hero-art screenshot automation from selector typos; it is not proof of exact original class-specific animation frames.
- Fixed-time snapshot selection now also fails closed: omitted `--render-battle-scene-time` keeps the live animated view path, but missing, non-numeric, non-finite or negative explicit values fail the CLI instead of falling back to live `TimelineView` time. The local render audit keeps fixed `0` and `0.267` second battle-scene samples for deterministic flame/combatant motion checks, matching the rounded official frame `0` -> frame `8` sample interval, while `TBH --self-test` and `scripts/audit-local-gameplay-fidelity.sh` guard that boundary through `battle_scene_snapshot_time_cli_guard`. This protects local animation regression metrics from nondeterministic frame selection; it is not proof of exact official frame timing.
- `scripts/audit-local-battle-scene.sh` can now run the same deterministic render fixture set against the packaged executable and packaged SPM resource bundle by setting `PACKAGED_BATTLE_SCENE_RENDER=1`. The script stages `dist/TBH.app/Contents/MacOS/TBH` and `dist/TBH.app/Contents/Resources/TBH-macOS_TBH.bundle` into a temporary CLI directory before rendering, because direct `.app/Contents/MacOS/TBH --render-battle-scene` can trigger AppKit application registration and abort on macOS. This verifies that bundled release payloads can render the checked battle strip and cue fixtures, not only that `swift run TBH` can render them from the build tree.
- The deterministic render audit now also includes source-category fixtures for `trapBurst`, `summonProjectile`, `shockBolt`, `shockCurrent`, `earthquakeImpact`, `earthquakeRockExplosion`, and `shockwaveImpact`, mapped to `充能陷阱爆炸`, `弩炮塔`, `电击弩箭`, `电击弩箭电流`, `大地强击`, `大地强击岩石爆炸`, and the derived `粉碎强击冲击波` log. The latest `RENDER_BATTLE_SCENE=1 scripts/audit-local-battle-scene.sh` run measured `damage_trap_teal_pixels=2956`, `damage_summon_fire_pixels=2937`, `damage_shock_bolt_pixels=4750`, `damage_shock_current_pixels=4081`, `damage_earthquake_pixels=3086`, `damage_rock_explosion_pixels=2687`, and `damage_shockwave_pixels=12606`, so local CI-style checks now fail if those trap, summon-projectile, lodged-bolt, current, earthquake, rock-explosion, or shockwave cues disappear from the deterministic battle strip. These are still compact source-category visual guards, not proof of exact original sprite art, keyframe timing, trigger placement, hit-frame geometry or projectile/rock entity behavior.
- The deterministic render audit now also includes a `contactPulseBaseline` fixture plus `heroContactPulse` and `monsterContactPulse` fixtures. These use the real battle log metadata to render compact contact-pulse rings at the enemy hit lane for hero-side damage and at the party hit lane for monster damage, then measure changed pulse pixels against the no-contact baseline in each region. The latest local run measured `hero_contact_pulse_pixels=1573` and `monster_contact_pulse_pixels=2905`, so local hit feedback cannot silently disappear while combat formulas remain unchanged. This verifies current local attack-contact readability in the enlarged battle lane; it does not prove exact original hit-spark sprite art, impact frame timing, screen shake, recoil distance, damage-frame parity or impact choreography.
- The deterministic render audit now also includes a `chaosBurst` fixture plus four source-backed monster incoming attack fixtures for fire, cold, lightning, and chaos. The latest local run measured `damage_chaos_pixels=5444`, `monster_fire_incoming_pixels=1590`, `monster_cold_incoming_pixels=1497`, `monster_lightning_incoming_pixels=2992`, and `monster_chaos_incoming_pixels=1437`, so the visual gate now rejects regressions where checked elemental monster attack metadata no longer produces distinct player-side hit-lane cues. This verifies local visibility for currently modeled incoming element categories; it does not prove exact original monster projectile sprites, travel timing, resistance formulas, or hit keyframes.
- The deterministic render audit now also includes an `enemyStatusEffects` fixture that activates the current local enemy `眩晕`, `寒冷`, `冻结`, and `出血` states and checks visible body-effect pixels around focused and side enemies in addition to the compact status badges. This verifies that modeled enemy statuses are readable in the battle lane instead of only appearing as tiny icons; it does not prove exact original enemy-status icon art, animation keyframes, duration, stacking, resistance or status-formula parity.
- The deterministic render audit now also includes a `playerStatusRow` fixture that renders the real bottom battle status row with source-checked Priest `力量祝福` / `守护祝福` continuous badges plus live `神盾领域` and `充能陷阱` counters. The latest local run measured `status_row_non_dark_pixels=9212`, `status_row_gold_pixels=160`, `status_row_teal_pixels=701`, `status_row_green_pixels=474`, and `status_row_light_pixels=1020`, so local checks now fail if the compact status row becomes blank, loses readable text/icons, or drops the blessing/shield/trap color signals. `TBH --self-test` now also guards the status row's deterministic badge order, verifies that the `playerStatusRow` fixture resolves to `力量` / `守护` / `神盾` / `陷阱`, and checks the shared crowded-row rule that shows the first five badges before folding the remainder into `+N`. The new `playerStatusRowCrowded` render fixture activates all currently modeled player-side statuses through the DEBUG battle buff path; the local audit measured `crowded_status_row_non_dark_pixels=12476`, `crowded_status_row_light_pixels=990`, and `crowded_status_row_overflow_light_pixels=90`, so the render gate now catches regressions where crowded badge folding or the visible `+N` count disappears. This verifies local visibility and ordering for modeled status feedback, not exact original buff-bar layout, icon art, duration or stacking rules.
- The deterministic render audit now also includes a `battleLogPanel` fixture that renders the real compact battle-log panel with retained hero/support highlights plus a long monster tail. The latest local run measured `battle_log_panel_non_dark_pixels=23028`, `battle_log_panel_light_pixels=6814`, `battle_log_panel_title_light_pixels=713`, `battle_log_panel_hero_blue_pixels=1225`, `battle_log_panel_support_purple_pixels=2007`, `battle_log_panel_monster_red_pixels=193`, and `battle_log_panel_critical_orange_pixels=375`, so the render gate now catches regressions where the panel becomes blank, loses its title/count region, stops showing hero/support rows, drops monster rows, or stops showing the critical-hit label. This verifies local readability of the current compact macOS log panel, not exact original combat-log typography, scroll physics, retention policy or row ordering.
- The deterministic render audit now also includes `victoryRewardBanner` and `victoryLevelCapBanner` fixtures that render the real victory banner with displayed rewards, source-backed loot presentation, multi-encounter reward detail and the local level-cap XP-stop warning. The latest local run measured `victory_reward_banner_non_dark_pixels=9165`, `victory_reward_banner_light_pixels=1758`, `victory_reward_banner_green_pixels=488`, `victory_reward_banner_rarity_pixels=781`, `victory_reward_banner_icon_pixels=1562`, and `victory_level_cap_banner_orange_pixels=1327`, so the render gate catches regressions where success state, reward text, loot color/icon, or the level-cap warning disappears. These checks verify local reward-readability and cap-warning visibility for the current macOS UI; they do not prove exact original victory-banner layout, reward economy, level curve or settlement presentation.
- The deterministic render audit now also includes a `completionSettlement` fixture that renders the real通关结算 view with the crown, playthrough title, final-stage line, kill/gold/death stats, retained-progress preview, and both defer/start-next-playthrough actions. The latest local run measured `completion_settlement_non_dark_pixels=58581`, `completion_settlement_light_pixels=7672`, `completion_settlement_gold_pixels=2296`, `completion_settlement_accent_pixels=35837`, and `completion_settlement_panel_pixels=8133`, so the render gate catches regressions where the settlement becomes blank, loses readable text/buttons, drops its crown/stat emphasis, loses the next-playthrough action accent, or loses the retained-progress/stat panel. This verifies local completion-settlement readability and action visibility; it does not prove exact original ending UI, new-game-plus economy, or original post-clear flow parity.
- The deterministic render audit now also includes a `battleTabLayout` fixture that renders the complete local Battle tab layout with the hero summary bar, enlarged battle scene, compact log panel and bottom menu bar in the `960x2554pt / content 2448pt / scene 2304pt / tab 46pt` popover footprint. The render gate catches regressions where the full layout goes blank, loses the enlarged warm battle lane, or drops the visible selected Battle tab in the bottom menu. This verifies current macOS layout integrity after moving the tab bar to the bottom; it is not proof of exact original window proportions, menu behavior, typography or platform-specific taskbar placement.
- The deterministic render audit now also includes an `inventoryPanel` fixture that renders the real inventory panel with checked source gear progression items, a selected comparable weapon, ready Synthesis inputs, a locked item, Cube progress, direct backpack expansion context and the current worse-equipment handling selector. The latest local run measured `inventory_panel_non_dark_pixels=126724`, `inventory_panel_control_light_pixels=7225`, `inventory_panel_grid_colored_pixels=3452`, `inventory_panel_detail_non_dark_pixels=71581`, `inventory_panel_delta_green_pixels=1126`, and `inventory_panel_rarity_pixels=3677`, so the render gate catches regressions where backpack controls, source-backed icon color, selected-item details, comparison deltas or rarity feedback disappear. This verifies local inventory readability and comparison feedback using existing source-backed art paths; it does not prove exact original inventory layout, item variant database, affix rolls, drop weights or per-variant icon parity.
- The deterministic render audit now also includes a `characterPanel` fixture that renders the real character panel with a Hunter hero, checked source-backed equipment icons, visible direct unlock affordances for party slots 2 and 3, two active-skill loadout slots, passive skill chips and equipped gear rarity color. The latest local run measured `character_panel_non_dark_pixels=175205`, `character_panel_hero_colored_pixels=4393`, `character_panel_party_orange_pixels=5756`, `character_panel_skill_colored_pixels=17975`, `character_panel_passive_icon_pixels=13511`, and `character_panel_equipment_rarity_pixels=3155`, so the render gate catches regressions where the hero identity art, party unlock controls, active/passive skill presentation, or equipped source icon/rarity feedback disappears. This verifies local character-panel readability and source-backed visual wiring; it does not prove exact original character panel layout, Rune Tree economy, party-slot UI, skill icon parity or original character animation frames.
- The deterministic render audit now also includes a `chestPanel` fixture that renders the real Settings chest controls with batch opening unlocked, automatic opening status visible for Normal Monster / Stage Boss / Act Boss families, and four saved source-backed chest rows. The latest local run measured `chest_panel_non_dark_pixels=148188`, `chest_panel_button_blue_pixels=6820`, `chest_panel_auto_green_pixels=2543`, `chest_panel_icon_colored_pixels=10636`, and `chest_panel_rarity_pixels=7874`, so the render gate catches regressions where the all-chests button, auto-open status strip, individual open buttons, source chest icons or rarity/source-family colors disappear. This verifies local chest-control readability and source-backed icon wiring; it does not prove exact original chest UI layout, chest drop rates, auto-open Rune economy, cooldown timing, box contents or opening animation parity.
- The deterministic render audit now also includes an `originalFidelityPanel` fixture that renders the real Settings original-fidelity boundary panel. The latest local run measured `original_fidelity_panel_non_dark_pixels=208820`, `original_fidelity_panel_pill_pixels=483`, `original_fidelity_panel_text_light_pixels=12635`, `original_fidelity_panel_status_green_pixels=330`, `original_fidelity_panel_status_orange_pixels=1864`, and `original_fidelity_panel_status_gap_pixels=1340`, so the render gate catches regressions where source/runtime coverage pills, readable boundary rows, or covered/partial/gap state markers disappear. This verifies local visibility of the evidence-boundary UI; it does not prove exact original gameplay, Rune Tree economy, item database, monster art, animation-frame or SFX parity.
- The deterministic render audit now also includes a `runeEvidencePanel` fixture that renders the real Settings Rune evidence and local cost review panels together. The latest local run measured `rune_evidence_panel_non_dark_pixels=152238`, `rune_evidence_panel_pill_pixels=965`, `rune_evidence_panel_text_light_pixels=10226`, `rune_evidence_panel_orange_pixels=23383`, and `rune_evidence_panel_green_pixels=358`, so the render gate catches regressions where the evidence-tier summary, candidate-cost queue, pending-cost groups, readable text, or verified/pending state colors disappear. This verifies local visibility of the current Rune review UI; it does not prove exact original Rune Tree economy, complete per-node costs, branch/path costs, reset/refund behavior, point economy, or exact original Rune Tree layout parity.
- The deterministic render audit now also includes a `skillEvidencePanel` fixture that renders real Settings skill review UI: `LocalSkillRuntimeCoverageView`, source damage/range review, and the pending-source skill review panel. The latest local run measured `skill_evidence_panel_non_dark_pixels=120724`, `skill_evidence_panel_pill_pixels=23590`, `skill_evidence_panel_text_light_pixels=13909`, and `skill_evidence_panel_orange_pixels=30226`, with green/blue/purple at zero because the current visible panel state primarily uses readable text and orange pending-source markers. This verifies local visibility of the current skill source/pending review UI; it does not prove exact original skill ownership, formulas, delivery geometry, target rules, cooldown timing, action frames, VFX/SFX, or full 106-row runtime parity.
- The deterministic render audit now also includes a `passiveEvidencePanel` fixture that renders real Settings passive review UI: `SourcePassiveSkillDatabaseView` plus representative `SourcePassiveSkillRow` entries covering source-backed passive icons and the current missing-source-icon markers. The latest local run measured `passive_evidence_panel_non_dark_pixels=155972`, `passive_evidence_panel_pill_pixels=3770`, `passive_evidence_panel_text_light_pixels=14484`, `passive_evidence_panel_source_icon_pixels=21515`, `passive_evidence_panel_missing_icon_pixels=4511`, and `passive_evidence_panel_green_pixels=7436`, so the render gate catches regressions where passive source rows, source-icon samples, readable text, coverage pills, or explicit no-source-icon markers disappear. This verifies local visibility of the current passive source/icon review UI; it does not prove exact original passive unlock paths, passive tree layout, full runtime semantics, missing source icon families, or original passive-effect animation/SFX parity.
- Dedicated `BattleTrajectoryCue` cases now distinguish source-checked movement skills from ordinary melee: `盾牌冲锋` renders a charge-dash trail and `猛击跳跃` renders a leap-arc trail, while non-movement melee damage such as `穿透突刺` still avoids a movement trajectory. `BattleSceneSnapshot` now has dedicated `shieldCharge` and `slamJump` fixtures, and the local screenshot audit measured `damage_shield_charge_pixels=12763` and `damage_slam_jump_pixels=3004` in the latest deterministic render. These are compact visual feedback cues, not a claim that exact original charge/leap keyframes or travel timing have been recovered.
- Dedicated Ranger `BattleTrajectoryCue` cases now distinguish the checked arrow-skill descriptions from a single generic projectile: `快速射击` renders a multi-arrow volley, `散弹射击` renders a tracking-volley fan-out, `箭雨` renders falling arrows, `穿透之箭` renders a piercing shot, and `穿刺射击` renders a lodged-arrow hit. The deterministic render audit now gates all five visible Ranger arrow categories separately through `rapidVolley`, `scatterShot`, `arrowRain`, `piercingArrow`, and `skewerShot` fixtures, with the latest run measuring `rapid_volley_pixels=13326`, `scatter_shot_pixels=13459`, `arrow_rain_pixels=12734`, `piercing_arrow_pixels=13129`, and `skewer_shot_pixels=2766`. Exact original arrow count, projectile spacing, retargeting, travel timing and hit-frame placement remain unverified.
- Dedicated Hunter bolt cue cases now distinguish the checked bolt descriptions from the generic projectile/element fallbacks: `爆炸弩箭` renders an explosive bolt trail plus heavier fire explosion, `寒霜弩箭` renders a frost bolt plus cold explosion, `电击弩箭` renders a shock bolt/impact, and the derived `电击弩箭电流` log renders current arcs. The deterministic render audit now gates the primary lodged-bolt cue through the `shockBolt` fixture with `damage_shock_bolt_pixels=4750`, in addition to the `shockCurrent` rider gate. Exact original bolt travel timing, explosion radius/placement, freeze keyframes, current duration/arc geometry and hit-frame placement remain unverified.
- Dedicated Slayer axe-spin cue cases now distinguish the already-modeled `旋转斧` and derived `旋转斧流血追击` logs from generic physical slash feedback. The deterministic render audit gates the spin arc and bleed-rend trail through the `axeSpin` and `axeSpinBleedFollowUp` fixtures, with the latest run measuring `damage_axe_spin_pixels=1312` and `damage_axe_spin_bleed_pixels=1794`. These remain compact local source-category visual guards; exact original spin frames, hit radius, bleed timing, bleed probability and extra-damage formula remain unverified.
- `陨石打击` now renders a dedicated falling-meteor trajectory and heavier meteor-impact cue instead of sharing the generic range-field/fire-burst pair used by ordinary fire skills such as `火球术`. This follows the checked skill text that it summons a meteor, but exact meteor sprite art, fall timing, impact delay and hit-frame placement remain unverified.
- `大地强击` now renders a ground-rupture trajectory and earthquake-impact cue, while the local `大地强击岩石爆炸` scaffold renders a separate rock-burst trajectory and rock-explosion impact cue after later modeled physical AOE consumes armed rock charges. Only the derived `粉碎强击冲击波` log renders a shockwave-ring trajectory and shockwave-impact cue. The primary `粉碎强击` hit remains an ordinary physical slash, so the visual distinction matches the currently modeled combat rider. Exact original rock entities, rock HP/positioning, physical-AOE trigger matrix, shockwave radius and keyframe timing remain unverified.
- Non-damage logs now use separate `BattleUtilityCue` visuals instead of being forced through damage trajectory or impact effects: `治愈` shows a heal pulse, `圣域` shows a sanctuary pulse, `复活` and `不屈意志` show a rising revive cue, `神盾领域` shows a shield field, `天堂之怒` shows a lightning utility cue, `神圣之刃` shows a sacred-blade glow, `迅捷觉醒` shows a blue haste streak, `快速装填` shows a green reload-haste cue, `将军怒吼` shows a roar cue, `嗜血` shows a blood surge, and other modeled buff logs show a compact aura. The deterministic render audit now gates the Priest `圣域` and `天堂之怒` utility fixtures with `utility_sanctuary_pixels=2267` and `utility_wrath_of_heaven_pixels=1927`. These cues align the macOS battle lane with the source-checked skill categories at a feedback level, but exact original heal, sanctuary, wrath, revive, shield-field, sacred-blade, attack-speed, shout and bloodlust keyframes remain unverified.

Remaining fidelity gaps:

- The deterministic macOS render now uses an enlarged local battle-scene aspect ratio configured at about `0.41:1` and estimated from the detected warm ground/flame geometry; the menu-bar popover is still a constrained macOS translation, so exact official aspect, crop, live desktop placement and taskbar-window parity remain unclaimed.
- Local screenshot analysis is available through `SCREENSHOT_PATH=...`, and deterministic rendering is available through `RENDER_BATTLE_SCENE=1` or `TBH --render-battle-scene`. Direct live `screencapture` can still return black images in the current shell/permission state, and the audit script reports that as a failure instead of treating it as a pass.
- Exact idle, attack, hit and death keyframes remain unverified; the new `combatant_motion_pixels` gate proves local deterministic motion is visible, not that original keyframes or timing have been recovered.
- Exact flame/background frame timing remains unverified; the current animated flame band is a presentation-level substitute based on sampled official movement.
- Exact sprite scale, x/y lane anchors, HP bar dimensions and stage pill offsets still need frame-level comparison against stronger original captures before claiming parity; the current deterministic audit only guards local regressions against the visible official strip composition.
- This media has no audio stream, so it cannot prove battle SFX timing or mix.
